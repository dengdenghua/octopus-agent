---
name: octopus-agent-dev-environment
description: octopus-agent 项目的开发环境搭建方式与已知预存测试失败基线
metadata: 
  node_type: memory
  type: project
  originSessionId: 73dba696-f7dc-4ee6-899f-63f46a601f05
---

octopus-agent（/Users/dangbei/Public/octopus/octopus-agent）的环境（2026-06-11 搭建）：

- 系统只有 Python 3.9，项目要求 ≥3.11。已用 uv（装在 `~/.local/bin/uv`）拉取 CPython 3.12.13 建好 `.venv`。跑测试/ruff 用 `.venv/bin/python -m pytest|ruff`。
- 系统原本无 Node。已装独立 Node 22 到 `~/.local/node/bin`，pnpm 10.26.2 经 corepack 激活。前端命令需 `export PATH="$HOME/.local/node/bin:$PATH"`。
- 仓库到达时**不是 git 仓库**（目录拷贝，混入了两个时间点快照的 `(1)` 副本文件）。已 `git init` 并建基线提交；被清理的副本在 `../octopus-agent-cleanup-backup-2026-06-11/`。
- 已知**预存**后端测试失败基线（与改动无关，经 git stash 对照验证）存于 `/tmp/post_change_failed.txt`，主要包括：test_tentacle_dashboard（9 个 ERROR，顺序性 flake，单独跑会过）、test_bugfix_demo×3、test_cli_smoke、test_app_meta_endpoints×4、test_anthropic_router、test_auto_docs_fresh、test_lint self-check、test_market_skills、test_parallel_agents_router_security、test_subprocess_backend 内存限制。前端预存失败 3 个：prompt-input-files.test.mjs×1、workspace-sidebar.test.ts×2。
- 仓库还有约 177 个预存 ruff 风格问题（E402 延迟导入为主），与 CI 宣称不符；test_lint 自检也因此失败。

**2026-06-19 复核的全量基线**（`.venv/bin/python -m pytest tests/ -rf`，经 stash 全量对照确认与改动无关）：**14 failed / ~6851 passed / 32 skipped / 5 xfailed**。完整 14 条 FAILED（用 `-rf` 才全列；别用 `| tail -N` 截断，否则只看到末尾几条会误判数量）：test_react_auto_checkpoint::TestCheckpointInterval×6（代码 `_DEFAULT_CHECKPOINT_INTERVAL=10`，测试仍断言 unset→0 的旧契约漂移）、test_llm_judge_bootstrap::TestFlagResolution×2、test_adaptive_immunity::TestAuditOnlyLearn×1、test_app_meta_endpoints::TestSkills×1、test_auto_docs_fresh×1、test_openapi_snapshot×1、test_orphan_module_check::TestBaselineGreen×1、test_system_prompt_size×1。另跳过 2 个：tree_sitter 缺失、workflow_editor_router 未实现。**顺序性 flake 警告**：`test_write_skills::TestBackgroundExec::test_background_output_survives_registry_loss` 在某些收集顺序下会失败、单独跑必过——新增测试文件改变收集顺序可能让总数瞬时变 15，非回归。这套基线取代上面 2026-06-11 的旧清单。

**2026-07-03 收尾基线(同日,7 项审计修复 + MCP OAuth 接线全部落地后)**:全量 `-rf` 复核 = **18 failed / 8423 passed**,清单固定不变(test_cowork_async_runner 测试顺序污染、test_doc_reality_alignment、test_evolution_modules、test_openai_gateway_sse_deep 已知 flaky、test_organization(_router)×3、test_production_readiness_gate×4、test_react_guards_surface、test_realtime_cerebrum、test_rules_persistence(gene_locks 拦)、test_workflow_applier×3)——**全部逐条 stash 对照过是 clean HEAD 就有的,与本轮改动无关**。中途 openapi 快照+docs/auto 因新增 MCP OAuth 路由过期跳出过 2 个失败,已用 `OCTOPUS_OPENAPI_WRITE=1 pytest tests/test_openapi_snapshot.py` + `gen_wiki.py` + `make frontend-types` 重新生成后消失(这不是回归,是生成物没跟上新路由,见 [[octopus-agent-generated-artifact-drift]])。前端全量 vitest 1046/1046 全绿。取代上面 06-19/07-03(22条)两版基线。:**22 failed / 8389 passed / 52 skipped / 5 xfailed**——比 2026-06-19 的 14 涨到 22,失败**集合几乎完全换血**。经 `git stash` 对照抽样确认是已提交回归,非 WIP、非环境。**已根因两个并当场修复**(见 [[octopus-agent-journal-redactor-corruption-fix]]):① `runtime/memory/journal/journal.py` 的 `write()` 对**已序列化的 JSON 整行**跑 Redactor,`phone` 正则(`\d{1,3}...\d{2,4}...\d{3,4}...\d{3,4}`,本质"9-15个连续数字无需分隔符")会命中 `latency_ms` 等浮点数值里的数字串,把 `[REDACTED:phone]` 文本拼进 JSON 数值字面量,破坏语法——之后整份 journal 每次 `read_all()` 全部解析失败静默丢事件。修法:redact 后校验仍是合法 JSON,不合法则回退未脱敏行(电话号码永远不会是裸 JSON 数字,破坏语法的匹配必是误报)。修复+回归测试(`test_journal_audit_budget_redactor.py::test_redaction_never_corrupts_json_numeric_literal`)已验证解决 `test_reflex_integration::test_ping_loop_counts_as_success`、`test_resume::test_real_resume_continues_from_checkpoint`。②`runtime/sensing/gateway/control_sessions_router.py`/`openai_gateway_router.py`/`stream_handler.py` 的两个安全 HIGH 已修(见 [[octopus-agent-audit-2026-07-fixes]])。**其余 8 个失败是不相关的独立 bug,非同一簇**:`test_rules_persistence::test_apply_writes_file` 是 `StaticPlanner.apply_rewrite_proposals` 被 `gene_locks blocked` 拦(gene-lock 策略问题,与 journal 无关);`test_workflow_applier`×3、`test_production_readiness_gate`×4、`test_realtime_cerebrum`、`test_react_guards_surface::TestSecretInPayloadGuard`(误报"Inline assigned credential"，也是检测逻辑问题非 journal)——**均未 triage,各自需要独立根因**。`test_auto_docs_fresh` 仍失败(docs/auto 未随代码重跑 `gen_wiki.py`)。CI 从没抓到这些:exception_audit 闸门先挂,pytest 在 CI 里根本没跑到。取代上面 06-19 基线。

**2026-07-03 晚终局基线**(HEAD `0f01d7415`):干净 worktree 全量 = **8476 passed / 0 failed / 46 skipped / 5 xfailed**——预存失败已全部清零。收尾三类修复:① flappy 四件套(react_guards/realtime_cerebrum/evolution/sse_deep)被 Codex `e9bc76baf` 重新钉约到现行为;② **"本机绿、干净 checkout 红"一族**(evolution×4+gate×4 因插件 draft 覆盖依赖 gitignore 本地插件;plugins 元数据测试×3 钉死 gitignore 的 browser 插件;webui 自检依赖 gitignore 的 frontend/dist)——Codex 提交 permission-review-fixture 插件解决前者(我先做的空真修法与之重叠且弱化闸门,已主动撤销;**判据:fixture 保留"发现机制坏掉时报警"的证据性,空真会静默放行**),我用 `0f01d7415` 把后两类改 hermetic(钉已提交的 product-design 插件;测试内造带真 pyproject 的假根);③ control_sessions IDOR 由 Codex `f0d9559c6` 修。**验证预存失败必须在干净 worktree 里跑**——主仓有 gitignore 产物撑着会假绿。

**2026-07-03 下午 WIP 合并收尾**(84 文件 ~7k 行 Codex WIP 审查+分主题提交,HEAD ≈ `687a57edd`):当时判定的预存失败(realtime_cerebrum/evolution_modules/sse_deep/react_guards_surface,在 WIP 基线 `2aee4e870` 上同样红)——已被上面终局基线清零。**大坑:组合批次假红**——organizations_router×3/workflow_applier×3/rules_persistence/organization/cowork_async_runner 在多文件同进程批跑时因跨测试污染失败(gene_locks 报 "requires level 3 · current level 2"),**单独跑/按文件跑全绿**;归因前先单跑,别当回归修。另两个确定性事实:① `test_production_readiness_gate` 把 `compute_automation_radar` 的分数**钉死在测试里**(4 处),radar 改动让 octopus 分数变了就要同步改钉子(96 为 2026-07-03 值);② MCP OAuth callback(`GET /api/mcp/oauth/callback`)在 **两层** 鉴权都要豁免(mcp_router 的 router-level `_auth_dep` + app.py 的 `_LEGACY_CONTROL_PLANE_PREFIXES` 中间件),否则 auth-on 部署 OAuth 死锁——已修并有测试钉住。Codex 并发会话会**主动分批提交自己的 WIP**(且会把我的 working-tree 修复一起卷进它的 sweep 提交),协作模式=我快速提交主题切片、它在其上继续;提交前必须 `git log -1` 防漂移。

**2026-06-27 前端预存失败**（经"把 chat-input-box 临时 `git checkout 948fe57e --` 回退后仍失败"对照确认，与个人模式/REC/浏览器合并接线无关）：**6 个 vitest 失败,全部由 Codex 的 `4f53697d`（"restore minimal workspace interface snapshot"，同一个把侧栏 agent 列表砍窄、我用 8ae70d74 修回的提交）引入,源与测试 out-of-sync**：`workdir-selector.test.tsx`×5（portaled menu / Electron 选择器 / filesystem roots / muted 空态 / desktop 选择器,多为 `getByTitle("Choose workspace folder")` 类查询对不上）、`local-slash-dispatch.test.ts`×1（`/permission`→onPermissionModeChange 映射）。`4f53697d` 同时改了 workdir-selector.tsx/permission-indicator.tsx/permissions.ts。**未修(超出接线范围,属 Codex 债)**。前端单文件对照法：`git checkout <baseline> -- <疑似源文件>` 跑失败文件,再 `git checkout HEAD --` 恢复。

**2026-07-03 晚基线(HEAD `eeda629d0`,gene-locks 假红根治后)**:全量 `-m 'not slow and not integration'` = **5 failed / 8457±**,固定清单:`test_cowork_async_runner::test_runtime_does_not_enable_runner_without_subagent_executor`(顺序污染,单跑绿)、`test_evolution_modules::…surpass_certification`(flappy)、`test_openai_gateway_sse_deep::…step_before_runtime_returns`、`test_react_guards_surface::…test_non_code_mode_silent`(flappy)、`test_realtime_cerebrum::test_cowork_project_mode_accepts_task_control_command`。**上一条"组合批次假红(gene_locks requires level 3)"已根治**(commit `eeda629d0`):根因=`simple_gate._store_path()` 是 cwd 相对的 `data/gene_locks.json`,测试读写仓库活状态,serve 跑成 production+level0 就把 organization/organizations_router×3/rules_persistence/workflow_applier×3 批跑翻红;现 `_store_path` 走 `app_paths().data_dir`(同 pause_control/capabilities,contract 测试钉住)+ conftest autouse `_isolate_gene_locks` 每测试私有 store——**别再把这批当污染 triage,再红就是真回归**。`data/gene_locks.json` 测试套件不再触碰(mtime 实证)。同日 .venv 新装:trafilatura 2.1/watchdog 6.0/anthropic SDK/tree-sitter 0.26+python+typescript——extract/skin/anthropic-router/code-intel 四个 AVAILABLE 门翻 True,`test_code_search_ast.py` 从 skip 转跑(13 测);TS 语法加载 bug 已修(`0880e28ce`,tree_sitter_typescript 无裸 `language` 符号)。

**2026-07-03 深夜基线(HEAD `02730937f`)= 全绿 0 failed / 8463 passed**(全量 `-m 'not slow and not integration'`,复跑确认)。上一条"5 failed"清单全部根因修复,不再是预存失败:① react_guards secret 守卫=测试钉错契约(守卫**有意**无视 is_code_mode 任何模式都开火,docstring 写明;"误报"是旧误判);② evolution radar 钉子 95→96(`687a57edd` 更新 production_readiness_gate 时漏了这处);③ SSE deep=**真网关 bug**(`Journal.subscribe` 基类 no-op 使 `hasattr` 永真→`_stream_chat` 轮询回退死代码→未包 StreamingJournal 的部署丢全部 step 帧;修在 stream_handler 检查类是否真覆写);④ realtime_cerebrum=测试断言时序反了(reassign…run 命令审计顺序 intervention→project.run 是对的);⑤ cowork=conftest 新增 `_reset_subagent_runners`(建过 app 的测试泄漏 persistent/ephemeral runner 全局)。**唯一观察项**:`test_team_rooms_router::test_team_room_websocket_presence_and_events` 在 3 次全量里挂过 1 次(60s 超时,单跑+复跑均绿)——负载偶发,再挂优先查 room 广播的时序而非当回归。

验证新改动是否引入回归的标准做法：`git stash push -- <我的文件>` + 临时挪开新增文件，跑全量 `-rf` 比对**完整失败清单**（不是只比总数）。**两个坑**：① zsh 不对未加引号变量分词，pytest 多文件参数要显式列；② 别 `| tail` 截断 pytest 输出否则漏看失败。见 [[octopus-agent-audit-verification-lesson]]。
