---
name: octopus-agent-improvement-roadmap
description: octopus-agent 三大工作模式改进的已完成提交与剩余路线
metadata: 
  node_type: memory
  type: project
  originSessionId: 73dba696-f7dc-4ee6-899f-63f46a601f05
---

截至 2026-06-11 的改进进度（提交均在本地 main，无远端）：

已完成：
- `e1d4658` 基线入库（恢复 git）
- `893e4a1` F821 真 bug ×3（resume 端点 500 / cli_mcp asyncio / VlmConfig 注解）+ 287 处 ruff 自动修复
- `5145829` macOS /etc 敏感路径守卫失效修复（/private 符号链接）
- `f76e5f8` 第 1 组：reducer 索引化 + 生产页 hunk 决策 UI + diff 截断标志端到端
- `b3d7adb` 第 2 组：审批超时/断线语义区分（含 CancelledError 砸线程修复）+ 前端 resolver 清理
- `918bfc5` 自动诊断假阳性修复（缺 mypy/tsc 时门控）
- `3fba039` 发送失败恢复用户草稿（octopus:send-failed 事件）
- `3a5afac` delta 后端聚合（_ReactBridgeState 50ms/64 字符合帧，TTFT 不受影响）
- `0e95507` thread/resume 分页（EventLog.paginate_turns + ChatPage"加载更早"横幅，无 limit 全量兼容）
- `317a3f8` 浏览器感知增强（共享 browser_dom_js：selector 唯一性验证 + disabled/value/checked/role 字段，密码不回显）
- `3d77909` 浏览器会话加固（profile 目录 700 权限、崩溃哨兵 recovered_from_crash、copilot action 30s 超时解冻）
- `8dc521e` 文档对齐：docs/implementation-status.md 四态状态表（已接线/可选后端/休眠/未实装，逐行代码证据）+ architecture/constitution 状态横幅
- `15740e8` LLM-Judge 层接线：validation/bootstrap.py 按 safety.enable_llm_judge 在 serve 启动注册（默认关、fail-open）；休眠→可选后端
- `f73c85a` Immunity Memory 层（抗体记忆）：attack_memory.py 晶化/召回/持久化，TrustEngine 在 tolerance 后最先查（可拦信任 glob 内已知攻击者）
- `03b9f2b` 统一 BrowserBackend 接缝：Protocol + resolve_backend 优先级 + Mock
- `8db5b75` Immunity 自适应层：adaptive_immunity.py z-score 异常评分（滑动窗口基线、I4 冷启动/I2 自旁路、只收紧）；可选后端
- `eebf97b` 删除休眠 NATS/Redis nerve 总线（零消费者，进程内 bus.py 留存）
- `29daf55` 浏览器三轨真 adapter：browser_backends.py Electron/Playwright/Extension（可注入 transport 单测，包各轨现有函数）

用户决定的三项已全部完成。完整 22 提交。

剩余（均需真机/真环境，本地无法验证）：
1. 切换三轨现有 skill 调用点改走统一 BrowserBackend 接口 + 真机端到端联调（adapter 已就绪）
2. 浏览器登录态 OS keyring 加密（profile cookies 目前明文，目录已 700）
3. 自适应/抗体免疫的真实流量调参（实装已就绪，阈值需生产数据）

2026-06-11 深度评审（46 代理，31 条经调用链核实）新增待修清单，按优先级：
- 安全：fs_router/terminal WS 零鉴权且 _scope_roots 空时 fail-open 放行任意路径（fs_router.py:452,760）；tentacle coordinator 默认 0.0.0.0:8765/8766 无鉴权（coordinator.py:12,72）；schema.py:35 ImmunityConfig 默认仍含 mcp://*（与 trust_engine.py:30 注释矛盾，yaml 路径生效）；生产 app 默认允许客户端 approvalPolicy=never 旁路审批；quarantine 判决执行层无消费者 + TrustEngine.learn 生产零调用（自适应层等效休眠，implementation-status 第 17/19 行失实）
- 真 bug（小时级）：graph_runtime/runtime.py:489 `_gi`/`gi` NameError（并行重试必炸）；:646 _try_replan 用 raw_input= 构造 ParsedIntent 必 ValidationError 被吞（重规划从未生效）；bugfix-demo pyc 缓存陷阱（demos/bugfix_demo.py 需 -B，golden path 第一步必败）；Dockerfile /app/resources 无代码读取（容器丢 87+ 技能）；OTel 65 处埋点无 provider 安装（span 永不导出）
- 架构债：8 包 20/21 对双向依赖（仅 protocol 是干净叶子），无 import 方向 linter；双组装根硬编码默认值已漂移；接缝 broad-except 吞 TypeError/ValidationError 的系统性模式；god_file/exception_audit 棘轮基线失准；约 9000+ 行孤儿代码（ItemView 树、cli_registry、nerves/hooks、file_safety）
- 信任链：ci.yml 引用已删除的 test_nerves_cross_process_e2e.py、eslint `|| true`；reflex 的 delegate_to_workflow 前端可配置但执行层从未实装（workflow_editor_router 模块不存在）
- 未核查待跟进：间接提示注入威胁模型整体缺席；per-step Budget($0.20) 不约束子代理树总开销；Apache-2.0 发布合规（无 NOTICE、89 个 skills/public 零归属）

终审已修（commit ba8d1e4 + 5ae081e，均本会话改动引入项）：自适应层三重锁死（learn 接入 executor + 无预测冷启动 + 单轴 max composite + 阈值下限）；ci.yml 删 nerves-cross-process job；attack_memory 畸形 JSON 崩溃；PlaywrightBackend.extract 派发错误；resume hasMore 早退丢弃；judge 配置 SafetyConfig 类型化+respect --config；密码 el.type property 读取；selectorFor 唯一性标志；第二诊断路径缺 missing-tool 过滤；崩溃哨兵 launch 失败泄漏；enoent 过宽；bool limit；connection_lost trace 契约。implementation-status 第 19 行已据实改为"可选后端·实际惰性"。
预存问题处理（用户授权后逐条核实再动）：
- 已修（commit 4f2eb11 + 8b53692，均经调用链核实 + 补测试）：graph_runtime:489 `_gi` NameError（并行失败重试必崩，确认）+ :646 ParsedIntent raw_input/缺 intent_type 必 ValidationError（重规划从未生效，确认）；schema ImmunityConfig 默认移除 mcp://*（与 trust_engine 安全默认对齐）。
- 核实后**故意不动**（属故意设计/文档化推迟，盲改有害）：fs_router fail-open 是 **已文档化的已知 gap**（test_app_fs_endpoints.py 头注释明写"no sandbox·known gap·见 ADR-002 scope tier"，并刻意锁定当前行为让未来修复以测试更新形式显现）——改 fail-closed 需先改锁定测试+做设计决策，非盲改；tentacle 0.0.0.0 是接收手机 LAN 连接的**设计必需**（coordinator.py 文档说明"手机请求母体决策→下发执行"，127.0.0.1 手机连不上），真缺口是 WS 缺鉴权握手=功能级设计，非一行可改。
- **更正前一条**（另一会话曾记 bugfix-demo"无法复现/无对应代码"——已证伪）：bugfix-demo pyc 陷阱**确实可复现且已修**（commit 6f563c8，本会话 870b57a3）。真实代码在 demos/bugfix_demo.py:102/125 与 bugfix_demo_v2.py n2/n5 的 `[sys.executable, "test_add.py"]`（无 -B）；n4 同长等秒编辑 `a - b`→`a + b` 撞 CPython pyc mtime+size 秒级失效 → n5 跑旧字节码。确定性证明（强制等 mtime）：默认→got -1 失败、加 -B→OK。已给四处 exec_shell 加 -B + test 改 sys.executable，test_bugfix_demo 由 3 失败→13 全绿，真 demo 显示 ✓ succeeded。教训：对**另一会话的"不可复现"结论同样要先实证**，与 [[octopus-agent-audit-verification-lesson]] 同理。
- 本会话 870b57a3 又修两项（用户授权"接特定项、先暂停 octoups"后做，均补测试 + 多套件回归绿）：
  · #7 ci.yml eslint `|| true` 去除（commit 4dd0e6f）——实测前端 0 error/179 warning，去 `|| true` 当下仍绿但恢复"新 error 即红"的闸门；179 warning 不 gate（留待 --max-warnings 单独收）。nerves-cross-process job 早由 octoups 5ae081e 删。
  · #8 file_safety 写路径接线（commit b178079）——check_file_write 此前零生产消费（仅 re-export+单测），凭据文件名黑名单（.env/id_rsa/~/.ssh/* 等）实际不生效。已在 executor.py 调 handler 前对 write/edit/delete/dangerous 技能 vet（DENY→PermissionError），与 ADR-002 写作用域互补（作用域管"写哪"、本层管"绝不写这些名字"）；另加专门 `except PermissionError` 让作用域逃逸+文件安全拦截的原因进 output（原катch-all 只留类型名）。implementation-status 第 19 行已据实补消费方证据。
- 本会话 870b57a3 第三轮（用户"456+架构债都做"，先只读设计工作流出方案再逐项亲手实现+补测试+原子提交）：
  · #9 架构债 import 方向 linter（commit 25fb465）：tools/lint/import_direction_check.py（AST+baseline 棘轮，仿 god_file_check）。规则：sensing.gateway 只能被 sensing/platform import；core/safety/memory/protocol 不得 import sensing；execution/adapters/tentacle 不得 import sensing.gateway；platform.models 永远放行；TYPE_CHECKING 豁免、函数体 lazy import 仍算债。baseline 26 条，接 ci.yml+pre-commit+test_lint。顺手补 .pre-commit-config.yaml 那条 `except:` 未引号致 YAML 非法。
  · #6 审批旁路（commit de204ce）：根因是机制健全+默认安全，但生产 app.py:1241/1255 硬编码 True。改 SafetyConfig 加 allow_client_approval_bypass（默认 None=安全），create_app 读取后透传两处，gateway 暴露到 app.state 供测试。
  · #5 tentacle WS 握手（commit 6e22c1b）：保留 0.0.0.0；TentacleWebSocketServer 加 auth_token（回退 OCTOPUS_TENTACLE_TOKEN），device/hello 前 hmac 校验，未过/二进制帧/task-execute 一律 close 1008；非 loopback 且无 token 全拒并 start() 告警；loopback 无 token 仍放行（本地）。补 5 测试。
  · #4 fs/terminal（commit ae45ad8）：_assert_in_scope 空 roots 改 fail-closed 回退 _assert_within_allowed_roots；create_fs_router/mount_terminal_routes 加 identity_store/require_auth/jwt（默认 False 不改本地）；fs 用 router 级 Depends 一处覆盖全端点；terminal WS accept 前鉴权失败 close 4401、kill 也鉴权；Request 提到模块级 import（future annotations 下 FastAPI 才认）。补 TestFailClosed/TestFsAuth/test_terminal_ws_auth。注：mcp serve（MCP-over-SSE，ADR-007）另算。
- 本会话第四轮（用户逐项点"做"）：
  · Apache-2.0 NOTICE 合规（commit 14c7e9d）：补 NOTICE（诚实声明捆绑技能混有原创+外部、出处未记，不伪造归属）+ CONTRIBUTING 要求今后 SKILL.md 记 source/license/author + root_hygiene 允许清单加 NOTICE。没给 88 个技能逐条补归属（来源无法从压缩导入还原，硬编=新的不实声明）。
  · 间接提示注入防御（commit 38e5aeb，用户从三大件中选此项）：新增 runtime/safety/validation/prompt_injection.py（纯 stdlib）：is_untrusted_tool（web/browser 亲和或 mcp_* 前缀）+ scan_for_injection（override/role/exfil/control-token 标记+severity）+ wrap_untrusted_observation（围栏化"数据非指令"，命中升级告警）。react_loop 两路（_dispatch_parallel_actions 并行 + 单动作 ~L2950）在 observation 回灌 LLM 前对外部工具输出加固；UI preview 保留原文；失败工具不包。**定界+标注不改写**，风险信号非保证。补 12 单测 + 2 集成测（run_react_loop 真跑：web 工具→围栏+告警+原文保留；read_file→不包）。implementation-status 安全治理表加一行。
- 本会话第五轮 架构债真实解耦（用户"26条双向"，26→10，砍掉 16 条=62%，三个原子提交）：
  · a7dd804 删两个死模块（core/nerves/sensors.py 孤儿 re-export、reflex/workflow_delegate.py 导入不存在的 workflow_editor_router）→ -4 边。先验零消费者。
  · 6718a4c 把 LLM 数据类型(Message/ToolSpec/ToolCall/ModelRequest/ModelResponse/ModelStreamEvent/枚举/helper)+纯 ModelRouter ABC 从 sensing/model_router/models.py 下沉到**新建 runtime/platform/models/llm.py**（子模块，避开与 platform.models.ToolCall 的命名冲突——LLM ToolCall vs 执行 ToolCall 两概念同名）；models.py 改为 re-export shim（~70 个旧导入不动）+ 保留 MockModelRouter(带 instrumentation)；8 个 forbidden 导入(core/safety/memory)改指 platform.models.llm → -8 边。
  · 06a39f8 llm_planner/guard_judge/prompt_mutator/llm_judge 从 sensing.model_router 包改指 platform.models.llm（只拉类型，无具体 router）→ -4 边。
  · 验证：shim 身份一致(Message is platform.Message)、178+112+80 测过、ratchet 自检绿、anthropic_router 1 失败是预存 SDK-未装环境基线(HEAD 同样失败)。
- 架构债 import 方向边**已 100% 清零（26→0，baseline 空）**，分多轮原子提交：
  · 第一批（26→8）：删死模块×2；LLM 数据类型+ModelRouter ABC→platform.models.llm；4 个 planner/judge 改指；token_juicer→core/cerebrum、model_supports_thinking→platform。
  · 第二批 8→0（用户"8条全啃到底"）：step 格式化器→platform.step_format(533182b)；cron 持久化→execution.cron_store(d8ae480)；codex 插件发现→platform.plugins.codex_discovery(0bc2b48)；_resolve_actor HTTP 鉴权→adapters.web_auth(0ea7f6a，3 边)；browser_act 删死的 _active_streaming_journal 导入(2c9d3b7，该名早已不存在、import 恒抛被 fallback 接住)；build_anthropic_tool_specs+常量→execution.tool_spec_builder(e1c4656，最后一条)。
  · 方法论：每个搬迁都验证"被搬符号的真实依赖来自 core/execution/platform 而非 gateway"→搬到正确层→原 web 模块 re-export(身份一致)→repoint forbidden 导入→跑回归+ruff+ratchet。tool_spec_builder/_resolve_actor 看似纠缠，实测依赖全是下层，可净搬。
  · 现状：ratchet --strict 绿、baseline 0 条，任何新增 wrong-way import 直接 fail CI、无 grandfather 例外。
- 孤儿代码清理（commit 5165627）：评审估的"~9000 行"是**严重高估**（把 all_skills 技能脚本——subprocess/动态加载、非孤儿——和前端 ItemView 树都算进去了；同样适用"先证后断言"教训）。用 AST import 图（含相对导入解析 + 动态字符串）严扫，真正的 Python 孤儿候选仅 ~641 行；逐个核实剔除假阳性（chat_page 明显在用、skill_export 是带 __main__ 的独立工具、*_skills 被 all_skills/__init__ 导入）。删了 4 个零引用模块 ~302 行：cli_registry（被 argparse 取代的装饰器 CLI 注册表 + dict/list 注解 bug）、config_interface（SkillConfigChecker 从未接线）、openai_gateway/models（未用的 ChatCompletion* pydantic，网关用 dict envelope）、recovery/camouflage（safety.experiments 的死 re-export shim，0 def）。回归仅预存失败、import-direction ratchet 绿。
- **并发会话协调**：octoups 同时在建 orphan_module_check.py 棘轮（更强、识别 importlib/registry，仅 flag 了 android_router——我也保守保留了它，两边一致）。互补：我删存量、它防新增。我的删除经它的独立 linter 交叉验证（删后它仍绿、未 strand 新孤儿）。octoups 的 ci.yml/pre-commit/linter 改动在工作树未提交，我未碰。
- 前端 ItemView 孤儿树清理（commit 9babc1c，删 15 文件/931 行）：评审/教训#1 称其孤儿——**核实属实**。严格追渲染链：生产路由 app/workspace/chats/[thread_id] 渲染 MessageList→MessageOutputSummary，后者**只** import item-views/file-change-view 的 `singleHunkDiff`（diff 工具函数），从不碰 ItemView 派发器或任何视图组件。逐项证伪假可达：无人 import 派发器/barrel、无动态/懒加载引用、12 个视图组件名在 item-views/ 外零引用、file-change-view 不依赖任何兄弟视图。删 ItemView 派发器+12 视图+2 测试，**保留 file-change-view.tsx**（singleHunkDiff 生产在用）。typecheck 过、eslint 0 error、vitest 813 过（3 失败=prompt-input-files×1+workspace-sidebar×2 预存基线，与本树无关）。本会话孤儿清理合计 Python 4 模块~302 行 + 前端 15 文件 931 行 ≈ 1233 行（评审"9000"仍高估，但 ItemView 树是最大真孤儿块）。残留小尾：file-change-view 里 FileChangeView 组件/DecideHunkFn 类型现成未用导出（仅被已删 index 用过），可后续 trim。
- 提示注入硬门控（commit 40393c6）：#10 软门控升级为真拦截。不可信工具输出带注入标记→给本轮打 taint(contextvar，prompt_injection.py)；后续高危工具(risk high/critical)在单动作审批点强制人工审批，覆盖 auto_approve/scoped_artifact_write/accept_edits 旁路。react_loop loop 起点 reset、两处包装点 mark、审批点 gate；ApprovalRisk.with_injection_taint() 标注。补集成测(tainted web→exec_shell 被门控、clean→自动跑)+ taint 单测，209 过。**已知/待红队确认的弱点**：(a) 外泄类工具 send_/email_/http_ 是 medium 非 high，gate 只查 high/critical 可能漏掉(exfil 正是注入主载荷)；(b) 并行 _dispatch_parallel_actions 本身不审批(多动作里的 exec_shell)；(c) mcp_* 工具可能未评 high。已跑对抗式红队工作流 w9dzcgsmj 核实，待裁决后修真绕过。
- 提示注入门控红队加固（commit d9c015e）：23 代理对抗审查确认 18 条绕过，归两类。**已修（强化单动作门控）**：gate 阈值 high/critical→**medium+**（外泄类 send_/http_=medium 是注入主载荷，原来漏）；assess_approval_risk 补 shell 别名(background_exec/run_command/run_python 等→high)、egress 同义词、mcp 工具(mcp_*≥medium，名字含 exec/write/upload→high)；is_untrusted_tool 把 mcp/external 纳入不可信亲和(MCP 输出按亲和判而非可改的 mcp_ 名前缀，堵 page-agent 绕过)。补 egress-gated + 分类单测，146 过。**已知待大改（执行点而非规则）**：门控只在单动作 react_loop 路径；并行 _dispatch_parallel_actions、stream_agentic_fallback、子代理 runner 执行工具**完全不过审批/taint 门控**(预存架构缺口，被 taint 工作暴露)。正解=把 untrusted-scan+taint+审批下沉到 executor.execute_step 单一 chokepoint + taint 挂 turn/session 跨线程传递。这是独立的较大重构，单列。
- 提示注入门控 chokepoint 重构（commit 2cb1224）：把 taint 标记+enforcement 下沉到 **executor.execute_step**（所有执行路径的单一必经点）。executor 跑工具前调 injection_taint_block()——本轮被 taint+工具 medium+ 风险+无 approval-capable loop 审核过→拒(immune_reject，fail-closed)；跑成功后若 untrusted+flagged→在此 mark taint(覆盖 agentic-fallback/子代理这些不经 react_loop 包装点的路径)。桥接：prompt_injection 加 set_injection_gate_handled/injection_gate_already_handled，单动作 react_loop(能问人)在 execute 外 wrap gate_handled=True 避免双拦；taint 是 contextvar 线程池不传，故 _dispatch_parallel_actions 把 risky/untrusted 动作强制 serial(inline) 让 executor 读到 taint。验证：并行 web_peek+exec_shell 批→exec_shell 被拦 handler 不跑；executor 单测(untrusted 输出 taint→risky 拦/read 放/reviewed 放)。实测 agentic-fallback 经 execute_step(tool_bridge:225)→已覆盖；子代理跑嵌套 run_react_loop→**自身内** untrusted→risky 经 chokepoint 已护，但**父→子 taint 不继承**(子 loop 起点 reset + 跑在新线程上下文 + auto_approve=True)。201+193 测过、import-direction 绿。
- 子代理 taint 继承（commit 49c13c4，"继续补完吧"，11 文件 +475/-1，249 测过）：父轮被注入污染→委派高危动作给新 spawn 子代理时不再洗白。**消费(honor)**：stream_react_loop 起点 reset 后从 intent.user_context["_inherited_injection_taint"] 重标 taint。**生产(capture，各 spawn 在父上下文、跨池边界前 stamp)**：bridge.call_subagent→context（主委派路径，子代理跑在内层 ThreadPoolExecutor）/ parallel_runner.submit→task.context（/api/tasks）/ orchestrator.dispatch→run_context（多代理批）。**穿线(关键暗坑)**：make_stack_subagent_runner 只把窄白名单 key 从 ctx 拷进子 intent.user_context——不把 `_inherited_injection_taint` 加进白名单则 capture 被静默丢弃、honor 永不触发。**ephemeral 路径**：ephemeral 子代理经 `skill.handler(**input)` 直跑，**绕过 executor chokepoint+审批通道**（确认 critic/implementer 角色含 exec_shell/write_text_file）；新建 runtime/execution/suckers/ephemeral_injection_gate.py 在此 fail-closed 拦截（重标继承 taint→tainted 时 BLOCK medium+ 风险工具，无人可问→直接拒；untrusted 输出注入命中→升级 taint）；抽成独立模块让 ephemeral_runner 保持 <1000 行 god-file 阈值。补 11 测（消费×2/生产×4/穿线×2/ephemeral×3）。
- **补完后整系统对抗式红队验证（workflow w2vnsukip，8 角度 21 代理 1.4M token，13 条经独立证伪 agent 核实→10 确认/3 驳回）**："继续"触发。每条确认的绕过我都按[[octopus-agent-audit-verification-lesson]]亲自追链路再修，5 个原子提交（403 测过、import-direction 绿）：
  · Commit C `11d8cee` taint 源+marker 规避（#1/#3/#4）：UNTRUSTED_AFFINITIES 加 `network`（git_pull/fetch/clone 远程内容入扫描，FP 低）；exfil 正则去英文介词依赖（中文 "api_key 发送到 https://" 原漏）；新增 change/modify/alter override 同义词模式（含 benign 防误报测）。**#2 read_file/git_diff 本地读预植文件=已知启发式边界**（泛标会污染几乎每轮），代码+测试显式钉住。
  · Commit E `67f9733` 跨轮持久化洗白（#8/#9/#10）：tainted 轮调低危 remember/update_soul/note_user 写 MEMORY/SOUL/USER.md→次轮干净加载进 system prompt 中毒。injection_taint_block 新增 `is_durable_persistence_write` 集合，tainted 时**在 gate_handled 检查之前**拦持久化写（单动作门控不会升级低危写，必须直拦）；ephemeral 门控同步。**附带修测试隔离 bug**：taint contextvar 跨测试泄漏→conftest autouse `_reset_module_state` 加 taint reset。
  · Commit B `6138fc4` gate_handled 泄漏（#6, critical）：单动作 wrapper 设 gate_handled=True 告诉 chokepoint"已审"；inline 子代理（call_subagent 默认 timeout_seconds=None）同线程继承父 True，子代理自身并行路径风险工具被骗过 chokepoint。修：loop 起点 reset taint 旁补 `set_injection_gate_handled(False)`。测试撤销修复即失败（证伪有效）。
  · Commit A `88ac442` meta-skill 直调绕 chokepoint（#7, critical）：use_capability（capability_skills.py:493）/forged 复合（forged_persistence.py:182，140 是 return 后死代码）直接 `skill.handler()` 不过 execute_step。injection_taint_block 加 `defer_if_handled` 参数（meta 内层传 False——单动作只审了外层低危 meta、没审内层 exec_shell）。**更广缺口（同样绕过 immunity/file_safety，非仅注入）已 spawn_task task_18c6b17f 登记**：正解是内层分发走 execute_step 而非逐处补门控。
  · Commit D `2bc3b7b` 并行批次顺序（#5）：串行批次按声明顺序跑，[exec_shell, web_fetch] 时 shell 先跑、taint 未标。改成 untrusted 工具先跑（稳定排序），结果仍按原 idx 存。测试撤销重排即失败。
- **剩余项收尾（"剩下的我决定了也要做"，3 提交，共享工作树串台下精确隔离提交）**：
  · `6635a90` 前端 trim：file-change-view.tsx 删孤儿 `FileChangeView` 组件树+`DecideHunkFn`（外部零引用，先证），只留生产在用的 `singleHunkDiff`（250→25 行）。typecheck/eslint/100 vitest 绿。
  · `6542e7a` 失控子代理树护栏：根因=每子代理拿独立 per-task Budget、**无任何深度/数量/递归护栏**，且会话级 TokenBudgetTracker 生产**根本没接**(cli_run/cli_core/builder 都不传 budget_tracker，ceiling=500k 只是 docstring 示例)。做**安全无策略的一半**=全局并发槽：call_subagent 整个子运行期占一槽，parent→child→grandchild 链每层占槽→单一上限同时界深度+宽度，超限 fail-closed 拒。默认 64，env `OCTOPUS_MAX_ACTIVE_SUBAGENTS` 覆盖。**剩余=产品决策**：累计 COST ceiling（子代理开销并入共享池硬停，上限值+硬停/告警须定）。
  · `339937a` read_file 本地读 taint（#2，保守收窄）：is_untrusted_tool 加可选 `args`；READ 工具 path 落在 world-writable/temp/downloads（/tmp、/var/folders[macOS]、~/Downloads 等）→ untrusted（堵下载后读 pivot），repo/cwd 读仍信任（边界保留）；写入 /tmp 不算。executor 扫描点穿 args。**注**：因共享工作树混入 octoups 的 meta-skill WIP，executor.py 用 `git apply --cached` 单 hunk 暂存只提交我那一行，未碰对方 use_trust_engine/skill_gate 改动。
- **#1 meta-skill 统一门控（task_18c6b17f）已由 octoups 在同一共享工作树实现（未提交 WIP）**：executor.py 现 `with use_trust_engine(self.immunity)` + 新建 skill_gate.py 的 gate_inner_dispatch/canonical_tool_path/file_safety_target/antigen_for，把 capability/forged 内层分发接回统一免疫门控。是对方的提交，我不碰。
- android_router：**用户决定暂保持现状**（不删不挂载，仅留基线孤儿）——它是已实现但未挂载的安卓远控服务端（203 行 6 端点+完整 docs/mobile+chromium 构建），删会毁规划功能，挂载是须慎重的对外远控激活。
- 仍待：累计 COST ceiling（产品决策）；per-step Budget 子代理树记账的 cost-rollup 部分。（OTel/Dockerfile 已由 octoups 修）
- **并发会话 god-file 棘轮失准（仅观察，非我引入）**：octoups 在搬模块（路径 siphon→gateway、regeneration→recovery、beak→… 已变），god_files_baseline.txt 未重生成 → `--strict` 现红(11 SHRUNK + 19 NEW，全是路径漂移/我无关)。我的 ephemeral_runner 抽模块后已退回阈值下、不在 NEW 里。待 octoups 落定后由其 `--write-baseline` 重锁。我提交只含我 11 文件，未碰 octoups 工作树里的 team_rooms_router.py 等。
- **协调教训**：本目录同时跑两个会话（本 870b57a3 与 octoups local_e0684814）抢同一分支，`git add` 被对方索引操作清空过一次。安全做法：用 `git commit -F msg -- <显式路径>` 一次性 add+commit 指定文件，不用 `git add -A`；动手前先 `git log/status` 确认工作树静止（对方已暂停）。

- **团队治理(微信/QQ/钉钉式,本会话 4 提交,均经 verify-first + 测试)**：在 team_rooms_router.py(WS 房间)上建发言治理。`48631bf` 先修越权(update_participant/remove_participant 只查 _require_member→任意成员能改他人角色/踢人/自我提权;收紧为成员只能改自己 display_name+退自己,角色/状态/他人=owner only)。`bae2ca9` per-member `muted`(owner 设、自我解禁也拦、重连保留)+ room `speaker_policy`(free/admin_only)+ 新端点 PATCH /speaker-policy;强制在 WS message 单一 chokepoint `_participant_can_speak`。`67981d9` 重生成 openapi 快照(仅该端点;**前端 types 故意不重生成**见 [[octopus-agent-generated-artifact-drift]])。`6c62e80` 回合引擎:round_robin/roll_call/moderated 共享 floor 状态机(current_speaker_id/moderator_id/floor_requests),WS 动作 floor:request/yield/grant,断线时 floor 接管保活;13 测(纯函数+单 socket WS)。**坑**:WS message 热路径加第二次 `with lock:` 会让既有 2-socket flaky 测试间歇死锁——复用循环顶部那次锁的快照即可。**剩余**:C 层数字分身 twin_agent_id(须被绑定者本人 opt-in,管理员不能强加)+ 托管发言;前端管理员控制面(需先解 openapi-types 基线脱债);team_rooms_router.py 已 1271 行越 god-file 阈值,拆分已 spawn task_57e466f4(抽 team_speaker_policy.py)。

- **团队治理 · 数字分身真正发言(commit `03a9ebf`,本会话,5 新测 + 既有 50 治理全绿;import-direction/god-file 绿)**:补上"AI 真正驱动 twin 发言"——此前只有委托数据模型(`de8bcbd` twin/hosted 本人 opt-in)+ 强制署名,无任何 AI 生成。**接缝(gateway 仍是 import 叶子,靠注入不反向依赖 model/execution)**:create_team_rooms_router 加可选 `twin_responder: (team,participant,transcript)->Awaitable[str|None]`,透传进 TeamRoomWsContext。**触发(team_rooms_ws 两个 floor 落点)**:round_robin 推进后 + roll_call/moderated 的 floor:grant 落点,若 holder speak_mode==twin 且 twin_agent_id 有值,锁外 await responder→复用 on_behalf_of 广播 shape(participant_id=被代言人 / spoken_by=twin_agent_id / via="twin")。round_robin 走 `_drive_round_robin_twins` 连续 twin 链(`spoken` set 防 all-twin 死循环,每座位至多一次,遇真人停);grant 路径只说一句不推进(moderator 控场)。None/空/异常→静默跳过、floor 不动(等同沉默真人)。每房 ~20 条 ring buffer 给 transcript,仅 twin_responder!=None 时填(无 twin 部署消息路径零改动)。**关键设计**:twin 触发是服务端自发=权威,不走 on_behalf_of 的客户端鉴权。**Serve 侧**:新建 runtime/platform/ui/team_twin_speaker.py(不塞 app.py 避免 god-file 增长 + 可独立测)`make_twin_responder(stack)`→按 twin_agent_id 在 team.members 找成员→persona(description)+transcript prompt→`stack.planner.router` 经 asyncio.to_thread 同步 call(off-loop);无 router→None 优雅降级。app.py 仅加 import + `twin_responder=make_twin_responder(stack)`。**剩余**:前端房间内 twin 发言指示/控制面(仍卡 openapi-types 基线脱债,见 [[octopus-agent-generated-artifact-drift]]);可选把 floor:yield 落点也接 twin(本次按计划只接 message-advance + grant 两点)。坑同前:WS 热路径绝不持锁 await——responder 在锁外、ring 读写各一次短锁。已知 flaky test_team_room_websocket_presence_and_events 仍预存(隔离 3× 绿)。

- **分享 · 自包含可回放 HTML 导出(本会话 3 提交,31 单测,typecheck/eslint 绿)**:用户要的"可回放链接"没走托管+鉴权重路,改**自包含 HTML**=隐私更安全的"可回放链接"(产物是用户先下载先自查再发的文件,脱敏发生在导出那一刻)。**关键认知(verify-first 推翻初始假设)**:in-app 工作台回放(agent-workbench-panel 的 ReplayController/ScreenFrameRow)是**逐 WorkBlock 步骤时间线**(terminal/file/browser… 各 kind,展开看 `$ 命令`/cwd/输出),**不是截图幻灯**——所以导出器复刻步骤卡,截图降级为某些块的可选附图(WorkBlock 无 image 字段,截图在 event.output/artifact)。**解耦三层**:① `core/sharing/replay-html.ts` 纯导出器 `buildReplayHtml(ReplayData)→html`,内嵌步骤 JSON + 原生 JS 播放器(步骤列表点选/播放/拖动/上下帧),XSS 安全(动态全走 textContent/img.src 不走 innerHTML + embedJson 把 `</`→`<\/`、`<!--`→`<\!--` 防脚本逃逸,仅页面 chrome 走 escapeXml);② `components/workspace/replay-from-blocks.ts` 适配器 `buildReplayFromBlocks(WorkBlock[],meta)→ReplayData`,**导出期脱敏**(redactSecrets 打码 sk-/ghp-/JWT/Bearer/`token=…`/≥40 hex + 1200 字截断)+ 截图仅内联已是 `data:image` 的(绝不抓网);③ `core/sharing/download.ts` shareSlug(纯,CJK 保留)+ downloadTextFile(DOM 胶水)。**接线**:ReplayController(看回放/跳当前/做同款 旁)加"导出"钮→把当前 `screenBlocks` 吐成单文件 .html;面板加可选 `replayTitle?` prop(默认"Octopus 运行回放",留缝待接 thread title)。ShareMenu 那条 `replayLinkSoon`"即将上线"→正名 `replayHint` 指向工作台导出(4 语言 + types)。**导入方向**:`import_direction_check.py` 只管 runtime/ Python(`_RUNTIME.rglob("*.py")`)不碰前端 TS,故 components→core/sharing 合法。提交 `c6ec04d`(导出器 proof,初版图片帧)→`fd8256e`(泛化步骤卡+适配器+下载+工作台接线)→`c7f74fa`(ShareMenu 指针)。**已实证**:真路径(buildReplayFromBlocks→buildReplayHtml)合成 6 步真实编程会话→7.5KB self-contained(external src=0)+ 密钥脱敏(`token=«redacted»` 无 `sk-` 原文)+ 截图内联,/tmp/octopus-replay-sample.html 浏览器打开验过。**未实证(诚实)**:UI 里"导出"钮的真实点击(chat workspace auth-gated + preview 易死,同上会话)——但 onClick 调的就是已测纯函数 + downloadTextFile,typecheck 过。**后续已做(同会话 3 提交,均测+typecheck+eslint 绿)**:`b9f85bf` thread title 经 `replayTitle` 接进面板(chats/[thread_id]/page.tsx,`thread?.values?.title||initialPrompt||undefined`,与 ShareMenu 同源;`:(literal)` 避 git pathspec 的 `[...]` glob);`7d95ea3` 播放器键盘(←→/空格/Home·End)+循环(⟲ toggle,aria-pressed CSS 高亮)+变速(0.5/1/2×,运行中改速实时 restart timer);`42eb3b9` 适配器从 `{kind:"image",media_type,data_base64}` 重建 data-url(读图片文件路径,builtins._read_file_image,≤1.5MB 上限)。**verify-first 校准死路(重要)**:原计划的"截图 fetch→data-url 内联"是**不可能的**——`computer_api_skills._compact_screenshot`(:45)在截图进事件流前**主动剥掉 `data_url`**只留 `data_url_omitted/data_url_bytes` 标记,事件里根本无截图图像数据/URL 可 fetch;工作台 ScreenFrameRow 也只渲染文本(命令/cwd/输出)无 `<img>`;`previewUrl` 是 live iframe 非冻结截图。所以唯一真实的"事件内图像"是 file-read 的 base64 记录(已接,见 42eb3b9),不存在可 fetch 的远程截图。教训同 [[octopus-agent-audit-verification-lesson]]:动手前先追后端到底发了什么,别按"应该有截图"的假设建 fetch 管线。**坑**:Write 工具曾给 `replace(/ /g,"")` 注入 NUL 字节(od -c 见 `/ \0 /g`)——整文件重写清掉;`grep -c $'\0'` 在 zsh 等于数总行(假信号),用 perl `/\x00/` 真查。**UI 收口(commit `788fbd7`,用户决策"整条回放栏删除→右上统一分享按钮+二级菜单",净 -192 行)**:删掉工作台底部 ReplayController 全部——看回放(面板内逐帧播放)/跳当前/做同款(composeReplayPrompt)/导出按钮 + replayPlaying·replayCompleted 状态 + auto-advance interval + replayTitle prop。导出移进 chat header 的 ShareMenu 下拉(新 `onExportReplay?` 可选 prop;聊天页 `toWorkBlocks(agentDisplayEvents)→screenBlocksForAgent(_,null)→buildReplayFromBlocks` 自行组装,`replayBlocks.length>0` 才显该项)。i18n `replayHint`→`exportReplay`(真动作,4 语言+types)。**replay-html/adapter/download 三模块完全不变、仍全测,只是 UI 入口从工作台搬到分享菜单**(三者现由 page.tsx 消费,无孤儿)。补 share-menu.smoke.test(`userEvent.setup()` 开 Radix dropdown 断言导出项 显/隐+点击触发 onExportReplay;参 model-picker.test,setup.ts 有 matchMedia/ResizeObserver shim)——这是 auth-gated 没法 live 点的正解。**做同款随栏一起删**;注意 share-card.ts 里的"做同款"是**图片分享卡的 prompt 块、另一回事**,保留。删 ReplayController 后同步删了 panel test 里 `"renders replay controls..."` 那条(断言已删按钮)。

- **audit.ultracode "总线"接线(2026-06-26,2 提交,backend+frontend 各一,均 verify-first)**:用户"开始总线那个工作"。先实证推翻子代理调研报告的"建新 streaming drive path"架构——`run_orchestration`(delegation_skills.py:3088)是**模型自调技能**(handler 注册、别处零直调),不该绕 ReAct 直驱。**核心认知**:① `workflow_preset` 从前端送达但**只存 session.metadata、全仓零消费**(react_execution.py:148);前端那套 `mode_contract/mode_preset/skill_pack_profile/promptContract` 同样**读取处为零**(纯装饰元数据)——真正被后端消费的只有 `agent_mode`(→react_context.py `_build_code_agent_mode_prompt`)、`capability_mode`、`verification_policy`。② 预算已**正确地由运营者闸控**:超 48 spawn 的唯一路径是 `session.metadata["orchestration_token_budget"]` 或 `OCTOPUS_ORCH_TOKEN_BUDGET` env(_resolve_max_spawns:1675;模型自传的 max_spawns 硬封 48),且 _run_orchestration 缺 session.metadata 时已回退 operator env。**故总线绝不该从 preset 设预算**(client 控的 workflow_preset 设预算=越权抬 ceiling 到 256 的洞;且对模型不可设,human-only)。**所以总线=纯行为转向**,非预算非新驱动路径:`317702d1` 后端在 react_loop.py:850 注入点加 `_build_workflow_preset_prompt`(react_context.py),workflow_preset=="audit.ultracode" 时下发"用 run_orchestration 做多代理深审(多角色/verify/synthesize),无该技能则手工多轮"的防御式 operating contract;深度仍由 operator 预算自然伸缩。`f3b41507` 前端 audit 模式加"标准/最高"强度开关(mode-selector 下拉内,仅 audit 显示)→`workflowPresetForMode(mode,intensity)` 纯函数(presets.ts,单测)把 max→audit.ultracode,develop/uxui 不漏;复用既有 `ultra*` locale 串、仅补 1 个 `standard` 标签×4 语言。验证:后端 145 test_react_loop 全绿+ruff;前端 typecheck/eslint/18 vitest(presets+locale 完整性)。**未实证**:live 点开 audit→选最高的端到端(:3000 是用户自己的 dev server 占着、preview 工具无法 attach;且到达该 UI 态需绑项目+解锁 code mode 多步)——逻辑已单测、HMR 已热更。两提交本地 main 未 push(其下 90646030 是 Codex 的 loop-hardening,也未 push)。

- **个人空间工作模式(2026-06-26 续,B 方案)**:用户指出项目空间(绑目录=可读写改文件)有模式、**个人空间(agent 内部对话/工作空间,无用户项目目录)没有**。核实链路:`isProjectCodeMode=!!workDir`→`effectiveMode="code"`,所有项目/code 模式转向都在 react_loop.py `if _wp:` 块内(无 workspace 一行不跑);`_build_code_agent_mode_prompt` 只认 builder/coder/architect。给个人空间补一套模式 **通用/构建/研究**:① 后端 `8e038389` 加 `_build_personal_agent_mode_prompt`(personal_mode=="build"→"在自己沙箱造可运行成果"contract,注入点在 react_loop 第~1033 行通用区、`if not _is_code_mode` 时);② `aa5e8a25` 研究半边——personal_mode=="research" 接进**既有** `_is_research_mode`(复用深度研究,不另造引擎、不切 reasoning mode 故无导航副作用)+ 新建 `personal-mode-selector.tsx`(通用/构建/研究下拉 chip,**标签本地自包含不进 i18n bundle**——刻意躲开并发编辑的 locale 文件)。研究>通用>构建三态。**未提交的 UI 接线**(chat-input-box.tsx + page.tsx:personalMode state + `showPersonalMode={isAgentRoute}` + 送 `personal_mode`)——**卡在并发隔离**:Codex 同时在 chat-input-box.tsx 建"Codex composer mode"(codexComposerMode/CodexComposerMode,且依赖其未提交的 presets.ts），与我的 personal 行**同 hunk 交错**,且该文件被实时编辑(strip-restore 会撞 Codex 新改)→ 故只提交可净隔离的(后端+组件+测试,显式路径),接线留工作树待 Codex 落地后再净提交。**功能已在用户 :3000 HMR 实时可用**(接线已热更,只是没提交)。教训重申:i18n/presets/chat-input-box 是并发热点文件,见 [[octopus-agent-generated-artifact-drift]];我撤回了自己加到 5 个 locale 的 personal* key 改走组件内本地 map,避免与 Codex codex* 纠缠。

- **#1 统一协作会话(CollaborationSession)收敛全链做完(2026-06-30,已 push origin/main)**:Codex 三个 source of truth(cowork thread by thread_id / team room by room_id / **team tasks by room_id**)全部折进一个**读模型**。链路:`63e8fab8` 读模型+事件溯源 room_link → `fe8baaa4` room transcript → `ce8775b7` room participants → `d153ed0a` 前端 `/api/collab` 客户端 → `f8e755e6` 会话级搜索 → `2513a932` 会话视图组件 → `ebdb7d68` **写侧** `POST /api/collab/{thread}/room-message`(经会话写进链接 room transcript,与读侧 `_search_room_messages` 对称;未链接 409) → `7600c75a` 会话面板进 workbench(`onlyWhenRoomLinked`,presence 仍归 collab bar 不重复) → `13912fb7` **第三源**:`room_tasks` 进会话(resolve_session 加 `room_tasks_provider`,router `_room_tasks` 读 team_tasks.json 按 room_id 过滤、接 GET /api/collab + /search;search 加 `room_task` kind)→ `51b095ba`/`ae7e78af` 前端 room_tasks/room_task + 写客户端 `postCollabRoomMessage`。**关键架构决断**:team 任务有完整 runner/鉴权/广播 router,**写侧故意维持两条 canonical 路径(cowork `/api/cowork/.../tasks` + team `/api/team-tasks`),收敛只做"一个读模型覆盖两套任务存储",不强并写路径**(强并=纯别名+高风险无收益)。验证:后端 cowork 全套 40 passed、前端 cowork vitest 21 passed。
- **本仓有前端自动镜像后端 cowork 模型变更的机制(重要,易被误判)**:我改后端 `CollaborationSession`/`search` 模型后,前端 `core/cowork/types.ts`+`i18n/locales/*`(kindRoomTask 等)+组件(collab-bar/session-view)+测试(api.test/roster.test/collab-bar.test)会**被某进程自动补上对应改动**——会在我两次 `git status` 之间冒出来。**别困惑于"我没写的前端改动"**:先 diff 确认它们纯 room_*/cowork 域 + tsc/vitest 绿,再按 [[octopus-agent-roster-unification]] 的 explicit-path 提交;**全量 tsc 红常是并发 oct/计费会话的在途 i18n drift(emailAutoRegister/emailCardDescription 加进类型+zh-CN 但 en/ja/ko 未补齐),HEAD 已提交态是绿的**,别误当自己引入。绝不 `git add` 整个 i18n 文件前先核 diff 无并发 key 混入。

- **统一 work-mode 解析(2026-06-30,2 提交已 push)**:"单人/多人、个人/项目 是不是一个模型"的延续。两根轴:**协调轴**(几个 agent)早已是一个模型(`turn_plan.plan_turn` 的 `is_multi=len(responders)>1`,单人=n=1 退化态);**work-type 轴**之前是 react_loop 里 ~120 行散落的 12 个重叠 flag(mode/capability/agent_mode/personal_mode/codex_mode/completion_policy/goal_mode/workspace_scope/workspace_path/personal_workspace_*)。Codex 已先合了 project↔personal 的 workspace 能力(`181e3191c`:加 `_effective_wp`=绑定项目目录 or personal per-thread cwd,个人线程也能翻进 `_is_code_mode` 跑完整 code 机器;`workspace_scope` project/personal 只是标签非能力门)。我把散落解析抽成纯函数 `runtime/core/cerebrum/work_mode.py:resolve_work_mode()→WorkMode`(`56be63d5`,14 parity 测),react_loop 用 `_wm=resolve_work_mode(_uc)`+薄别名替换、下游 ~20 用点不动(`f6218c04`,+19/-68)。干净 worktree 验证 **test_react_loop 150/150 全绿=行为中性**。
- **致命 git 坑(本会话踩到,务必记)**:`git commit -- <path>` 提交的是**工作树版本**(会卷入并发 Codex 的同文件 WIP),**不是**你 `git apply --cached` 进 index 的纯净版!我第一次提交 react_loop 接线就这样把 Codex 的 guard 重构(`_final_answer_needs_pre_emit_guard` 等 +200 行、依赖未提交的 react_security_guards 符号)误并进我的 commit,还破了 test_zero_anchor(被 Codex 重构改了 salvage 路径,不是我的 work_mode)。**正确隔离法**:(a) `git apply --cached <我的patch>` 把我的 hunk 叠到 index,然后 **`git commit`(无 pathspec,提交 index)**;或 (b) 已污染要修时:`git hash-object -w <纯净文件>`→`git reset HEAD`→`git update-index --cacheinfo 100644,<blob>,<path>`→`git commit --amend`,**全程不碰工作树**保住对方 WIP。验证用 **`git worktree add --detach /tmp/wt HEAD` + 绝对 venv python + `PYTHONPATH=/tmp/wt`** 在隔离的已提交态跑测试,不受并发工作树污染。教训:并发热文件提交前必查 `git diff <我的上一提交> HEAD -- <file>` 确认只含自己的 hunk;且 react_loop.py/react_guards.py/react_security_guards.py 是 Codex 的活跃热文件群。

相关：[[octopus-agent-dev-environment]]、[[octopus-agent-audit-verification-lesson]]、[[octopus-agent-generated-artifact-drift]]、[[octopus-agent-verification-guards]]、[[octopus-agent-multiagent-gap]]、[[octopus-workspace-roster-unification]]
