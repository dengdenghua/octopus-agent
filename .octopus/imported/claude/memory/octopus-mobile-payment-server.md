---
name: octopus-mobile-payment-server
description: octopus-mobile 账号/计费/会员服务端部署状态 + Stripe 付费接入进度 + 服务器↔仓库分叉
metadata: 
  node_type: memory
  type: project
  originSessionId: 1d6d27c3-dc6a-43a7-97bf-65acc5304019
---

**⚠️ 重大状态变更(2026-07-05):盒子已整份前向同步到仓库 HEAD,「服务器↔仓库分叉」这个长期事实基本消解。** 之前一直是「盒子非 git、阿泽手改、比仓库少 ~50 行、我每次外科式合并」。这次要把并行 session 攒的市场功能(创作者分成 + 小红书式图文帖社交 + 崩溃上报端点,git 里领先盒子 ~1200 行/3 特性)部署上线,深查发现:**盒子其实是仓库的严格旧子集,零阿泽独有逻辑**——用函数级规整比对(scratchpad 脚本)证实 19 个相异函数里盒子「独有行」全是仓库改过的旧版本(旧支付/旧签名/旧列注释/旧密钥变量),0 条真独有;30 个仓库独有函数=纯新增市场功能。故「只上市场保留盒子支付」反而要回退 16 个函数(最险),用户拍板**整份前向更新**(box→repo app.py 4514 行)。**部署已成功**:装 python-multipart(盒子 venv 缺,requirements 已补 c1e2a04)+建 uploads/+备份 app.py&octo.db → scp → 盒子 import OK(92 路由)→ restart → 全绿(healthz 200/creator·square·crash 端点从 404 变活/支付·OTA·小程序市场没坏)→ 生产 DB 7 张新表(square_*/crash_reports/idempotency_keys)+2 列(author_earnings/download_count)迁移到位、日志零报错。**连带生效的仓库演进**:① 支付发放行为变了——订单发全额永久 credits(绕过 FREE_CAP)+ 会员顺延加 MEMBER_MAX_DAYS(默认365)封顶防无限续费;② `DEVICE_TOKEN_SECRET` 缺 env 时回退 JWT_SECRET,盒子只设 JWT_SECRET 故设备哈希与旧版一致、**配对不失效**。备份 `app.py.pre_market_fullsync.*` + `octo.db.pre_market_fullsync.*` 可回滚。**教训:盒子分叉程度以实测函数级比对为准,别信「阿泽手改所以有独有内容」的旧记忆——多轮外科合并早把阿泽的改动上游进 git 了。**

**octopus-mobile 付费会员体系**(2026-06-26 接 Stripe)。App `account/` → 服务端 `server/app.py`(FastAPI+SQLite,~2300-2400 行)→ 上游 LLM 中转。

**主力模型(2026-06-29 改 → 锁定 qwen3.5-flash)**:极速/默认档从 **Agnes 换成通义千问**(阿里云百炼,**专属**端点 `https://ws-jwyh0cui1dlyd8qx.cn-beijing.maas.aliyuncs.com/compatible-mode/v1`,OpenAI 兼容)——**因 Agnes 不支持并发,撑不住主力**。Agnes 退出文本主力,留作后续生图/生视频增值插件。

- **最终锁定模型 = `qwen3.5-flash`**(Qwen3.5 代,**原生多模态/带视觉**;上游 cn-beijing 基础档≤128K:**¥0.2入/¥2.0出每1M**,分档 128K-256K→¥0.8/¥8、256K-1M→¥1.2/¥12;HIGH 置信,直解官方页 raw HTML。专属 ws- 端点=普通按量同价,非预留算力;现可能仍吃免费额度100万tok/模型/90天)。注意区分:用户口语「千问3.5 flash」= `qwen3.5-flash`(快照 `qwen3.5-flash-2026-02-23`);而裸 `qwen-flash` 是 **Qwen3 代、纯文本**(`qwen-flash-2025-07-28`)——两代不同,别混。该专属端点实测可调:`qwen-flash`/`qwen3.5-flash`/`qwen3.5-flash-2026-02-23`/`qwen3.6-flash`/`qwen-plus` 全 ✅。
- **单档 + mimo 彻底移除(2026-06-29 用户拍板「就用一档模型 把mimo 移除」)**:`_DEFAULT_MODELS` 现只剩 `qwen3.5-flash` 一个;`PROVIDERS` 现只有 `qwen`+`agnes`(mimo provider、`MIMO_*` env 读取、两处 `m.get("provider","mimo")`默认、`_resolve_model` 兜底 spec 全部改/删,代码里 mimo 只剩两行"已移除"注释)。`.env` 的 `MIMO_API_KEY`/`MIMO_BASE_URL` 留作休眠(没删,无引用)。
- **运营硬锁 `FORCE_MODEL`(app.py `_resolve_model` 开头)**:`.env` `FORCE_MODEL=qwen3.5-flash` 非空时,**忽略客户端请求的任何 model,一律走它**。单档后它是双保险(目录就一个 + 锁再兜底)。已验证:`mimo-v2.5-pro`/`mimo-v2-flash`/`agnes-2.0-flash`/乱编 id 全部 → `qwen3.5-flash`/provider=qwen/0.2×。`MODEL_SPEC=['qwen3.5-flash']`、`PROVIDERS=['qwen','agnes']`。
- `.env` 已设:`QWEN_BASE_URL`/`QWEN_API_KEY`/`DEFAULT_MODEL=qwen3.5-flash`/`FORCE_MODEL=qwen3.5-flash`。**App 不用重新打包**(发旧 id 也被锁到 qwen3.5-flash)。备份链:`.pre_qwen.20260629_055534`→`.pre_lock.20260629_061922`(加锁+升3.5)→`.pre_nomimo.20260629_062623`(删mimo)→`.pre_clean.20260629_062727`(清死代码)→`.pre_mult.20260629_064343`(倍率0.2→0.5)。每步都过:本地+服务器双编译、完整性校验防覆盖阿泽手改、health 重试、失败自动回滚、import 验路由。
- **毛利/倍率校准(2026-06-29,多代理核价后用户拍板 0.2→0.5)**:计费 `扣积分=ceil((tin+tout)/1000 × CREDITS_PER_1K_TOKENS(=0.13) × multiplier)`,**最低1积分、向上取整**。qwen3.5-flash 输出¥2/M=输入10×,成本大头在输出端。**关键闸:`MAX_OUTPUT_TOKENS=8192`(早就存在)把单次输出焊死在8k**,所以「长输出击穿成本」场景到不了——之前担心的−53%(用30k输出测)其实不可能。**输出封顶8k下真实最差毛利:0.2时已 +44%(从没亏过),0.5时 +55%**(旗舰包¥0.04/积分);典型小请求86-92%。0.5 仍是有意义加固(把地板44→55)。**缓存命中¥0.02只省输入≈没用、Batch5折是异步实时用不了**——输出无实时折扣。积分售价(月卡反推):旗舰¥499→¥0.040/积分、入门¥99→¥0.066、赠送没用完~¥0.099。⚠️专属端点现可能仍吃免费额度→当前成本≈0、毛利≈100%,额度完才回到上述曲线。
- **仓库同步完成(2026-06-29)**:这几轮只改在线上服务器(非git)的改动已搬回仓库 `octopus-mobile`,分支 **`chore/server-qwen-sync`**(commit `bd40e90` server/app.py+.env.example 追平 qwen单档+FORCE_MODEL;`304d16b` App隐藏档位UI)。**未push**(共享树满是 Codex WIP,只 add 这4个我独有的干净文件、绝不 add -A)。仓库独有代码(`_is_allowed_origin` 等+37行)未动。
- **App 档位 UI 已简化(304d16b,未编译验证—本机无JDK)**:`activity_account.xml` 整段「对话档位」(标题+三按钮卡片)`visibility=gone` 隐藏(视图保留,Kotlin findViewById 不崩);`AccountConfig.platformModel` 不再按档位映射、直接返回 `qwen3.5-flash`。**需重新打包 APK 才生效**;走 CI 或本地有JDK处编译验证。

上游中转(历史):MiMo 付费 + Agnes 免费。已建:会员档位/有效期、积分(永久+月赠+每日免费、原子预扣对账)、邀请返利、邮箱验证码登录、JWT、admin 后台、订单 + `_settle`。

**线上服务器(阿泽部署)**:Lightsail `32.185.238.217`(ssh alias `mi`,key `~/.ssh/LightsailDefaultKey-us-west-2.pem`,也在微信发的 mi.zip 里)。`/home/ubuntu/octopus-server/`,**systemd `octopus-account.service`** 跑 `.venv/bin/uvicorn app:app --port 8081`,nginx 反代 `api.octoapk.com`(DNS 已指向该 IP,HTTPS 在)。`{"status":"ok"}` 健康。**1GB 内存(紧,还跑着 :3000 Next.js 下载站 octoapk.com)**。免密 sudo。.env 已配 JWT_SECRET/MIMO_API_KEY/MIMO_BASE_URL/AGNES_API_KEY/ADMIN_TOKEN/EMAIL(SMTP)。**用户现在就能注册/登录/聊天**。

**⚠️ 服务器 app.py 非 git 管理,阿泽直接手改 + 时间戳备份**(`app.py.pre_pay.20260623`/`pre_sub`/`pre_goods`/`pre_merge`…—— 他在自己迭代支付/订阅/商品)。**与仓库分叉**:服务器版(~2298 行)比仓库版(~2340)少 ~52 行(仓库多 `_is_allowed_origin` 等)。**单一事实源未定**——以后要么服务器改合回仓库、要么从仓库部署,否则越改越乱。动服务器前先跟阿泽对齐。

**⚠️盒子新增第二服务:资产 Registry(2026-06-30,我部署,与收款隔离)**。这台 Lightsail 现在**同时**跑收款(8081)+ **资产分发 registry(8090)**。registry = `octopus-enterprise` 的公开只读资产分发层(角色/技能/插件/经验,详见 [[agent-asset-consolidation]]),做成**极轻独立服务**(无 DB):`/home/ubuntu/octopus-registry/`(`registry_app.py` + 最小 `app/` 包[空 `routers/__init__.py` 绕开企业版重路由] + `app/agent_assets/`{catalog/twins/twins_data.json/sources} + 自建 `.venv` 只装 fastapi+uvicorn),**systemd `octopus-registry.service`** 跑 `.venv/bin/uvicorn registry_app:app --host 127.0.0.1 --port 8090`(MemoryMax=300M)。**nginx 改动(api.octoapk.com)**:在收款的 `location /`(→8081)**之前**插了一条更具体的 `location /api/v1/registry/ { proxy_pass http://127.0.0.1:8090; }` → 只把 registry 流量分流到新服务,**收款 8081 一行没碰**;备份 `/etc/nginx/sites-available/api.octoapk.com.bak.registry.<ts>`、过 `nginx -t` 才 reload、失败自动回滚。公网 `https://api.octoapk.com/api/v1/registry/assets`(role204/skill97/plugin46/twin3/exp1),无鉴权公开只读。**实测**:外网 HTTPS 雇 util_pitch_agent=13资产 checksum 全过、收款 GET / 仍 404(8081 应答)零影响。**更新 registry 内容(2026-06-30 已升级为蓝绿)**:盒子 `…/app/agent_assets/sources` 现在是**符号链接 → `releases/<ts>/`**(不再是实目录)。统一用 dev 的一键脚本发布:`octopus-enterprise/scripts/publish-registry.sh [--rebuild] [--apply|--rollback] [--allow-shrink]`(缺省 DRY-RUN)。`--apply` = rsync 到一个全新 release 目录 → 校验 → `mv -T` 原子切换符号链接 → 健康门(盒子 catalog 资产数 == 公网 total,不达标非零退出);**线上目录永不被 `--delete` 触碰**。`--rollback` = 原子切回上一个 release(保留最近 6 个)。catalog 仍按 mtime ~2s 自动反映(rglob 跟随符号链接)。该脚本经对抗式审查(22 确认 findings)加固,守卫覆盖全 6 源目录非空 + `--max-delete` 兜底,详见 [[agent-asset-consolidation]]。**回滚整个 registry 服务**(非内容):`sudo systemctl disable --now octopus-registry` + 删那条 nginx location + reload,收款不受任何影响。

**Stripe 接入(我做,2026-06-26)**:服务端原 `_create_cashier`/`payment_webhook` 是桩。已实现 Stripe(raw httpx + stdlib HMAC,无新依赖,跟手写 JWT 一致):`_stripe_checkout_url`(Checkout Session→收银台 URL)、`/billing/webhook/stripe`(验 Stripe-Signature HMAC-SHA256+5min 防重放→`checkout.session.completed`→复用 `_settle`,幂等)、`/pay/success|cancel` 落地页。App 走 H5(payUrl→浏览器),不接支付 SDK。会员是**一次性 30 天顺延,非自动续费**(Stripe Subscriptions 后续)。
- **仓库**:commit `d011882`(local 未 push,octopus-mobile)+ `.env.example` 加 STRIPE_*。
- **线上服务器**:已把同样 4 处 Stripe 改动**外科补进服务器版并部署**(锚点与仓库一致)。备份 `app.py.pre_stripe.20260626_110401`,venv 编译过、重启 health ok、`/pay/success` 200、28 处 stripe。**`PAYMENT_PROVIDER` 仍 mock → Stripe 代码休眠、零行为变化、可回滚。**

**⚠️更正(2026-06-30,实查盒子):Stripe 收款其实早已打开,不是休眠!** 之前"休眠/没建号"判断全错(记忆过时,用户+阿泽后来开了)。SSH(别名 `mi`)只读核盒子 `/home/ubuntu/octopus-server/.env`:**`PAYMENT_PROVIDER=stripe`、`STRIPE_SECRET_KEY` 已设、`STRIPE_WEBHOOK_SECRET` 已设、服务 active**。Stripe Dashboard(acct `acct_1TmRzzGlGVBUUxxL`,Workbench→Webhook)有端点 **whimsical-dream → `https://api.octoapk.com/billing/webhook/stripe`、使用中、订阅 1 个事件、错误率 0%**。**但:① 0 次投递 + 收入 ¥0 = 配好了从没真收过一笔(端到端未验证,建议实跑确认);② 待确认 test 还是 live key(未读值)+ webhook 那 1 个事件是否 checkout.session.completed;③ ✅**竞态修复已部署到盒子(2026-06-30)**:把 race-fix(`_settle` 原子认领 + webhook 金额兜底 + query_order)外科补进盒子 app.py。流程:`mi` 备份 `app.py.pre_racefix.20260630_055426` → md5 守卫确认未被阿泽改动 → scp 覆盖 → 盒子 venv import OK(58 路由)→ `sudo systemctl restart octopus-account` → health `{"status":"ok"}` → grep 确认修复在线、服务 active。**只改 3 块 22 行、未碰阿泽其它代码**,一条 `cp 备份 app.py` 即回滚。盒子收款现已幂等。**`live` key(用户确认是 live 不是 test)。** 教训:盒子状态以实查为准、别信记忆。

**离真收款剩(只欠 Stripe key + 一次配置)**:① 用户开 Stripe 账户拿 `sk_...` + Dashboard 加 webhook 端点 `https://api.octoapk.com/billing/webhook/stripe`(事件 checkout.session.completed)拿 `whsec_...`;② 我在服务器 .env 加 `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` + `PAYMENT_PROVIDER=stripe` + 重启;③ 测试卡 4242 跑通(**务必先 test 模式**,test/live 的 key 与 webhook secret 必须同模式,否则验签恒 400)。**App 侧待办**:Stripe 是国际卡,下单建议默认 `currency=USD`(GOODS 有 priceUsdCents)。⚠️**安全边界:Stripe Secret key 我不经手,由用户在盒子上亲手贴 .env;账户/绑卡也是用户做。**

**Stripe 上线前安全审计 + 加固(2026-06-30,本地 `server/app.py`)**:3 代理对抗审计支付代码。**验签 SAFE**(HMAC-SHA256 `{ts}.{raw_body}` + `compare_digest` + 5min 防重放 + raw body,无伪造/重放路径)。**揪出并修了 2 个**:① **CRITICAL 幂等竞态**——原 `_settle` 用 `SELECT 看 PENDING` 当闸、与置 PAID 非原子,并发/重复回调会双发钱;**已修:`_settle` 开头用单条 `UPDATE orders SET status='PAID' WHERE order_no=? AND status='PENDING'` 原子认领(rowcount!=1 即返 0 不发放),取写锁靠 SQLite 写串行挡并发;删末尾冗余 UPDATE;`query_order` 处理 granted=0 展示。功能测试实证:首次发 1500、重复发 0、余额只 +1000 ✅**。② 金额未核对(审计判 critical 但**实为夸大**——Stripe Checkout 金额服务端建会话时写死、用户改不了,"付$1拿$99"不成立);**仍加了防御纵深:webhook 用 `obj.amount_total`/`currency` 兜底核对订单 `amount_minor`/`currency`,不一致返 200 不结算;amount_total 缺失不阻断**。教训:支付代码 LLM 审计会夸大"金额"类(没理解托管收银台金额服务端固定),但"幂等竞态"是真问题、值得修。

**管理后台增强(2026-06-30,本地 `server/app.py`,未提交未部署)**:原 `/admin` 用户表只有余额,补了三块按用户的统计:① **累计消耗**=聊天(`usage_log.credits`)+媒体,带「聊天X·媒体Y」拆分;② **生图/视频次数** —— 新建 `media_log` 表(`id/user_id/kind/model/credits/ref_id/ts`),`_log_media()` 在 `images_generations`/`video_generations` 成功后各记一笔,**会员免费也记(credits=0)** —— 补的正是原黑洞:`_charge_media` 对会员直接 return 不留痕、会员媒体调用永远统计为 0;③ stats 加「生图次数/生视频次数/媒体消耗积分」3 卡。改动点:schema 加表、`_log_media` helper、两端点各 1 行、`admin_users` 加 4 个子查询(spent/calls/image_calls/video_calls/media_spent)+ 响应字段、`admin_stats` 加 media 聚合、前端 `loadUsers` 表头+行、`loadStats` cards 数组。**⚠️media_log 不能回填历史**(上线前的调用没存)。④ **后台 AI 经营分析**(2026-06-30):新 `POST /admin/api/ai-analysis` —— `_collect_analysis()` 把行为元数据聚合成「每用户特征 + 规则分群(高价值/待转化/活跃普通/沉睡/封禁)+ 转化/流失/VIP 名单」,再 `_qwen_complete()` 调 qwen(复用 `PROVIDERS['qwen']` base+key,`{base}/chat/completions`,非流式)出 markdown 经营分析;前端新「AI 分析」tab + `mdLite` 轻量渲染。**关键约束:平台不存聊天内容/图片 prompt(11 表全元数据,chat 只数 content 长度估 token 不落库)→ 只能做行为画像,"用户主要用来做啥(话题)"做不了,除非中转时打话题标签(只存标签,有成本/隐私权衡——用户暂没选)**。隐私:发 LLM 的画像邮箱脱敏(`a***@dom`)、无聊天内容。本地 mock 无 qwen key → LLM 段优雅降级提示,结构化分群规则实时可用;**真叙述需部署到有 key 的盒子**。规则要点:高价值=会员 or 余额≥500;待转化=非会员 and (媒体≥5 次 or 累计消耗≥40)。⑤ **意图/话题采集(选项 C,2026-06-30)**:回答"用户主要用来做啥"。`message_tags` 表(**只存 user_id+tag+model+ts,无任何聊天内容**);聊天端点挂 `_fire_bg(_tag_intent(...))` 异步 fire-and-forget,**只在每段对话首轮**(≤1 条 user 消息)触发降量;`_tag_intent` 调 qwen 把消息前 400 字归到 9 固定类目(编程/写作/翻译/学习答疑/生活咨询/角色扮演/办公效率/信息查询/其他)只存标签。模型用 **qwen 不用 agnes** 的理由:qwen 本就是聊天上游、内容早看过=零新增第三方暴露(agnes 会新增第三方 + 并发弱要异步基建);成本~¥0.00007/条、首轮触发再砍量。后台 AI 分析加「用途分布」横条 + 每用户主要用途喂 LLM。**3 代理对抗审计 + 1 复核**:隐私 PASS(内容零落库);可靠性首轮 fail→已修(qwen 调用加 `timeout` 参,打标签用 8s 短超时防挂久;`_fire_bg` 加 `_BG_MAX=64` 背压、丢弃任务 `coro.close()`)→复核 RESOLVED;数据最小化报的 2 个"critical"经核多为误判/设计(admin 鉴权后看真邮箱是对的、脱敏只对发 LLM 那份;消息发 qwen=用户已选的 C 方案权衡)。Python/本地起服务见上方 Python 条目;实测 Claude Preview `octo-admin`(8099)+ scratchpad demo 跑通、播种验证。⑥ **盈利模型 + AI管理(选项 L1+L2+L3,2026-06-30)**:新「盈利/经营」tab。**L1 财务/单位经济快照**(`_profit_snapshot()` 确定性):收入=PAID `orders` ¥;上游成本=`usage_log` token×qwen价(`QWEN_IN_PRICE`¥0.2/`QWEN_OUT_PRICE`¥2.0 每百万)+ `media_log`×Agnes价(现0);**两套口径并排**:当前(`QWEN_FREE_QUOTA`→qwen≈0)vs 满负荷(真实价);毛利率、净盈亏、已消耗积分价值(×`CREDIT_PRICE`¥0.05)、未消耗积分潜在负债、ARPU、人均成本、`costPerCreditFull`(¥/积分真实成本基)。**L2 盈亏平衡 What-if**:前端纯 JS 实时算(活跃用户/人均消耗/转化率/会员单价/成本口径拖杆)→月收入/成本/利润/盈亏平衡转化率;公式 beR=c×k/p×100。**L3 AI 经营顾问**:`POST /admin/api/profit-advisor` 把财务快照(纯聚合无 PII)喂 qwen 出诊断+定价/增长/风险建议(本地无 key 降级)。`GET /admin/api/profit` 出 L1+L2 基数。**核心洞察(demo 实测)**:qwen 便宜+媒体免费→上游成本 ¥0.00023/积分、毛利率 99.9%、盈亏平衡转化率~0.05%——**瓶颈是收入/转化不是成本**(Stripe 未上线→收入恒 0、纯补贴)。数学已手算核对一致。**✅整套后台增强【累计消耗+生图视频+AI分析+意图标签+盈利模型】已部署到 live 盒子(2026-06-30)。** 做法:`scp mi:app.py` 拉盒子当前版(2539 行,阿泽分叉基底)→ 写「区间锚点合并脚本」(`scratchpad/merge.py`:用两端未改公共锚点 A/B,把 box 的 A..B 之间换成本地 app.py 的 A..B 之间,新内容直接取自本地、A/B 之外阿泽代码一行不碰)产出 box_v2(2948 行,14 区间)→ **diff 复查:412 增、仅 4 删(全是我自己的锚点行,无阿泽支付/商品代码)** → py_compile + import(61 路由)→ 临时目录跑 box_v2 + Claude Preview `box-test`(8100)逐点 6 tab **零 JS 错** → md5 守卫(盒子未被阿泽改)+ 备份 `app.py.pre_admin.20260630_061640` + scp + 盒子 import + 重启 + health + 标记校验 + 可回滚。盒子生产库重启自动建 `media_log`/`message_tags` 表。**盒子有真 qwen key → AI 分析叙述/意图打标签/盈利诊断现在都真跑**(意图打标签已在真实聊天首轮触发,~¥0.00007/条)。**注:盒子 `_settle` 仍无 `MEMBER_MAX_DAYS` 上限(本地有,本次未带,属另一改动)。** 部署同时确认:Stripe 收款 live(`PAYMENT_PROVIDER=stripe`+key+webhook),但生产库订单仅 1 笔且收入 ¥0 = 有人下单未付成、端到端从未跑通一次(待真付一笔验证)。

相关:[[octopus-optimization-scan]]、[[octopus-ecosystem-and-os-fork]]、[[agnes-media-api]]。

**⚠️更正(2026-06-29):本机能编 Android!**之前"无 JDK"是误判。用 **Android Studio 自带 JBR(JDK 21,与 CI 同版)**:`JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`,SDK 在 `~/Library/Android/sdk`。在 git worktree 里 `echo "sdk.dir=..." > local.properties` 后 `./gradlew :app:compileDebugKotlin`(快,连带验资源/R)或 `:app:assembleDebug`(全量出 APK,~36s,依赖已缓存)。**坑:别用 `| tee`,会把 gradle 退出码盖成 tee 的 0(假绿);直接 `> log 2>&1` 再 grep `BUILD SUCCESSFUL`。** 服务端本地跑/编译见下方 Python 条目。

**本机 Python 已修 + 本地起后端实测通(2026-06-30)**:PATH 的 `python3`(`~/.local/bin/python3`)原是 **TRAE 装的坏 shim**(指向已删的 `…/TRAE SOLO CN/…/python@3.13`,一跑就 "No such file")——系统只剩 `/usr/bin/python3`=3.9.6。已装 **uv**(`~/.local/bin/uv` 0.11.25,astral 官方脚本,无 sudo)+ `uv python install 3.12 --default`(CPython **3.12.13**)→ `python`/`python3`/`python3.12` 全指向它;坏 shim 备份为 `~/.local/bin/python{,3}.trae-broken.bak`(可回滚)。**本地起后端**:`cd octopus-mobile/server && uv venv .venv --python 3.12 && uv pip install --python .venv/bin/python -r requirements.txt`,再 `ADMIN_TOKEN=… ALLOW_MOCK_AUTH=1 OCTO_DB=<scratch>/x.db .venv/bin/python -m uvicorn app:app --port 8099`。app.py 有 `from __future__ import annotations` 故 3.9 也能 import,但已升 3.12 实测 58 路由 import ok。**admin 控制台** = `GET /admin`(口令=`ADMIN_TOKEN`,空=页面 404 关闭),登录页 `#tokIn`+`#login button`,JS 把口令存 sessionStorage `octo_admin` 走 `X-Admin-Token` 头;tab 用户/用量/订单/操作审计,可改积分/赠会员/封禁,操作留痕 `admin_log`。Claude Preview 经 `.claude/launch.json` 配置 `octo-admin`(端口 8099,**preview 要自己托管、别手起 uvicorn 占口**)。注:**线上 `.env` 据记忆已配 `ADMIN_TOKEN` → `api.octoapk.com/admin` 大概率早已启用**(非 404),登需 token 值 + 可能过 nginx IP 白名单。
