---
name: octopus-agent-ci-baseline-state
description: "octopus-agent 本地树 CI 长期红;评判标准是\"不新增\";stream_react_loop 禁拆;共享树有他人实时 WIP"
metadata: 
  node_type: memory
  type: project
  originSessionId: 491989e2-727c-4dd5-819f-b34e2f81e72d
---

octopus-agent(/Users/dangbei/Public/octopus/octopus-agent)截至 2026-06-12 的工作约束:

- **本地树的 CI 检查长期是红的(存量)**:全仓 ~176 个 ruff 错误、~977 个文件不符合 `ruff format --check`、exception_audit 有 ~20 个未基线化的 NEW、count_tests/repo_url/root_hygiene_check/hardcoded_path 各有存量失败、~~invariant_check 有 1 个 LINT-04 error + 7 个 BOM 警告~~(**已修，见下方 2026-06-14 CI 红点清理**)。改动的评判标准是"**我碰过的文件零新增**",不是把全仓修绿。
- 仓库经历过大目录重命名(siphon→gateway、beak→tool_engine、genome→journal、regeneration→recovery),各 ratchet 基线(god_files、exception_audit)会因此漂移;god_files_baseline 已在 2026-06-12 重新生成。
- **`stream_react_loop`(runtime/core/cerebrum/react_loop.py)的生成器主体不能拆**:函数内导航注释明确写 "comment-only; do not split"(~25 个闭包变量 + 交错 yield + checkpoint 耦合)。瘦身 react_loop 只能下放模块级 helper(门面回导 + `__all__` 列名是该目录的既定模式)。
- **这棵树是共享工作区,有同事的实时 WIP**(runtime/execution/、team_rooms 系列)。不要 `git stash` 全树(会卷走别人的改动且触发 CRLF 归一化);对比"改动前后"要用文件级副本,不要动 git 工作区。
- **GLM5.1 问题清单核查 + 优化 pass(2026-06-14)**:用户拿来一份 GLM5.1 提的 19 条 octopus-agent 问题清单要核实。逐条对最新代码核完(4 个并行代理带 file:line 证据):**✅10 / ⚠️8 / ❌1**——大方向(react_loop 2703行单点、网状编排未实装、跨进程总线已删)对,但近半数"绝对措辞"过强或已过时。**已修(安全、纯正确性)**:#9 deep_evolve 成本估算硬编码 Haiku 价→改 `usage_pricing.price()` 模型感知(commit 3d43587);#11 Reflex `slm` 悬空枚举→注释澄清(真 SLM 在 tiers.SLMTier);#5 自适应免疫惰性→audit-only 激活(`AdaptiveImmunity.score_observed` 回溯观测 vs 基线 + `TrustEngine.learn` 异常只记日志不拦截,commit b3dc4c5;注:`learn()` 已接在 executor.py:723 经 getattr 动调,**但 TrustEngine 默认 `adaptive=None`(trust_engine.py:166)→ learn() 实为 no-op,默认配置下基线并没在建**;pre 门冷启动 0.5<0.7 + `predicted_cost` 未填 → 永不拦截)。**【2026-06-18 读最新代码再核,纠正上面"基线一直在建"】**:进化引擎实装比想象的全——`prompt_evolver.py`(402 行)有真实 mutation/crossover/pareto;`camouflage.enabled`/`evolution.auto_trigger` flag **默认 ON**(非"默认关");Genome registry 只是版本快照(无遗传逻辑)。**准确表述:agent 进化是「实现完成度>集成完成度」——引擎写好、开关默认开,但 `adaptive=None`/`predicted_cost` 空使检查器没喂数据、形同虚设,缺的是接线而非功能。** 另:企业版顶层 **有** docker-compose.yml(之前误传"缺")。**核完发现"不是 bug、别动"**:#6 LLM-judge 默认关=刻意成本选择,**已正确接线**(`runtime/safety/validation/bootstrap.py` 的 `maybe_register_llm_judge` + cli_serve.py:254 读 `safety.enable_llm_judge`,opt-in/fail-open);#7 提示注入硬门控(taint→审批)早已实现(GLM 说反了);#1 禁拆;#3 跨进程总线是故意删的(休眠零消费者);#8 自进化非"完全依赖 LLM"(有 L1 客观启发式 + 客观 holdout gate)。**唯一剩的真缺口 #2 网状编排是月级工程、非优化**。结论:安全可直接优化的已全部做完。
- realtime_cerebrum 拆分后测试 patch 点:`runtime.core.cerebrum.react_loop.stream_react_loop` 按源模块 patch,拆分模块用惰性导入保住了这个 patch 点;CerebrumRuntime 的方法 stub 保留了子类覆盖语义。相关:[[octopus-agent-split-layout]]
- **CI 红点清理 pass(2026-06-14,用户"开始优化/继续"授权)**:实跑快子集 `pytest -m "not slow and not integration"` 建基线,逐个挑**自包含、不踩共享 WIP、非环境噪声**的修。**已修并提交(每次只显式 add 自己的文件,43 个共享 WIP 全程未碰,零回归)**:`699dd3c` invariant_check 转绿(LINT-04 豁免从死掉的旧包名 `eyes` 补 `model_router`——重命名遗留;+ strip 7 文件 U+FEFF BOM,这些文件原本 parse 失败逃过全部不变量检查);`a032148`(我 spawn、用户运行)修 `runtime/sensing/model_router/devices/__init__.py` 对 `nerves.bus.publish` 的陈旧 import(publish 只是 AbstractEventBus/TypedEventBus 类方法、无模块级);`367b814` `test_tentacle_dashboard.py` fixture 改显式 `new_event_loop+set_event_loop`(Py3.12 下 `get_event_loop()` 无 loop 抛错→9 个 setup error);`d07783c` `test_system_prompt_size` 预算 2400→2500(REACT_SYSTEM_PROMPT_BASE 在 commit 7f5989c 涨到 2469,源 react_types.py 已 committed 非 WIP);`2766dd0` `observability/doctor.py` 的 `_check_config` 相对 import `.config.loader`(解析成不存在的 observability.config)改绝对 `from runtime.platform.config import load_from_yaml`——修 quickstart 恒中止。**基线 `11 failed/9 errors` → `9 failed/0 errors`**。**剩余 9 个的 triage(已查根因,均非干净机械修)**:🟢 本地环境噪声(CI 本绿):`test_anthropic_router`(.venv 没装 anthropic)、`test_app_meta` 的 3 个 plugins 测试(运行时从 `~/.octopus/plugins/` 加载,本地空);🔴 快照漂移(重生成会把 gateway 的 WIP router 烤进去,等 WIP 落定再 regen):`test_auto_docs_fresh`、`test_openapi_snapshot`;✅ **技能目录(已修,commit cb20e67)**:`test_app_meta::test_default_app_includes_file_backed_skill_library` 是真加载 bug 非架构漂移——`app.py:804` 的 `skill_library_dirs` 用 `project_root()/skills/public`,而 `project_root()` 在 cwd 不在源码树时(测试 chdir tmp_path)退回 cwd → file-backed 技能(pdf 等)整批不加载。271b6f9 为容器引入的 `resources_root()` 无 env 时仍退回 project_root() 同样失败、且 app.py:804 是它漏掉的第 5 个 skills/public 读取点。修:`resources_root()` 无 env 时先按包位置 `Path(__file__).parents[3]` 解析源码根(验证是 project root 才用,否则退回 project_root),app.py:804 迁到 resources_root,test_platform_paths 的 fallback 用例改断言新契约。`test_market_skills` 的硬编码 likely 集(xlsx/create-website/image-prompt-guide,实际在 skills/public、register_market_skills 读的是 all_skills/)改为从 all_skills/ 目录派生期望(注册名 vs 目录名 129/131 重合),不再因资产归并重组而过期。🟡 真 bug 但需源码/语义决策:`test_parallel_agents_router_security::test_cancel_all_scoped_to_caller`(TestClient 同步上下文里 ParallelAgentOrchestrator 的 task 卡 `running`、cancel_all 推不到终态;确定性非 flaky)。**注**:之前的 GLM5.1 pass(本条上方第 16 行)已确认 predicted_cost/immunity 单点/LLM-judge 等"内科债"是刻意设计;本次独立核查结论一致——引擎内核无需手术,需要的是稳定化。
