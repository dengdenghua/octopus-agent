---
name: octopus-runtime-sdk
description: "octopus-runtime 消费端 SDK 雏形(agent 接 registry):拉取→验签→落地母体现有布局,round-trip 已证"
metadata: 
  node_type: memory
  type: project
  originSessionId: 1d6d27c3-dc6a-43a7-97bf-65acc5304019
---

**octopus-runtime SDK 雏形(用户「母体呢」+ 决策「建 octopus-runtime 消费端,先 skills/data-kind」,2026-06-30)**。
这是把母体(octopus-agent)接 registry 的**消费端**,按用户**自己写的设计文档** `octopus-agent/docs/design/capability-plane.md`(dangbei 2026-06-20,303行,**非 Codex WIP**)落地:
- 文档 §B「octopus-runtime SDK」= thin client 拉取→验签→**落地产品现有磁盘布局**→绑产品 runtime,**读/解析半边永不 import 产品 runtime**。
- 文档 §A「Cortex Registry」≈ 我已建的 enterprise registry(分发面)。
- 三种 skill kind(prompt_pack/code_backed/native_builtin):**执行代码永留产品本地、不过线**,只共享 data。

**落地**(新顶层包 `octopus-agent/octopus_runtime/`,additive,零碰 18 个 Codex WIP[cowork/oct/i18n],零 runtime 耦合):
- `client.py`:`RegistryClient`(httpx+pydantic)`list_skills`/`fetch`(信封+body)+ **sha256 验签**;`RegistryAsset`/`AssetPayload` 模型。
- `materialize.py`:`materialize_skill`(registry body 无 frontmatter → 用信封 name/description **重建** SKILL.md frontmatter → 落 `<skills_dir>/<slug>/SKILL.md`)+ `sync_skills`。
- `__main__.py`:CLI `python -m octopus_runtime list [--mobile]` / `sync <slug>... --skills-dir skills/public`。
- **安全闸**:`SAFE_TYPES={skill}` —— registry 把所有 skill 粗标 `kind=code`(为将来签名/沙箱),但 `type=skill` 资产**全是 SKILL.md prompt-pack**(body 被产品当 **prompt 注入、从不执行**),故落地安全;真正可执行的(plugin)默认不落地,需 `--allow-code`。

**关键集成事实(实查母体代码)**:① 母体扫 `resources_root()/skills/public/*/SKILL.md`(92个),`register_market_skills`(`runtime/execution/suckers/market_skills.py:287`)用**通用 `_make_prompt_handler` 注册** prompt-skill,无需自定义码;② enabled 默认开(`market_skills.py:346`,只有显式 `enabled:false` 才跳)→ materialize 不用动 `skills_config.json`;③ 落地到 skills/public,母体**下次启动**自动加载(v1 不做运行时热注册)。

**round-trip 已证**:`.venv` 跑 `octopus_runtime` import runtime-free;从公网 registry sync brainstorming/dcf-model/pitch-deck → 校验+落地 → 母体自己的 `_parse_frontmatter` 全部解析成功(name/description/body 完好)。

**✅ HTTP 路由已做(用户「继续」,2026-06-30)**:新 `runtime/sensing/gateway/registry_consumer_router.py`(additive,照抄 `agent_world_router.py` 工厂+`_resolve_actor` 鉴权 dep 模式)——`GET /api/registry/skills`(列,search/category/分页)、`GET /api/registry/skills/{slug}`(详情+body 预览+installed)、`POST /api/registry/skills/{slug}/install`(`octopus_runtime.sync_skills` 下载验签落地 + `register_market_skills(respect_enabled_flag=False)` **运行时热注册、无需重启**)。`app.py` additive try/except `include_router`(在 agent_world 挂载后、企业版消费前,passes `state.registry`/`cocoloop_*` auth)。**实测**:py_compile + ruff 全过;TestClient 打公网 registry:LIST 200(search=pitch→4)、DETAIL 200(brainstorming body 10354字)、INSTALL 200(落地 SKILL.md 真实存在)。

**✅ 前端"云端"tab 已做(用户「继续」,2026-06-30)**:`frontend/src/components/store/` 干净(非 WIP)→ additive:新 `core/registry/api.ts`(fetch `/api/registry/skills` list/install,hand-rolled 不依赖 openapi-types)+ 新 `registry-skills-panel.tsx`(浏览+搜索+安装,自包含、最少依赖 Button/lucide/tailwind)+ `unified-store.tsx` 加第4个 tab「云端」(grid-cols-3→4,`StoreTab` 加 "registry")。**文案硬编码绕开 i18n WIP**(locales 是 Codex 活跃 WIP,没碰)。**验证**:`pnpm typecheck` 我的 4 文件零 error;唯 2 个 tsc error 在 `i18n/locales/{ja-JP,ko-KR}.ts`——**Codex 自己的 WIP**(给 locales 加 cowork 键、ja/ko 未补齐),与我无关。

**整条母体接 registry 已三段全通且各自验证**:SDK(round-trip)+ HTTP 路由(TestClient)+ 前端 tab(typecheck)。

**✅ 端到端最后一环已证(用户再「继续」,2026-06-30)**:找 registry 有、母体 skills/public 没有的 **5 个 net-new 技能**(patent-search/claim-extract/fto-compare/patent-risk-register/patent-import-xlsx,专利/IP 类,母体没打包)→ `octopus_runtime sync` 落地 temp → 母体自己的 `register_market_skills(fresh SkillRegistry, respect_enabled_flag=False)` **注册数=5**、`reg.has(patent-search…)` 全 True。**= 母体凭空获得它从未打包的能力**;**用 temp 目录 + fresh registry,零写真 skills/public(零 Codex 树 footprint)**。「灌进真 skills/public 母体就真用上」的机制已验证,差的只是把目标目录从 temp 换成真目录(一条命令)。

**✅ #1+#3 已做(用户选「1 3」,2026-06-30)**:**#1 灌进真 skills/public** —— `octopus_runtime sync claim-extract/fto-compare/patent-search/patent-risk-register/patent-import-xlsx --skills-dir skills/public` → **skills/public 92→97**(5 个专利/IP 技能,**untracked**,不会误入 Codex 提交);`register_market_skills` 对真目录注册 has() 全 True = **母体真有了这 5 个它没打包的能力**。**#3 「停止打包」lockfile 机制** —— 新 `octopus_runtime/bootstrap.py`:`bootstrap_skills(lockfile, skills_dir)`(按 lockfile 同步缺失、幂等)+ `write_lockfile`(从现有 skills 生成 lockfile,迁移辅助);CLI 加 `bootstrap`/`lockfile` 命令。**实测**:bootstrap 第一发同步3/第二发幂等0、`lockfile --skills-dir skills/public` 生成 97 技能的 `skills.lock.json`、ruff 全过。**= commit 一个 lockfile 代替 97 个 SKILL.md 目录、启动 sync(capability-plane §B lockfile)。**

**✅ 停止打包「可执行部分」就绪(用户选「1」,2026-07-01)**:生成 `octopus-agent/skills.lock.json`(97 技能,untracked,= 代替 97 个 SKILL.md 目录的迁移产物)+ Makefile 加 `bootstrap-skills` 目标(`python -m octopus_runtime bootstrap`,**Makefile/builder.py 都干净非 WIP**;故走 Makefile 不碰 builtins)。实测 make 目标解析 OK、bootstrap 幂等(97全在→同步0)。**✅ Codex 把我整套 registry 消费提交了(`968aae868 feat(registry): add cloud skill install surface`,2026-07-01)**:octopus_runtime 包 + registry_consumer_router + 前端 registry tab/api/panel + skills.lock.json + 5 个 net-new 技能,全进 git(api.ts 加了 `OctApiError` 小润色)。**= "他提交了" 指 Codex 提交了我的活。** builtins.py 也随之提交变干净 → 当时挡 #1 的 WIP 解了。**✅ #1 已补(用户「你有两个不是没有做么」+ Codex 解锁,2026-07-01)**:builtins.py `register_market_skills` 前加 additive 容错 hook —— `resources_root()/skills.lock.json` 存在则启动调 `bootstrap_skills` 从 registry 同步缺失技能,registry 挂了只跳过用磁盘已有、不阻断启动。py_compile+ruff 过、hook 实跑同步0/已有97。**⚠️ #2 `git rm` committed 技能 = 仍没做**:已验证**安全可逆**(lockfile 97 == registry 97,缺 0 → 删了能从 registry 全拉回)+ #1 容错 hook 兜底,但它是**热 Codex main 上删 ~97 文件的大破坏性 diff**,且**移除 bundle = 去掉离线兜底**(registry 已是事实源/启动自动同步,bundle 仅作离线兜底)。**需用户明确说"删"才执行**(destructive,不擅自做)。**⚠️ 可靠性提醒**:全删 bundle → 母体每次启动都依赖单盒 registry(api.octoapk.com),它挂了就无技能启动;建议留 bundle 作离线兜底 OR 给 bootstrap 加缓存韧性,再考虑全删。**剩下要你执行的全migration**:commit skills.lock.json → 分支上 `git rm -r` 92 目录 → gitignore skills/public → bootstrap 接进 boot/CI → 测离线。
**真·其它剩下(需你拍板)**:① (见上,停止打包的 git rm + builtins 钩子待你+Codex);② plugins/agents/roles materializer(roles 可复用母体现成 `enterprise_assets_router` 的 scaffold;plugins=code 需沙箱)未做,更大;③ **未 git 提交**(SDK/路由/前端/bootstrap 全 untracked additive + skills/public 5 个新 dir,Codex 共享树,提交/推由你决定);④ 提交前需 `make openapi-snapshot` + `make lint` + 待 Codex i18n WIP 落定后前端 tab 文案 i18n 化。

**⚠️ 架构级发现:registry 是「body-only」,不是「full-bundle」(2026-07-01,用户授权全做时发现)**。试做停止打包 `git rm` 时发现:`git rm -r skills/public/*/` 删的是 **748 个文件**不是 97 个 —— **57/97 技能目录带额外文件**(`dcf-model`=SKILL.md+TROUBLESHOOTING.md+requirements.txt+scripts/;`3-statement-model`+references/ 等),agent 角色目录也多文件。**registry 只存那段 markdown body,scripts/requirements/references 全没有** → 删了/装了都不全。**已安全回滚**(`git checkout HEAD -- skills/public` 还原 97 目录 + 还原 .gitignore + 删分支 + 回 main,builtins hook 改动保留)。**影响**:① **停止打包 `git rm` = 真数据丢失**,不能做(我之前"安全可逆"只验了 SKILL.md 覆盖、漏了多文件);② **从 registry install 带脚本的技能/角色 = 装不全**(只拿到 prompt);③ **真正的 prerequisite = registry 改存/发「full-bundle」**(整目录打包 tar+checksum,producer 摄取全目录 → 服务 archive → 消费端 materialize 全目录)—— 这才解锁安全停止打包 + 完整 install。是个多层大改,待用户定 scope。**✅ full-bundle 已实现+上线(用户选「实现 full-bundle」,2026-07-01)**:① **producer** `octopus-enterprise/scripts/package_skill_bundles.py`:把 agent 多文件技能目录打成确定性 tar.gz(排除 pycache)→ `sources/skill-bundles/<slug>.tar.gz`(57 个/1.5M)。② **registry** `catalog.py` 加 `_SKILL_BUNDLES_ROOT`+`skill_bundle_path()`;`registry.py` 信封加 `bundle:{ref,checksum,size}`(按 path+mtime 缓存 sha)+ `GET /registry/assets/{id}/bundle` 端点(serve tar.gz + X-Checksum-Sha256 头 + ETag)。③ **consumer** `octopus_runtime`:client 加 `BundleRef`+`bundle` 字段+`fetch_bundle()`;`materialize_skill` 改 bundle-aware(有 bundle 则 `_safe_extract`[防穿越/防 symlink]解整目录,否则 body-only),sync 传 client。**部署+验证**:registry.py+catalog.py+skill-bundles 源 rsync 到盒子 + 重启(备份 .bak);**线上 dcf-model 信封带 bundle、57/97 有 bundle、公网 `octopus_runtime sync dcf-model` 落地全目录(SKILL.md+TROUBLESHOOTING+requirements+scripts/)**,473 健康、收款零影响。**= body-only(纯 prompt)+ full-bundle(带脚本)双轨,消费端自动选**。⚠️ 我的 consumer 改动(client.py/materialize.py)**未提交**(Codex 提交的是旧 body-only 版);停止打包要安全,得先提交 bundle-aware consumer(否则 fresh-clone bootstrap 只还原 SKILL.md)再 git rm。roles full-bundle 同法可扩(未做)。**✅「1 2」已提交(用户授权,2026-07-01)**:**② agent 停止打包** = 分支 `chore/stop-bundling-fullbundle` 提交 `8bf26fbb5`(752 文件:`git rm` 97 技能目录 748 文件 + bundle-aware consumer + builtins hook + gitignore;**0 Codex WIP**;`git checkout main` 后 main 干净[97 目录在、我的改动还原回分支]→ **整套 full-bundle consumer + 停止打包都在这一个分支,合并即落 main**)。**① enterprise producer/registry** = enterprise main 提交 `b41550b`(registry.py+catalog.py+package_skill_bundles.py,+493)。**都未 push**(本地提交;agent 分支可 `git branch -D` 回退、enterprise 可 revert)。剩:用户择期 merge agent 分支 + 部署、enterprise 把 registry_app/clients/sources 等更大产物单独提交。**已做的 (a)**:registry.py 的 `mode/platforms` **已重部署到盒子**(线上真发字段:brainstorming→inject/[mobile,desktop],xlsx→[desktop],89/97 适配;total 473、收款零影响)。

**⚠️⚠️ 重大更正:「215 本地技能里 122 个没同步」不是缺口,是两个同名 `register_all` 的坑(2026-07-01 深查坐实)**。用户/Codex 报告本地技能数(215)远超 registry(97),疑似策展/同步遗漏。**深查结论:不是缺口,是分类不同,且我全场 session 手测用错了函数**:
- 仓库有**两个不同的 `register_all(registry)`**:① `runtime.execution.suckers.builtins.register_all`(我全 session 手测用的这个,`skills/public` if存在 else `all_skills` **二选一**);② `runtime.execution.all_skills.register_all`(**`builder.py`/`build_from_config` 真实 `octopus serve` 走的这个**,按 `_GROUP_REGISTRARS` 逐组注册,其中 `"market"` 组调 `register_market_skills(registry, respect_enabled_flag=False)` **不传目录**→落到 `market_skills.py` 默认值 `all_skills/`;**注册完这组后,函数末尾再额外调一次** `all_skills_dir=skills/public` ——**两个目录都注册,是叠加不是二选一**,`skills/public` 同名的被内部去重跳过、只净增新名字)。
- 活 server(10:25:51 启动,`/api/skills` 查证)证实:`trusted_source` 分布 `all_skills:227`(136物理目录+alias展开)+ `public:82` + private/browser_act/builtin/computer/forged 等其它组,**总 357**。`3-statement-model`/`accrual-schedule` 这类**明明在 skills/public 里的名字,trusted_source 却是 `skill://all_skills/`**——证明 all_skills 组**先注册、赢得名字**,skills/public 补的是净增部分。
- **真实结论**:`runtime/execution/all_skills/`(136目录/约227技能名)是**内置技能层**,跟 browser_skills/git_skills/computer_skills 同档次,**从设计上就不经 registry 分发**——不是"该同步没同步"。`skills/public/` 才是"停止打包"这条线处理的**外置层**,与 registry **精确 1:1 镜像、零缺口**(registry 技能数 == skills/public 目录数,实测校验过)。
- **连带更正**:我这一整场 session 手测 `register_all()` 报的数字(97/130/78/5 等)测的都是 `builtins.register_all`,**不是生产真实注册路径**,不能拿这些数字类比真实 `octopus serve` 环境的技能目录。以后测注册数须 import `runtime.execution.all_skills.register_all`,或直接查活 server 的 `/api/skills`(权威)。
- 活 server 还有个隐含坑:**只在启动那一刻注册一次**,之后 `skills/public/` 再怎么变(bootstrap/install)不重启就不反映到这个进程内存里——之前用这个活 server 测试 bootstrap/install 效果时如果没重启,结果不可信。
- **已写入 `docs/design/capability-plane.md`**(新增小节「两个同名 register_all,别测错」,§2 死代码章节后)存档,避免以后又踩。

**✅ `all_skills/` 137→130 个技能已发布上云(用户「137 也同步云端啊」,2026-07-01)**。深查坐实:`runtime/execution/all_skills/`(134 物理文件夹/141 理论名字)跟云端 registry 几乎零重叠(仅 4 个直接撞名:code-quality/frontend-design/pdf/typescript-best-practices),此前"215 vs 97"的缺口不是同步遗漏而是**这批内容从没被发布过**。新 producer `octopus-enterprise/scripts/publish_agent_all_skills.py`(复用已验证的 `package_skill_bundles.package()`,零改动打包逻辑):130 个(排除 4 撞名)→ flat `.md`(全部)+ full-bundle tar.gz(93 个多文件的,含 `cn-finance-data`229文件/`backend-building`67文件等大体量)。**发布前密钥扫描过关**(无硬编码凭证/敏感文件名)。本地验证:agent-skills 92→222、skill-bundles 57→150、registry skill 97→227,下载+解压回环测试通过(ad-copywriter 4文件完整、body-only 技能 checksum 对)。enterprise 提交 `8d28986`(224文件,窄提交排除 Approval 等 27 个不相关文件)。

**⚠️ 部署时踩了个操作坑**:直接 rsync 到盒子 `sources/`(符号链接指向当前 release)+ 重启,**绕开了自己建的 `publish-registry.sh` 蓝绿脚本**——回想起来这不是本次首次,mode/platforms 部署和第一批 57-skill full-bundle 部署也都是这么干的,一直没走蓝绿。首次备份命令 `tar -czf` 没加 `-h`,只打包了符号链接本身(4KB空壳,不是真备份)——已发现+补了正确备份(`-h` 追踪符号链接,8.5M/667文件)。**用户选"要"接进蓝绿流程后**:跑 `publish-registry.sh --apply` 补一次规范发布,生成干净新 release(`20260701_140333`),校验 6/6 匹配、健康门通过(603==603)、release 历史保留 4 个可回滚点。**以后 sources/ 任何内容更新(不管哪个 producer 写的)都应走 `publish-registry.sh --apply`,不要手动 rsync**——这条已验证过一次能正确捕获"外部直接改过 sources 内容"的场景(dry-run 显示零意外差异,--apply 干净生成新 release)。

**✅ 母本自己的操作性角色(admin/coder/general 等10个)已发布上云(用户「一起做,进化历史也蒸馏发布」,2026-07-01)**。这是继 all_skills 之后的**第三条角色源**(`source=octopus-agent`,跟 echo 铸造的 canon白幽灵/non-canon util_* 两条线平行)——这10个是真正在跑的功能性人格(SOUL系统提示+`profile.jsonc`的`capabilities`权限注入块+`tool-registry.jsonc`技能绑定),不是echo的lore卡片,**不是重复内容,是全新第三源**。
- **catalog.py/registry.py 扩展**:角色也支持 full-bundle(跟技能同法,整目录tar.gz);新 `_asset_from_echo_profile` 复用(它本就是通用 profile→资产 loader,`source`字段从文件里读,非echo专属)。
- **producer** `octopus-enterprise/scripts/publish_agent_roles.py`:bundle 含 profile.jsonc+agent-core模板文件(SOUL/IDENTITY/AGENTS/BOOTSTRAP/TOOLS.md+tool-registry.jsonc)+avatar+当前visuals;**明确排除运行时私有状态**(MEMORY/HEARTBEAT/USER.md+`.scores.jsonl`+visuals/backups历史)。deps 两块:①`tool-registry.jsonc`的`private_skills`过滤到真有云端内容的(跳过脏引用/内置工具如web_fetch/通配符`*`);②有`.scores.jsonl`的3个角色(admin/coder/general)用 echo-universe-engine 的 `experience_foundry.mint_experience()` 蒸馏成 experience 资产(**只含定量适应度,不编造 avoidance_rules/lessons**——这些角色没有任何定性数据可用)。
- **本地8个echo同名角色明确不删**(用户此前"把本地素材都给云端本地移除"的指令,经内容核实后我拒绝了删除部分——local admin的SOUL是完整功能系统提示,cloud echo_leon只是~400字lore卡片,内容完全不同不是重复;且`.scores.jsonl`进化数据+~347M头像迭代历史一旦删除不可逆)。**这次是纯发布,不碰本地文件**。
- **验证+部署全链路**:本地 boot 测试(10角色+3经验资产正确加载)→ bundle下载解压回环(checksum对、无runtime-state泄漏)→ 密钥扫描过关→ enterprise 窄提交(`6da8399`)→ `publish-registry.sh --apply` 数据同步(release `20260701_153537`,606==606健康门过)。
- **⚠️ 新踩的坑(以前没遇到过)**:`publish-registry.sh` 只同步 `sources/` **数据**,不碰 `app/agent_assets/catalog.py`/`app/routers/registry.py` **代码**!盒子跑的是独立轻量服务 `/home/ubuntu/octopus-registry/registry_app.py`(uvicorn直启,systemd `octopus-registry.service`,**不是** enterprise 主后端 `app.main`,**没有git**,是手动维护的独立文件树,只是恰好复用同名 `app/agent_assets/catalog.py`+`app/routers/registry.py`)。数据同步后角色仍404,查健康门数字对不上(+3只是experience,+0 roles)才发现——**catalog.py/registry.py 加新加载逻辑(如这次的 `_AGENT_ROLES_ROOT`)必须额外 `scp` 到盒子对应路径 + `sudo systemctl restart octopus-registry` 才生效**,纯数据类改动(新增/更新已有目录下的json/tar.gz)才能靠 `--apply` 的 rsync+活管线mtime指纹自动反映。**以后凡是改了 catalog.py 或 registry.py 的加载/端点逻辑,发布清单要多一步:scp 两个文件 + restart 服务**,不能只跑 `publish-registry.sh --apply`。已验证:重启后 role/admin 200、bundle checksum对、总数616、健康检查ok。

**⚠️⚠️ 重大发现+已修:195个util_\*根本不是echo造的,是vendor第三方开源库,发布时丢了署名/许可证(用户问「echo怎么造出角色来的」深查坐实,2026-07-01)**。查证 `echo-universe-engine/echo_engine/neural/utility_foundry.py`+`generators.py`+`llm.py`:
- **canon角色(9个白幽灵小队)**:设计上该有"魂"(LLM按canon硬设定写新角色卡),但 `Settings.model_provider` 默认`"stub"`、仓库无`.env`——**现在没接真LLM**,`run_character_agent()`每次吐的都是代码里写死的同一份"Mira Voss"示例草稿,不是AI现写。
- **util_\*(195个)完全不用LLM**,是`mint_utility_agent()`纯模板拼接。而且`utility_roles/`整个目录是**vendor进来的第三方开源库**——根`LICENSE`=MIT(Copyright 2025 AgentLand Contributors,206个md),但`utility_roles/financial-services/`子树带**自己独立的**`LICENSE`=Apache-2.0(80个md,模板未填具体版权方姓名,无NOTICE文件)——**两种许可证,不能一刀切**。铸造时`creator`字段被写死`"echo-universe-engine"`,且不带任何许可证声明就发布到公网registry——署名和法定版权声明全部丢失。
- **已修**(用户AskUserQuestion选"补上署名+MIT声明"):`utility_foundry.py`新增`license_info_for(input_dir, rel_parts)`按spec所在子目录精确返回许可证信息,`mint_utility_agent`/`mint_from_markdown`/`_profile`全部穿线;profile新增`source`(agentland|oss-financial-plugins)/`license`(MIT|Apache-2.0)/`licenseAttribution`(人话说明)/`licenseText`(许可证全文,原样embed)四个字段。`catalog.py`的`_asset_from_echo_profile`透传三个license字段;`registry.py`信封加`license`/`licenseAttribution`短字段(bulk list不放全文避免撑大),`licenseText`只在`/download`详情给(跟`body`同规格)。**验证**:185个MIT+10个Apache-2.0,精确对应;自研内容(echo canon 9个+octopus-agent 10个)license仍是null没被误伤;线上`api.octoapk.com`验证通过(`role/util_design_ux_architect`→source=agentland/license=MIT,`role/util_earnings_reviewer`→source=oss-financial-plugins/license=Apache-2.0)。echo-universe-engine提交`8ade028`(仅utility_foundry.py,该文件此前就未入git,首次提交)、enterprise提交`7148e10`(catalog.py+registry.py+195个刷新的echo-utility/*.json)。部署:两边都过——data走`publish-registry.sh --apply`(616==616)+code走scp两个文件到盒子+`systemctl restart`(这条"改catalog.py/registry.py要额外scp+restart"的坑上次已入memory,这次直接照做没再踩)。
- **遗留(用户选"先不接了,改天再说",2026-07-01)**:canon线LLM仍是stub空转。已查清接入方案(下次直接用,不用重查):`octopus-agent`自己有个"oct网关"(`runtime/platform/config/schema.py:219` `OctConfig`,`base_url=https://api.octoapk.com`,`default_model=qwen3.5-flash`),鉴权是email+验证码登录换JWT(`runtime/adapters/integrations/oct/router_auth.py`的`/email/send`+`/email/login`),换到的`oct_token`是**静态Bearer token**(非每次都要OAuth),恰好跟echo `llm.py`本来就有的"固定Bearer key调`{base_url}/chat/completions`"完全对上——**只要`ECHO_MODEL_BASE_URL=https://api.octoapk.com/v1`+把oct_token当`ECHO_MODEL_API_KEY`塞进echo的`.env`,零代码改动**。卡点只是需要真走一次登录(发验证码到某邮箱→用户回传验证码→换JWT),用户选了先不做。

**✅ all_skills/ 本地long-tail清理:删33个,留102个(用户「保留注册的+云端的」,2026-07-02)**。135个目录按两个维度核对(角色`tool-registry.jsonc`的`private_skills`引用 + `meta_router.py`硬编码特殊分发表)——**注意"注册的"不能只查private_skills**:94个被角色引用,另**7个被meta_router特殊分发但不在任何角色private_skills里**(`agnes-image-generate`/`agnes-video-generate`/`agnes-video-poll`/`seedance-video-generate`(Agnes媒体生成,付费功能)+`agent-visual-kit`(有专门测试+前端UI)+`agent-generator`(前端页面引用))——纯查private_skills会误删这7个。剩33个(deal/M&A/bond等金融长尾)本地零引用、云端已核对完整(bundle或单文件body一致,含3个自带LICENSE的目录[earnings-preview-beta/funding-digest/tear-sheet]LICENSE文件在bundle里确认原样保留)→ `git rm`(agent仓库commit `e0da54b4c`,50文件/13996行)。`runtime/execution/all_skills/__init__.py`的`_add_file_backed_skill_catalog()`是**动态glob**(非硬编码manifest),删目录后`register_all()`自动少注册,零代码改动,pytest 122过。docs/auto自动生成文档regen会带出其它并发工作的drift(module数852→855等),已排除不提交,只保留跟这次删除相关的部分(其实all-skills.md本身内容不受目录数影响,无需更新)。

**✅ 商城tab前端去重已修(用户「前端显示会去重么」,2026-07-02,本季最早那句"前端显示里面要去重啊"终于闭环)**。`registry-skills-panel.tsx`(云端商城tab)此前只按 session 内点击记录判断"已安装",不跟本地已注册技能(`useSkills()`)交叉核对——本地已有的(all_skills/skills-public 里的)在云端商城tab还是显示"安装"按钮,误导重复安装。修法:按 slug 做交叉核对(`registrySlug(cloudSkill.id)` vs 本地 `skill.name`,两边约定一致的kebab-case,已抽查确认),本地已有直接显示"已安装"+禁用按钮。浏览器实测:刚删的33个(deal-screening等)正确显示"安装",仍保留本地的(agnes-image-generate等)正确显示"已安装"。commit `8c4402a8b`。**注意**:`app/workspace/skills/page.tsx` 才是真正在用的Skills页面(已安装/商城两个tab),`components/store/unified-store.tsx` 是另一个较早/并行的4-tab商城入口(agents/plugins/skills/registry),两者都引用同一个 `RegistrySkillsPanel`,这次修的是共享组件,两边都生效。

相关 [[agent-asset-consolidation]](registry 分发面)、[[octopus-mobile-skill-marketplace]](mobile 消费端,参考 client 同源)。
