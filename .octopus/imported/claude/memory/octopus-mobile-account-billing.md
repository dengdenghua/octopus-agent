---
name: octopus-mobile-account-billing
description: octopus-mobile(同级 Android 项目)的自有账号+付费体系实现 + 该仓的构建方式
metadata:
  node_type: memory
  type: project
  originSessionId: 73dba696-f7dc-4ee6-899f-63f46a601f05
---

**同级项目**:`/Users/dangbei/Public/octopus/octopus-mobile`(独立完整仓,非 octopus-agent 内)——Kotlin/Android AI 自动化 App(LLM 经无障碍服务 `ClawAccessibilityService` 操作安卓,渠道:钉钉/飞书/QQ/Discord/Telegram/微信)。包名 `com.apk.claw.android`。还有 octopus-enterprise、octopus-os 也是同级。

**该仓构建方式(关键,本机无系统 JDK)**:`export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`(OpenJDK 21)+ Android SDK 在 `~/Library/Android/sdk`(local.properties 已配)→ `./gradlew :app:compileDebugKotlin` / `:app:testDebugUnitTest`。**所以 mobile 改动能在本机编译+单测自验**。`timeout` 命令本机没有,别用。

**栈约定**:Compose(23 文件)+ XML/View 混用;**配置类表单屏走 XML+`BaseActivity`+`CommonToolbar`+`KButton`+CardView 包 EditText**(参考 `ui/settings/LlmConfigActivity`),不是 Compose。网络 OkHttp+Retrofit+Gson;持久化 **MMKV via `utils/KVUtils`**(`getString/putString` + `getXxx/setXxx` 模式);协程 lifecycleScope/runBlocking;i18n 三份 `res/values{,-zh,-ja}/strings.xml`;Manifest `<activity android:name=".ui.x.X" android:exported="false"/>`;设置页 `MenuGroup.addMenuItem(leadingIcon,title,onClick,showDivider)`(onClick 是普通 lambda,导航项可直接 startActivity,**不必动 SettingsViewModel 的 MenuAction 枚举**)。KButton 继承 AppCompatTextView,默认 colorBrandPrimary 底,`app:btnBackground/btnTextColor/btnCornerRadius/btnBorderColor`。

**已实现(本 session,参考 Molili 形状但 octopus 自有、mock-first、外部厂商收口后端)**:
- 数据层包 `account/`:`AccountModels`(Gson DTO,钱用 fen)、`AccountGateway`(接口)、`MockAccountGateway`(内存默认:验证码固定 `123456`、新用户送 100、订单自动 PAID 到账、每日领 20)、`HttpAccountGateway`(OkHttp+Gson,连自有服务器)、`AccountStore`(MMKV 存 token/手机/积分)、`AccountConfig`(baseUrl+mockMode,**默认 mock / baseUrl 空即 mock**)、`AccountRepository`(单例,按 config 选网关,StateFlow 状态,Result 包装)。
- UI 包 `ui/account/`:`LoginActivity`(手机+验证码+60s 倒计时,mock 自动填码)、`AccountActivity`(余额/套餐充值/每日签到/登出;充值=createOrder→有 payUrl 开浏览器收银台否则直接轮询→PAID 刷新余额)。布局 `activity_login.xml`/`activity_account.xml`/`item_goods.xml` + `ic_account.xml`。
- 接线:Manifest 注册两 Activity;`activity_settings.xml` 加 `accountGroup`;`SettingsActivity` 顶部加"账号与充值"入口(直接 startActivity,登录态决定开 Account 还是 Login);三语 strings 各 +32 条 `account_*`/`menu_account`。
- 测试 `app/src/test/.../account/MockAccountGatewayTest`(5 例:发码/错码拒绝/新用户送分/充值到账/每日领一次)。

**后端 API 契约**(octopus 自有服务器待建,SMS/支付/大模型厂商均服务端决定、app 厂商无关):`POST /auth/sms/send|/auth/sms/login` · `GET /account/profile|/account/balance` · `GET /billing/goods` · `POST /billing/orders` · `GET /billing/orders/{no}` · `POST /account/daily-claim`。Bearer token 鉴权。接真服务器只需设 `AccountConfig.baseUrl`,`HttpAccountGateway` 已实现该契约。

**验证状态**:`compileDebugKotlin` BUILD SUCCESSFUL + 单测全绿。**未在真机/模拟器跑过**(本机无法),UI 仅编译+约定对齐验证,非运行时截图。**未提交**(git 工作区列着这些改动,待用户点头才提交)。

**已实现(freemium 计费策略 + 服务端中转骨架)**:产品模型=**后台统一接小米 MiMo key(只在服务端中转,默认路径,按 token 扣积分)**;**BYO 接自己的大模型 = 会员特权**(当月买"会员月卡"SKU 解锁,BYO 走客户端直连不扣积分);**优先平台**(默认走付费体系)。
- App:`BalanceResult`+`membershipActive/membershipExpireAt`;`Goods`+`kind`("credits"|"membership");`AccountStore.memberExpireAt`+`byoUnlocked`(=expire>now);`AccountState`+byoUnlocked;**`LlmRouting.effective()`**=路由唯一决策(默认平台 relay[baseUrl=AccountConfig.platformLlmBaseUrl=baseUrl+/v1,key=account token,model=platformModel 默认 mimo-v2-flash];会员且 modelSource=="byo"且有本地配置才走 BYO;**中转未配置(baseUrl 空)或未登录→回退本地 LLM 配置,绝不 brick**);`AppViewModel.getAgentConfig()` 改成走 LlmRouting;`SettingsActivity` 的 LLM_CONFIG 入口在 `relayConfigured && !byoUnlocked` 时弹"会员专享"引导充值;`LlmConfigActivity` 保存即 `AccountConfig.modelSource="byo"`;AccountActivity 显示会员状态。**整套策略仅当 `AccountConfig.relayConfigured`(baseUrl 非空)才生效**——没部署中转前 App 行为不变。MockGateway 加会员 SKU + 购买解锁 30 天。
- **服务端中转骨架** `octopus-mobile/server/`(FastAPI 单文件 `app.py` + sqlite,只依赖 fastapi/uvicorn/httpx[惰性]):实现全部 AccountGateway 契约 + `POST /v1/chat/completions` 中转(校验 token→积分>0 否则 402→转发 MiMo→按 usage 扣积分)+ 支付 webhook 桩。SMS/支付/MiMo 全 env 可插拔,**默认 mock 本地跑通**。`.env.example`/`README.md` 齐。部署:venv+uvicorn 127.0.0.1:8081 挂现有 nginx。**v0.2 参考 Molili 补了三样**(用户"补"):① 中转**流式 SSE 透传**(注入 stream_options.include_usage,末尾抓 usage 扣费)② **`/v1/models` 模型目录 + 每模型 multiplier 倍率定价**(MODELS_JSON 可覆盖;扣费=ceil((tin+tout)/1000×CREDITS_PER_1K_TOKENS×倍率))③ **JWT(HS256,stdlib 自实现无 PyJWT)会话**(替掉 sessions 表,login 发 JWT、actor 验 JWT)。扩展冒烟全过(JWT 登录/鉴权、目录、倍率扣费 pro1.0x→-2/flash0.2x→-1、坏 token 401、relay 503)。**验证**:`compileDebugKotlin`+单测绿;server `py_compile`+用 octopus-agent 的 .venv 跑 TestClient mock 全链路冒烟通过(会员激活、relay 无配置 503)。

**性能/服务器实测**(那台 `ssh mi` = AWS Lightsail):2 vCPU(Xeon)/ **911MB RAM(已被一个 Next.js 占一半,剩 ~400MB)**/ 38GB 盘 / Python3.12+Node22,无 Docker/PG/Redis;80/443 有 web 服务、3000 跑着 Next.js。中转是 I/O 密集(不跑推理),2 核够;**1GB 内存是瓶颈**,MVP/几十并发可用,正式上量前升 2–4GB + 中转拆机。SQLite 别上 Postgres(省内存)。

**已提交**:并发会话先把 P1 用无关消息 `385d796 "fix(release)...Coil"`(实含 1265 行账号代码)提了;我的 P2+server 两条干净提交(显式 pathspec,main):`ce54d20 feat(account): 会员解锁BYO+默认平台路由` + `439af24 feat(server): 中转骨架`。**教训**:octopus-mobile 也有并发会话会 `git add -A` 扫走在制品,务必显式 pathspec。

**已部署到服务器(`ssh mi`)**:`rsync server/ → ~/octopus-server/`;免密 sudo 装 `python3.12-venv`;`.venv` 装 fastapi/uvicorn/httpx;写干净 `.env`(mock、随机 JWT、**MiMo 留空**、OCTO_DB 绝对路径)——注意 **systemd EnvironmentFile 不支持行内 # 注释**,.env 必须纯 KEY=VALUE;systemd 服务 **`octopus-account`**(User=ubuntu、WorkingDirectory=~/octopus-server、`uvicorn app:app --host 127.0.0.1 --port 8081`、`MemoryMax=300M`、enable --now、开机自启)。curl 冒烟全过,**uvicorn 仅占 34MB**。管理:`sudo systemctl restart/status octopus-account`、`journalctl -u octopus-account`。**MiMo 已接真实并验证**:key/base 来自 **octopus-agent `data/custom_models.json` 的 `mimo2.5`**(base=`https://token-plan-cn.xiaomimimo.com/v1`,模型 `mimo-v2.5`/`mimo-v2.5-pro`,supports_thinking+vision)。已写进服务器 .env(key 经 ssh stdin 不外显)。app.py 目录改成真实模型(mimo-v2.5 倍率0.5 / pro 1.0)+ 加「未知模型回退 MIMO_DEFAULT_MODEL」防滥用。**实测端到端通**:登录→中转→真 MiMo 返回真 usage(model=mimo-v2.5)→扣费正确(283 tokens×0.5→1 积分,100→99)。注:思考型模型 max_tokens 太小会把 token 全花在 reasoning 上、正文空,调大即可。**已全量上线(公网 HTTPS 端到端通)**:域名 `octoapk.com` 在 **Cloudflare**(DNS-only 灰云),用户加了 A 记录 `api→32.185.238.217`;服务器 nginx 加独立 server block `api.octoapk.com`(`/etc/nginx/sites-available/api.octoapk.com`→反代 127.0.0.1:8081,proxy_buffering off + read_timeout 600s 供 SSE)+ `certbot --nginx` 签 LE 证书(自动续期)。**公网实测**(从本机 --resolve 走真公网):`https://api.octoapk.com` healthz/登录/`/v1/models`/**真 MiMo 中转**(回「我是 MiMo-v2.5…」334 tokens)/扣费 100→99 全过。App 默认接线上:`AccountConfig.baseUrl` 默认 `https://api.octoapk.com`、mock 默认关(KEY_MOCK 默认"0")、`platformModel` 默认 `mimo-v2.5`。**全部已提交**:`ce54d20`/`439af24`/`3491890`/`53d5cf9`(都 main,显式 pathspec)。**剩余可选**:真支付(微信/支付宝 webhook)、真短信(现 mock 固定码 123456)、内存升 2-4GB、JWT 吊销。注:curl 多选项在 zsh 下别用未引号变量(不分词),内联写。

**已加邮箱验证码登录**(用户在 Google/GitHub OAuth vs 邮箱码里选了**邮箱码**,更简单——镜像短信、无需注册第三方/deep-link):服务端 `POST /auth/email/send|/auth/email/login`(同 JWT;users 加 email 列幂等迁移 + email_codes 表;`send_email` mock 默认/`EMAIL_PROVIDER=smtp` 走 stdlib smtplib),已部署 api.octoapk.com 公网验证通过(邮箱用户无手机号也建号送100)。App:登录页加「手机号/邮箱」tab 切换,`AccountGateway` 加 sendEmailCode/loginEmail(Mock+Http),`LoginResult` 加 email,`AccountStore` 存 email + 展示 mobile.ifEmpty(email)。提交 `9baf78d`(server)+`dfcbe91`(app)+`2dbd6bc`(gitignore 清 __pycache__/.env/.db/.venv;注:之前 `git add server/` 误把 py_compile 的 pyc 跟踪了)。**Google/GitHub OAuth 未做**(用户选了邮箱码替代;要做需各家注册 OAuth app + App 加 octopus:// deep-link 回跳,服务端 Web 授权码流)。真发邮件需配 SMTP(octoapk.com 在 Cloudflare,可用 SES/Resend/邮件中继)。**已接真邮件(网易 163,非 126——用户原想 126 但实际是 163 手机号账号)**:服务器 `.env` 设 `EMAIL_PROVIDER=smtp` + `SMTP_HOST=smtp.163.com` `SMTP_PORT=465`(隐式 SSL,代码已支持 `commit 67b14ae`)+ `SMTP_USER/FROM=18685323548@163.com` + `SMTP_PASS`=163 客户端授权码(网页版 设置→POP3/SMTP/IMAP 开启服务后短信验证生成的 16 位,**非登录密码**;只显示一次)。授权码经 ssh stdin 写入 .env(chmod 600)。实测 `POST /auth/email/send → 200 OK` 无 SMTP 报错=真发出(经 api.octoapk.com 公网)。**凭据从用户 Chrome 页面截图读取**(扩展没连→computer-use request_access "Google Chrome" 只读截图;邮箱中间4位被打码,问用户补全)。注:163 免费邮有发送量/频率限。SMS 仍 mock(123456)→ 用户决定**先只用邮箱登录**:App `LoginActivity` 默认 mode=email + 隐藏手机号 tab(`commit 9d71e93`;服务端短信端点保留,日后接真短信可恢复)。

**模拟器实测(AVD `octo_test`,arm64 android-36,emulator-5554)**:`./gradlew :app:assembleDebug`(ABI 拆分,装 arm64-v8a APK)→ `adb install` → app applicationId=`com.octopus.mobile` 但类在 `com.apk.claw.android.*`(`am start` 要全类名;非 exported activity 直接 am start 被拦,要走 launcher SplashActivity)。**发现并修的真 bug**:账号入口之前只加在**旧 View 版 `SettingsActivity`**,而主界面是 **Compose `MainActivity`**(底部 Settings tab = `ui/compose/screen/SettingsScreen.kt`),所以**主界面根本看不到账号入口**→ 已在 SettingsScreen 顶部加「Account & Credits」卡片(未登录→LoginActivity,已登录→AccountActivity 显示余额/会员)`commit e5d0405`,顺带修登录提示文案手机号→邮箱。**端到端演示通过**:设置→账号卡片→邮箱登录(演示时临时把服务端 EMAIL_PROVIDER 切 mock 让码自动回填,演示后已切回 smtp)→ 钱包页显示 余额100/会员状态/每日签到/充值套餐(会员月卡¥19、100积分¥9.9、500积分+50¥39.9、1000积分+200¥69)/登出,全部正常。小瑕疵:商品标题是服务端硬编码中文、副标题走 app en 本地化→en 设备下中英混排(可后续把商品标题也本地化)。Compose 设置页加卡片模式:`SettingsCard(title,icon,onClick={context.startActivity(...)},compact=true){ SettingsRow(...) }`;uiautomator 读不到 Compose 文本,但能读 clickable 节点 bounds 来定位点击;adb 驱动 View 版输入框可用 resource-id bounds。

**三项准入策略(commit e74e12f,模拟器实测)**:① **强制登录**——`SplashActivity` 闸:未登录(`AccountStore.isLoggedIn` false)→ 进 `LoginActivity`(带 `EXTRA_GATE=true`)不准进主界面;登录成功后 gate 模式跳 `MainActivity`(CLEAR_TASK),非 gate(从设置账号卡片)跳 `AccountActivity`。② **自定义模型配置仅登录可见**——SettingsScreen 的 Model 卡片 `if (loggedIn)` 才渲染。③ **优先用完积分 + 自定义需当月会员(每月重置防白嫖,commit 46592b5)**——`LlmRouting`:BYO 三重前提 `byoUnlocked(当月会员) && credits<=0 && modelSource=="byo" && hasLlmConfig()`。即:积分>0 走平台扣分;积分==0 且是当月会员且配了 key → 用自己的模型;否则平台 402 提示充值。**关键:会员每月到期(服务端 member_expire_at +30 天,App 经 balance 刷新自动收回 BYO 权限)→ 非会员/过期用户积分用完也不能白用自己的 key,必须按月续费**。Model 卡片提示:非会员→"开通会员后可用(每月重置)";会员有积分→"积分用完后启用"。

**安全审计(亲手读代码 + 线上 PoC 实锤,commit bf39ce2 已修部署)**:JWT 密钥 64 位随机(✓ 不可伪造)、SQL 全参数化(✓ 无注入)。**已修**:① `SMS_PROVIDER=mock` 时 `/auth/sms/*` 返 403(原:公网开 + 固定码 123456 → 任意号无限造号送 100 积分,已 PoC;App 只用邮箱,关掉无影响;线上复测 403);② 限流(进程内滑窗 `rate_limit()`:邮箱发码 同邮箱60s1条/同IP时10条、邮箱登录 同邮箱10min10次防撞码、中转 每用户60/min);③ 签到改原子条件 `UPDATE ... WHERE last_claim_day<>today`(防并发重复领);④ 流式中转断连兜底扣费(拿不到 usage→按 prompt 估算+已收输出字符 `out_chars//4` 扣,防"读完即断"白嫖)。**内测策略(用户定:先送积分不支付)**:`.env` SIGNUP_BONUS=1000 / DAILY_BONUS=100,PAYMENT_PROVIDER 留 mock(免费充值=内测送积分机制,不动)。**已加每账号免费上限(commit 8c097fe)**:`FREE_CAP`(线上=3000)+ users.free_granted 列(已迁移);注册礼/每日领/mock充值统一走 `_grant_free()` 累计封顶,真实付费(PAYMENT_PROVIDER!=mock)不受限。实测:注册礼1000+cap150→只发150,签到/充值顶格不再发。→ 单账号免费积分(=MiMo 成本)被封死;多账号仍需真邮箱+限流,每个也封顶。**中转多上游路由 + agnes 默认档(commit `f4d5e30`,已部署)**:中转从单一 MIMO 改为**多 provider 路由**——每模型 spec 带 `provider`,按 `PROVIDERS`(mimo/agnes 各 base+key,env 注入)选上游。新增 `agnes-2.0-flash`(走 Agnes 端点 `apihub.agnes-ai.com/v1`,key 经 ssh stdin 写服务器 .env)作 `DEFAULT_MODEL`。**关键澄清(我一开始理解反了)**:agnes 是**对"平台"免费(它给我们免费额度→我们零成本),不是对用户免费** —— 用户仍按倍率 0.5 扣积分(0 余额照样 402),这部分近乎纯利润;mimo 要花真金留作高级档。`/v1/models` 加 `free` 标(现都 False)。**速度对比实测**(从 Mac):agnes TTFT 0.69s / mimo(思考型)4.44s,agnes 快 ~6 倍但波动大;质量未测。经对抗式审查(15 agent)修了:流式连接失败全额退(不再误扣 prompt 估算)、坏 MODELS_JSON 回退默认、2xx 非JSON 按估算兜底防白嫖、上游错误不回显原始体、**mock 邮箱端点默认 403(对称 SMS,仅 `ALLOW_MOCK_AUTH=1` 放开)**、流式 finally 对账加 try/except。本地 17 smoke + 线上真 agnes/mimo 实跑(各扣 1、路由正确、余额 99→97)全过。**两档位 + 无限白名单(commit server `bfdc63d` / app `a1ed169`,release v0.0.4 已部署+打包)**:用户**纠正了关键概念**——agnes 是对"平台"免费(我们零成本),**不是对用户免费**;且**不要给用户暴露模型名**,只给"两个档位"(倍率/速度不同)。最终形态:① 服务端目录收成两档 `极速`(agnes,0.5×,对平台零成本)/ `高级`(mimo-v2.5-pro,1.0×),`display_name`=档位名 + 加 `tier` 字段 → `/v1/models` 不露底层模型名。② `UNLIMITED_EMAILS`(用户加了 `dengdenghua@dangbei.com`)白名单走中转**不预扣/不扣费/不被余额拦、仍记 usage(credits=0)**:`_reconcile_usage(...,free=)` + relay `_settle()` 统一入口、`hold=0 if unlimited`。③ App:`AccountConfig` 用 `modelTier`(fast/premium,默认 fast)→ `platformModel` 内部映射(fast→agnes-2.0-flash / premium→mimo-v2.5-pro,**UI 永不暴露**);钱包页加「对话档位」segmented 卡片(极速/高级 + 选中高亮 + 文案"更快更省/更强更耗",strings×3)。验证:本地多组 smoke(白名单免费记0、普通 402、两档扣 2/4 不同倍率、目录两档藏名)+ 线上实测(/v1/models 极速0.5/高级1.0、白名单 dengdenghua 0 余额聊天免费)+ release v0.0.4 模拟器实测(档位卡片渲染、点高级高亮切换+文案变、模型名不露)。**计费校准**:之前查到 MiMo V2.5 真实价 $1/$3 per 1M(in/out,2026-05-27 永久降价),`CREDITS_PER_1K_TOKENS=1` 正合适(别调低)。**注意**:agnes key 也是从 octopus-agent `custom_models.json` 的 `agnes-2.0-flash`(base `apihub.agnes-ai.com/v1`)经 ssh stdin 写服务器 .env。**真机实测(用户用真邮箱 `dengdenghua@dangbei.com` 登录 v0.0.4)**:登录→聊天→默认极速路由 agnes、切高级路由 mimo-v2.5-pro,全端到端通;扣费实测高级档一条 in10345+out277→扣 11(=⌈10622/1000×1.0⌉)✓。**注意输入 token 大是因为整段对话历史每次都发**(聊越久单条越贵,后续可加新会话/清上下文省分)。**当前线上 .env 状态(用户要求留作测试)**:`UNLIMITED_EMAILS=` **空**(白名单已临时关、计费开),`dengdenghua@dangbei.com` 当作**计费测试号、余额设了 10 万**(管够测试用)——这与"它本是内部免费白名单号"不同,是临时测试态;要恢复内部免费就把 `UNLIMITED_EMAILS=dengdenghua@dangbei.com` 加回 .env 重启。`.env.example` 里仍留着该邮箱作示例。**会员月卡起充价 ¥19→¥39**(commit `a8e0766`,priceFen 3900,商品服务端下发 App 无需重打)。**自定义模型(BYO)配置入口已隐藏**(commit `fd0cabc`,release **v0.0.5**):`SettingsScreen.kt` 加 `SHOW_BYO_MODEL_CONFIG=false` 挡住 Model 卡片(以后放开改 true),模型状态改读 `LlmRouting.effective()`,顺带删了"其他"卡片里打不开的假占位行(浏览器引擎/LAN/设备管理/插件)。模拟器实测 v0.0.5 设置页已无 BYO/Model 配置卡。桌面包已更新到 v0.0.5(arm64+v7a)。**版本线**:0.0.3 邀请码UI → 0.0.4 档位切换 → 0.0.5 隐藏BYO+清假入口 → **0.0.6 多方在制品合并快照(commit `88a3212`)**。**0.0.6 注意**:这仓有**多个并发 agent** 同时改 Android(广场/探索 改 Explore/Toolbox 双tab+本地化、底部导航 Features→广场/Plaza、远程控制页 RemoteControlActivity[WIP 需连真机]、BrowserActivity 大改)。用户让"提交"→我 `git add -A` 整树落盘(含**删除根目录 Vite/React 网页工程** src/+package.json+vite,如需恢复网页从 88a3212 前回滚)。审计时抓到多方叠改导致 `SettingsScreen.kt` 引用已删的 `modelName` 编译断裂,补了一行修复。桌面包已换 v0.0.6(验签 v2 过)。**教训**:多 agent 并发改同一仓,打包前务必整树 `compileDebugKotlin` 兜底(叠改会互相打断编译);提交是 WIP 检查点性质。

**仍开/待办**:⑴ **mock 支付=免费充值(内测故意;上线接真支付微信/支付宝+`/billing/webhook` 验签才关)**;⑸ JWT 不可吊销(30天);⑹ 预扣-对账非原子(进程崩在二者间会丢预扣;完整方案要挂单账本 reservation ledger,内测先记日志兜底);⑺ App 默认模型切 agnes(需重打包)才真正省成本。**⑶⑷ 已修(commit `6a56f1f`)**:中转改 **pre-auth reserve→reconcile**——`_reserve_credits` 原子预扣 worst-case hold(`UPDATE…WHERE credits>=hold`,prompt 估算+max_out),不足直接 402;`_reconcile_usage` 拿真实 usage 多退少补(clamp≥0),所有出口(非流式成功/上游错/异常、流式成功/上游错/断开)都结算,上游报错全额退;杜绝「调用前判 credits>0、扣费在响应后」的并发超支(50 线程余额100/hold8 实测恰 12 过、余额4 不为负)。`db()` 加 `busy_timeout=5000`。`MAX_OUTPUT_TOKENS`(默认8192)钳 max_tokens;`CREDITS_PER_1K_TOKENS` 仍=1(保守不亏),校准公式写进注释/.env(需 MiMo 实际¥报价才能下调)。线上真 MiMo 实跑:hold≈5、283 tok→扣1、100→99 退差正确。PoC 残留测试号 `130...099`(可删)。验证法:`adb shell pm clear com.octopus.mobile` 模拟登出 → 启动 SplashActivity → 落 LoginActivity ✅;登录后落 MainActivity ✅(演示登录临时切服务端 EMAIL_PROVIDER=mock 让码回填,之后切回 smtp)。Molili(octopus-agent 内 `adapters/integrations/molili/`)是参考蓝本,**不动它**。相关:[[octopus-agent-capability-plane-saas]]、[[octopus-agent-dev-environment]]

**release 打包(commit `5093329`,v0.0.2→0.0.3 / versionCode 2→3)**:`./gradlew :app:assembleRelease`(JBR JAVA_HOME)出 ABI 拆分签名包 `app/build/outputs/apk/release/OctopusMobile_v0.0.3_{arm64-v8a(237M)/armeabi-v7a(201M)}_<ts>.apk`。签名 keystore 走 `local.properties`(KEYSTORE_FILE/PASSWORD/KEY_ALIAS/KEY_PASSWORD,已配),repo 里有 `release.keystore`;CN=Octopus Mobile,apksigner v2 验签过。**真实暗坑(打包前必查)**:release 开 R8 `minifyEnabled+shrinkResources`,而 `account/` 的 Gson wire DTO 多数字段**无 `@SerializedName`**(靠字段名==JSON 键),proguard 原本只 keep 了 agent/tool/channel/shizuku/media/navigation,**没 keep account** → R8 改名 token/credits→null,登录/积分/邀请在 release 静默全坏(此前只 debug 验过)。已加 `-keep class com.apk.claw.android.account.** { *; }`(app/proguard-rules.pro);mapping.txt 确认三个 DTO 映射到自身未混淆;release APK 装模拟器启动正常、邮箱登录页渲染无崩溃。**教训:凡经 Gson 反射的 DTO 包,release 前都要确认有 keep 规则**。APK 237M 偏大(app 自带大量原生/ML 依赖,非本次引入)。

**管理后台 /admin(用户选「可操作后台」)——commit `44de866`,已部署 api.octoapk.com**:单页无依赖前端 + `/admin/api/*`,口令登录(`X-Admin-Token`,sessionStorage)。功能:概览统计(用户/积分/会员/订单/收入/用量成本)、用户列表搜索、**手动加减积分 / 封禁解封 / 赠送延长会员 / 用量查询+CSV导出**,所有变更写 `admin_log` 审计表。**默认关闭**:不设 `ADMIN_TOKEN` → 页面 404、API 503(零攻击面)。**鉴权**:字节级 `hmac.compare_digest` + 可信 IP 限流 120/min + 可选 `ADMIN_IP_ALLOWLIST`。**封禁**在 `actor()` 与登录端点双重生效(令牌即刻失效 + 不再签发)。新增 schema:`users.banned` 列(已迁移)、`admin_log` 表。**口令**:服务器 `.env` 的 `ADMIN_TOKEN`=`openssl rand -hex 32`(64 hex,只在服务器,我没打印;用户取:`ssh mi 'grep ADMIN_TOKEN ~/octopus-server/.env'`)。`TRUSTED_PROXIES=1`(nginx 用 `proxy_add_x_forwarded_for`+Cloudflare 灰云直连→XFF 最右段=真实 IP)。**经 4 视角对抗式审查 workflow(22 agent)修了 15 项**:核心是 `client_ip` 原来取 XFF 最左(可伪造)→ 改取右数第 TRUSTED_PROXIES 个(修限流绕过 + `_rl` 撑爆 OOM,清理改驱逐整桶过期 key);membership 改原子 SQL 消 TOCTOU;`_valid_email` 收紧挡注入;CSV PII 导出留痕;ban 必须显式 bool;后台页 CSP `connect-src 'self'`。本地 26 项 smoke + 禁用态全过;公网实测:`/admin` 200(标题「Octopus 管理后台」)、无/错口令 401、对口令 200、healthz 正常。访问:`https://api.octoapk.com/admin`。

**邀请码拉新返利(用户选「每人自带码」+「受上限」)——服务端 commit `1927fdb`、App commit `0e39cd4`,均部署/提交**:模式=每个用户自带一个邀请码,新用户兑换后**双方各得 `REFERRAL_*_BONUS`(默认 200)**,且**走 `_grant_free()` 受 FREE_CAP 封顶**(防刷)。服务端:users 加 `invite_code`(注册即生成,`_gen_invite_code` 从 `ABCDEFGHJKMNPQRSTUVWXYZ23456789` 取6位)+`invited_by` 列 + partial unique index(已迁移);`GET /invite/info`(返回自己的码+invitedCount+redeemed+双方奖励;老用户惰性补码)、`POST /invite/redeem`(原子 `UPDATE users SET invited_by=? WHERE user_id=? AND invited_by IS NULL`,挡自兑/重复/无效码)。本地+线上冒烟过(A 码→B 兑→双方+200、重复/自兑/无效全 400)。App:`InviteInfo`/`RedeemResult` DTO;`AccountGateway`+Http/Mock 实现 `inviteInfo()`/`redeemInvite()`;`AccountRepository` 兑换成功刷积分;`AccountActivity`+`activity_account.xml` 加「邀请好友」卡片(展示自己码可复制到剪贴板 + 已邀人数 + 兑换框,已兑换隐藏输入)；strings ×3。`compileDebugKotlin`+account 单测绿。**模拟器实测通过**(AVD octo_test,登录态为 `demo@octoapk.com` 余额100):设置→账号卡片→钱包页底部「邀请好友」卡片正常渲染——自己的邀请码 `3YT3D5`(monospace 大号,**来自线上 `/invite/info`,证明 HTTP 链路真通**)+「已邀请 0 人 · 双方各得 200 积分」+ Copy(实测复制到剪贴板,系统剪贴板浮层显示 3YT3D5)+ 兑换框。兑换自己的码→线上返 400→toast「不能使用自己的邀请码」,账号不变(未消耗 redeem slot)。**顺手修了报错 UX**(commit `3208faa`):`HttpAccountGateway.exec` 解析 FastAPI `{"detail":...}` 作异常消息(回退原 `HTTP <code>:<body>`),所有账号接口报错都变友好提示。导航法:`am start ...SplashActivity`(已登录直落 MainActivity)→ Settings tab clickable 节点 `[789,2193][1038,2324]` → 账号卡 `[47,564][1033,787]`(Compose 无 text 但有 content-desc/clickable bounds 可定位)→ AccountActivity(View 版,有 resource-id)。
