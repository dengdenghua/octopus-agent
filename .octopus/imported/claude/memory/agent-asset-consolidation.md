---
name: agent-asset-consolidation
description: 数字分身/agent角色/模板/人力池归并企业版做可插拔资产的进度与决策
metadata: 
  node_type: memory
  type: project
  originSessionId: 1d6d27c3-dc6a-43a7-97bf-65acc5304019
---

**数字分身/角色归并企业版**(2026-06-13 起,用户发起,同 [[octopus-ecosystem-and-os-fork]] 的 PM 归并同构:消费而非 fork)。把 octopus-agent 的角色/分身/模板/人力池搬企业版做**可插拔资产**,agent 瘦身。

**用户确认的拆分**:
- **留 agent**:核心角色系统(`runtime/execution/agents/` loader/registry/scaffold/groups)+ `agents_router` 增删改查 + **数字分身语音引擎** `runtime/platform/ui/team_twin_speaker.py`(team rooms 里分身代发言,team 协调基建,类比 PM 归并保留 team_rooms)+ **5 个常用角色**(general/coder/vibe_selling/ecommerce_mind/market_researcher)+ 新建 agent(`/workspace/agents/new` + POST /api/agents scaffold)。
- **搬企业版**:`agent_world_router.py`(777行市场 API)+ `agent_market_sources/`(**286 角色模板** .md)+ 22 内置任务模板 + 数字分身**模板**(前端 `DIGITAL_TWIN_PROFILES` 创始人/产品/项目分身 + `DIGITAL_TWIN_ROLE_GROUPS`)+ financial_* 领域 agent(9 个)+ market_researcher 的市场副本。
- **删 agent**:搬走后删自带市场前端/模板/领域 agent,改 HTTP 消费企业版(配 URL,不配则只 5 角色+新建)。
- **关键区分**:分身'语音引擎'(留)vs 分身'模板/profile'(搬)是两回事;用户明确选"留引擎只搬模板"。

**企业版已有承接基建**:`Plugin` 模型(category/install_count/rating=资产商店范式)、`persons.py`(persons+roles=人力池雏形)、`agents.py`(执行层,调 agent)、`plugins.py`(可插拔商店)。

**进度**:
- **B 已完成(企业版 1a2dfd9)**:`backend/app/agent_assets/`——286 模板拷入 `sources/`、`catalog.py` 解析(frontmatter+正文,统一形态 id/name/description/category/tags/icon/source/kind + 22 内置任务模板)、`routers/agent_assets.py`(GET /api/v1/agent-assets 列表+过滤、/categories、/{id} 带 body)、main.py 注册(authenticated)。实测 catalog 加载 **308 资产**(286角色+22任务)、7 分类、get_asset 带正文、搜索可用;ruff 过。企业版前端测试 WIP(AgentChatPanel.test 等)勿带。
- **C 只读消费侧已做(agent bf45355)**:`runtime/sensing/gateway/enterprise_assets_router.py`——`GET /api/agent-market/enterprise`(列举,category/search)+ `/{asset_id}`(详情含 body),配 `OCTOPUS_ENTERPRISE_URL`(+`_TOKEN`/`_TENANT` 头)即接通企业版 `/api/v1/agent-assets`,不配则 `available=false`(前端隐藏);网络错优雅降级。app.py fail-soft 挂载。4 测试 + agent 实启验证(200、未配 available:false、路由在 openapi)。**additive 不删自带市场**。**C 剩余**:① 安装(从企业版模板 scaffold + load+register 本地 agent,需复用 agent_world_router 的 `_install_template_agent`/`load_agent` 路径,该函数与本地模板耦合深);② **destructive 删**自带 `agent_world_router`+`agent_market_sources`(286)+financial_* 领域 agent,留 5 角色+新建+分身引擎(待消费链路验证 + 用户拍板,同 os P2 删除的谨慎)。前端市场 UI 加「企业版」来源 tab = D。
- **D 已做(agent 899f2ac)**:agent 市场(`components/workspace/agents/agent-world-unified.tsx`)在「角色库/数字分身」旁加「企业版」tab(Building2Icon);新组件 `enterprise-assets-tab.tsx`(自包含,卡片网格列举,loading/未接通/空 三态)+ `agent-world-api.ts` 的 `listEnterpriseAssets()`。tsc 全项目 0 错误。**端到端可视数据验证**:写 mock 企业版(用真实 catalog 308 资产)+ agent 后端指向它,curl `/api/agent-market/enterprise` 回 available:true/308 条/search 过滤(marketing 14)/详情带 body(7470 字)全通。注:agent venv 缺 anthropic,molili-off 的 auth-off 桌面预览起不来(端到端 API consume 不需 auth-off,已用 molili-ON+mock 验);要桌面截图需 `uv pip install anthropic` 进 agent venv + auth-off + vite。**C 安装也做完了(agent d74bc33)**:`enterprise_assets_router._scaffold_local_agent`(从资产 dict 写最小可加载结构 profile.jsonc + agent-core/{SOUL=persona body/IDENTITY/tool-registry})+ `POST /api/agent-market/enterprise/{id}/install`(取 body→scaffold→`load_agent`+registry.replace/register,镜像市场安装路径)+ app.py 挂载传 registry/runtime + 前端「导入本地」按钮(installEnterpriseAsset + toast + reload 名册)。**关键风险已验**:`load_agent` 实测能加载我的最小 scaffold(比市场 _install_template_agent 写得少)→ 返回 Agent、SOUL 含 body。6 测试 + tsc 0 错误。注:default_agents_root = repo 的 `agents/`(测试用 monkeypatch 到 tmp 避免污染仓库)。**数字分身归并 A/B/C(消费列举+详情+安装)/D(前端 tab+导入按钮)全链路完成**——企业版托管资产 → agent 后端消费/安装 → 前端市场展示/一键导入。**演进为「生态资产 registry」愿景(2026-06-14,用户拍板)**:用户提出把 agent 的 skills/plugins/数字分身/角色做成**独立可下载资产**,生态各项目按需下载、**不全打包**("不用全部打包就简洁")。我评估=对的方向(npm/VS Code 市场/HF Hub 的成熟做法,消费而非 fork 终极形态)。**关键决策:破坏性删自带市场 → 取消**,改成 **Phase1④:agent 构建时不打包 `agent_market_sources`(从镜像排除,不删 814 行代码),自带市场机器变空转死代码慢慢瘦**——更安全更简洁。**方案见 octopus-enterprise/docs/ASSET_REGISTRY_PLAN.md**(企业版 d7e2328):统一资产信封(type + **kind=data/code 安全分水岭** + version + deps + checksum/sig)、Registry API(`/api/v1/registry/assets`,现有 agent_assets 作 role 实现泛化)、消费端 client(下载+本地缓存+版本钉)、**data 类先做(角色已做=Phase0 地基)/code 类后做(签名+沙箱+OS权限闸,接"网页→Agent第一攻击面")**、base 打包+长尾下载(离线可起)、os 本地镜像(本地主权)。**前面 B/C/D 不白做=registry 的 role 类型地基。** 之前列的删除清单(留5角色/删286模板+10 financial agent+剥814行市场机器+改 test_agents_router/test_app_meta_endpoints)作废,改"不打包"。
- **执行落地(2026-06-14 本会话,用户重新直接授权"删")**:指令="智能体库留10角色(AOI必留)+其余&数字分身模板搬企业版+智能体库先隐藏"。**关键约束:octopus-agent 是 Codex 活体共享树(43 WIP=capability-plane,含 agent-world-data.ts / agent_world_router.py / test_agents_router.py / agents/aoi / agents/coder)——只动干净文件、绕开 Codex WIP**(否则一提交就卷走它的 WIP)。① **隐藏角色库 tab**:agent-world-unified.tsx(干净)加 `SHOW_LOCAL_AGENT_LIBRARY=false` 开关 gate trigger、默认 tab 改 digital-twins、`?tab=agents` 回落;tsc 干净。② **删 9 个本地 financial agent**(gl_reconciler/kyc_screener/market_researcher/meeting_prep_agent/model_builder/month_end_closer/pitch_agent/statement_auditor/valuation_reviewer):回收到 repo 外 `.recycle/financial-agents-<ts>/` + 企业版 `financial-services/agent-plugins/` 早有 canonical=**三重可恢复**;commit agent `2f6d94a`(只显式 add 自己 10 文件,Codex 43 WIP 零混入)。**保留 financial_earnings_reviewer**:已提交的 `test_agents_router::test_lists_all_registered` 硬要求它为核心 preset(`make_all_agent_presets`→`load_all_agents` 扫磁盘),而该测试文件是 Codex WIP(+91 行 coder 测试,没碰 required 块)改不得→曾留它=本地 10 角色。**(后用户坚持 9 并指出我不该保留,已删 financial_earnings_reviewer → 本地正好 9 角色,commit `760d822`;接受 test_lists_all_registered 变红=CI 本就长期红,Codex 落地后再改 required 集)。**运行时无对磁盘 financial 的硬引用(`_load_financial_services_templates` 读捆绑模板目录非 agents/,与删除无关)。③ **数字分身模板搬企业版**(twins 之前不在企业版,需新建):脚本从 agent-world-data.ts 提取 **3 profile + 9 行业组/100 岗位模板**→ `backend/app/agent_assets/twins_data.json` + `twins.py`(`load_twin_assets`,kind=twin/twin-role)+ catalog.py 接入(`_load_all` 追加、`list_assets`/`get_asset` 改剥 `_` 前缀键 + body 取 `_body`)。总资产 308→**411**,ruff 过,commit 企业版 `6e1419c`。**agent「企业版」tab 自动消费(C/D 已建,无需改 agent 侧)。** 验证:test_lists_all_registered + 4 个 financial store 测试全绿。**tab 可见性最终定稿(用户两次更正后,commit `3414288`)**:我曾误把「角色库」tab 隐藏(用户明确:"没让你隐藏角色库"——角色库就是要保留的 9 个角色,不该藏)。**正确终态 = agents 页只显示「角色库」(本地 9 角色),隐藏「数字分身」+「企业版」两个 tab**(`SHOW_LOCAL_AGENT_LIBRARY=true` / `SHOW_LOCAL_DIGITAL_TWINS=false` / `SHOW_ENTERPRISE_ASSETS=false`,默认+URL 都落角色库)。搬到企业版的 twins/financial **消费走「后续 SDK」,现不在 agents 页显示**(所以企业版 tab 也藏)。**教训:别替用户隐藏 UI——他只要隐藏数字分身+企业版,保留角色库。****剩余待办(Codex WIP 阻塞,等它落地)**:(a) 删 `agent-world-data.ts` 的 twin 常量(Codex WIP,该文件 capability-plane 可能还在用 twin 数据);(b) 改 `test_agents_router` 的 required 集去掉 financial_earnings_reviewer(Codex WIP +91 行,改它会卷走其未完成工作)。两者都不是不能做、而是会破坏 Codex 活体工作,故等它落地。twins 与 Codex capability-plane 的 Persona 资源后续对齐(都是数据,可重塑)。**消费层决策(2026-06-14 用户拍板):搬出去的资产「后续通过SDK」用回来(按需下载+版本钉+缓存,而非打包/各拷一份);但该 SDK 对齐 Codex capability-plane 的 `octopus-runtime` SDK(§10/P1,Codex 正在设计),不建平行 SDK。现阶段用已建的 HTTP consume(`enterprise_assets_router` + 前端「企业版」tab)作雏形/手动版,够用。** 即:不要再独立造 registry 消费 SDK——等 Codex 的 octopus-runtime 落地或与之对齐。
- **待办 D(前端)**:agent `/workspace/agents` 市场 tab 改消费企业版资产库;保留新建 agent + 5 角色切换器;搬走 role-profile-dialog(分身模板选择器)。
- **待办 os/mobile**:同样可消费企业版资产库(可选)。

**资产分发 Hub MVP 已建(2026-06-30,本会话)**:用户重提「软件不打包技能/插件/角色,云端 Hub 按需拉取→缓存→加载」,拍板「升级 enterprise agent_assets 为独立 Hub 服务」。**已建并对齐 plan(本次):`octopus-enterprise/backend/app/routers/registry.py`**(原 hub.py 已回收到 scratch)= 公开只读分发层(对照 `agent_assets.router` 企业鉴权),挂 `/api/v1/registry/assets*`,**完全照 `ASSET_REGISTRY_PLAN.md` §1 信封 + §2 API**:`GET /registry/assets?type=&category=&search=`(列目录,信封不含 body)、`/registry/assets/{id:path}`(详情+versions,id=`type/slug` 命名空间、也接裸 slug,download 路由定义在 detail 之前避 `:path` 抢匹配)、`/registry/assets/{id}/download?version=`(payload=manifest+body,**ETag→304**)。统一信封=`{id:"type/slug", type(role/twin/twin-role/task..), version(content-hash前12), kind=data|code(★安全分水岭:role/twin/twin-role/task/prompt=data;skill/plugin=code), name/description/category/tags/icon/author/source, deps:[], content:{ref, checksum="sha256:"+sha256(body), signature=None}}`。`_hash_index` lru。接 main.py 公开挂载。**真HTTP验证全过**:411全量、?type=task22/role286、命名空间id解析、download body7470字、checksum==sha256(body)、ETag304、/registry 200公开 vs /agent-assets 401。**= ASSET_REGISTRY_PLAN Phase-1 的 role 类落地**(不是并行第三套,已对齐文档)。kind=data/code 分水岭 == 「声明式 vs 执行体」:data(角色/分身/技能指令)100%可云端按需加载;code(skill/plugin 带 dex/bash/MCP)分发清单可、加载执行要签名+沙箱或服务端工具桥。⚠️坑:`type_=Query(None,alias="type")` 直接函数调用时是 Query 对象(truthy)→ 单元测会误过滤为空;经 HTTP 才解析成 None,验证用真 HTTP。**已资产化母本 92 技能(续）**:`cp octopus-agent/skills/public/*/SKILL.md → enterprise sources/agent-skills/<slug>.md`(92个,同 frontmatter 格式)+ catalog.py 加 `_SKILLS_ROOT` 常量 + `_asset_from_md(…, kind="role")` 加 kind 参 + `_load_all` 加 skill 源(`kind="skill"`,category="skill")。catalog **411→503**(role286/skill92/task22/twin3/twin-role100)。registry `?type=skill`=92、`kind=code`(skill 不在 `_DATA_TYPES`={role,twin,twin-role,task,prompt}→按 plan「skill=code」分水岭,即便内容是 markdown 指令也"未审先不信"待签名/沙箱)。dcf-model body 48977字。**注:技能进 catalog 后,企业鉴权的 `/agent-assets`(前端 411→503)也含技能 → 前端「技能」tab 自动有内容**(可能影响企业版 count 类测试,未跑企业测试套件)。codex 插件 46 个(type=plugin,kind=code)待后续 adapter。**registry MVP = 多 type(role/skill/twin/task)+ 多 kind(data/code)分发全打通。**

**echo-universe-engine 与资产链路的关联(2026-06-30 摸清)**:`echo-universe-engine`(同级目录,Python)= **完整「AI 原生互动宇宙」引擎**(doctrine 明写 `not AI chatbot skin store`),不只 IP 层:`characters/001_zero~009_mira_voss`(白幽灵小队 Ghosts 角色卡)+ `bible/factions/locations`(canon)+ `asset_factory`(ComfyUI/SDXL 生 avatar/front/side)+ realm治理/NPC/身份分层/daily-tick 玩法。**octopus-agent 是 echo 的对话 runtime**(MVP loop:绑Ghost→读日记→"通过 octopus-agent 在 main chat 以角色发言"→daily tick)。**导出链路**:`echo_engine/neural/octopus_export.py` 的 `export_octopus_agents()`→产 `profile.jsonc`+`agent-core/tool-registry.jsonc`+avatar,默认写 `outputs/octopus_agents/`,可直写 `../octopus-agent/agents/echo_*` 或 `ECHO_OCTOPUS_AGENTS_ROOT`;CLI `export-octopus-agents [--sync-octopus-runtime]`;接线总图 `integrations/octopus_ecosystem.yaml`。**关键缺口**:echo 角色现在走 **echo→octopus-agent 直连**(绕过 enterprise catalog/registry)——所以企业 hub 截图里 Eve/Kane/Luna/Zero 是"已加入"活实例,catalog 的 286 role 是 agency-agents 不含 echo。**echo = registry 尚未接入的上游内容源**;要纳入分发:① echo 导出多写一份到 enterprise `sources/echo-characters/`(像搬技能)+ catalog 加源,或 ② registry 加 adapter 直读 echo profile.jsonc/characters。echo 已 `--sync-octopus-runtime`,接 registry 可与 Codex octopus-runtime 一并对齐。**架构建议(我给的,2026-06-30):铸造(echo)≠分发(registry)≠业务平台(企业版),三层该「连」不该「并」——echo 当角色铸造厂、registry 当分发台、企业版留 PM/计费;别把企业版整个溶进 echo(混层)。用户"所有角色从 echo 铸造"的直觉对在"生产层",但 286 agency-agents 是工具型业务角色,塞进 echo 的 Ghost/canon/realm 治理是品类错配,echo 要做需另开 non-canon utility 铸造线。** **已落地 echo→registry MVP**:`cp echo-universe-engine/outputs/octopus_agents/echo_*/profile.jsonc(9 铸造产物)→ enterprise sources/echo-characters/<id>.json` + catalog 加 `import json`、`_ECHO_ROOT`、`_loads_jsonc`(去 // 注释/块注释/尾逗号,echo profile 是 JSONC)、`_asset_from_echo_profile`(jsonc→role 信封,body 从 description+character_profile 语录/定位/能力/主题合成,`_body`+`_path=None`)、`_load_all` 加 echo 源。catalog **512**(role295[286 agency+9 echo]/skill92/twin3/twin-role100/task22),registry `?type=role` 含白幽灵小队、kind=data、author=echo-universe-engine、`?search=kane`→role/echo_kane。⚠️**当前是 echo 导出产物的快照拷贝**——echo 重铸后企业版副本会过时;真管线 = echo `export_octopus_agents` 直接 publish 到 registry store(或 registry 读 echo 输出目录),让重铸自动流。**已建 echo 非 canon 工具角色铸造线(用户选 B,2026-06-30)**:新 `echo-universe-engine/echo_engine/neural/utility_foundry.py`(纯 stdlib、无 echo_engine 依赖)= 与 canon 线(`octopus_export.py` 从 CharacterCard 产带 canon 治理的 Ghost)**平行的第二条生产线**:`mint_utility_agent(spec)` / `mint_from_markdown(md)` 把工具角色(name/description/category/tags/body)铸成**同一套 octopus 封装**(profile.jsonc + agent-core SOUL/IDENTITY/tool-registry + avatar.svg),但 **`kind=utility` + `capabilities.canon_review_required=False`**、SOUL 是"professional expert, NOT a fictional character, no canon/worldbuilding"、category 来自 spec(coder→arms[web_read,fs_writer,git,shell] 等)。**实证**:喂真实 agency-agent `design-ui-designer.md` → 铸出 `util_design_ui_designer`、kind=utility、canon_review_required=False、5 文件结构齐。**意义:echo 现在能当全角色铸造厂(canon Ghost + non-canon utility 两线),工具角色不污染 echo Ghost canon,且与 canon 角色同格式 → 同样经 catalog→registry 分发**。**✅ A+B 已落地(用户选 A B,2026-06-30)**:① **批量铸造 + 发布**:`utility_foundry.py` 加 `mint_utility_agents(input_dir,output_dir,category_map)`(递归 .md、**只铸有 `name:` frontmatter 的真 agent、跳过 README/QUICKSTART 等文档**、category 按子目录映射 `_AGENCY_CATEGORY`)+ `publish_registry_json(minted_dir,dest_dir)`(把铸出的 agent 目录压成扁平 JSON:profile 字段 + `body`=SOUL 全文 → 发布)。② **echo CLI**:`echo_engine/cli.py` 加 `mint-utility-agents --input-dir --output-dir --publish-dir` 命令(import + choices + 2 args + handler)。③ **catalog 接管**:`_asset_from_echo_profile` 改为优先用产物自带 `body`(utility=SOUL 全文;canon 角色仍合成);加 `_UTILITY_ROOT=sources/echo-utility` 源;`_load_all` 用 echo-utility **替掉** `_AGENCY_ROOT`(带回退:echo-utility 不存在才读原始 agency md,平滑迁移)。**实测**:CLI 跑 agency-agents(200 .md)→ 铸 **184** 真 agent(滤掉 16 文档)→ publish 到 `sources/echo-utility/util_*.json`。catalog **496**(source: echo 193[util184+char9]/financial80/hardware6/skills92/builtin22/twin103;**agency 源=0**),registry `?type=role`=279(util_184 + echo_9 + financial/hardware),`util_design_ui_designer` bundle kind=data、body 13122字(SOUL=真指令)、含"NOT a fictional character"=非 canon。**= echo 现在是生态全角色铸造源头(canon Ghost + non-canon utility 两线),enterprise 只索引 echo 产物分发。** **✅ 源头归 echo 完成(用户再选 A,2026-06-30)**:`mv enterprise/sources/agency-agents → echo-universe-engine/utility_roles`(200 .md 规格搬进 echo);echo CLI `mint-utility-agents` 默认 `--input-dir=utility_roles`(自包含);从 echo 自己的 utility_roles 重铸 184 → publish 到 enterprise `sources/echo-utility`。**enterprise 不再存 agency 角色内容**(sources 现仅 agent-skills/echo-characters/echo-utility/financial-services/hardware-startup);catalog `_AGENCY_ROOT` 回退分支自动失活(目录没了),零破坏,role 仍 279。**echo 现在拥有角色规格(utility_roles)+ 铸造,enterprise 只索引产物分发。** **✅ financial+hardware 也归 echo 完成(用户再选 A,2026-06-30)**:`mv enterprise sources/{financial-services,hardware-startup} → echo utility_roles/`。这俩是**插件包**结构(`agent-plugins/<插件>/agents/*.md`=角色 + `skills/*/SKILL.md`=技能),故升级 `mint_utility_agents`:**跳过 SKILL.md / skills 子树(只铸 agents 角色)、category 按「相对 input 顶层目录」映射**(`_AGENCY_CATEGORY` 加 `financial-services→financial`/`hardware-startup→specialist`)。重铸全量 utility_roles → **195 真 agent**(agency184+financial10+hardware1;技能/文档正确跳过)。catalog `_FINANCIAL_ROOT`/`_HARDWARE_ROOT` 自动失活(目录没了),零破坏。**最终:catalog 421,role 204 全 source=echo(echo-utility195+echo-char9);enterprise sources 仅剩 echo/agent-skills/builtin/digital-twin —— 角色内容 100% 出 enterprise、全归 echo。** role 279→204=清掉 75 个混进来当角色的 financial/hardware 插件技能/文档(更正确)。**剩待办**:① ✅ 插件孤儿技能已处理(见能力包);② ID 变了(`design-ui-designer`→`util_design_ui_designer`)客户端要更新;③ 仍是快照→活管线;④ 部署 registry 公网。

**人力市场愿景(用户提出,2026-06-30):角色=可雇佣的"人才",自带 能力包(skills)+插件包(plugins)+脱敏记忆经验(memory)**,雇佣=连人带装备带经验一起到手。架构挂钩=registry 信封的 `deps` 字段(plan §1 早留)。**✅ 已落地「能力包捆绑」(用户选 A)**:① echo `utility_foundry`:`_profile`/`mint_utility_agent`/`mint_from_markdown` 加 `deps`;`mint_utility_agents` 加**插件包检测**(agent 在 `<plugin>/agents/` 且有同级 `<plugin>/skills/` → role.deps=[`skill/<id>`...] + 把技能 publish 到 skill_dest);CLI 加 `--skill-dir`。② enterprise:`_asset_from_echo_profile` 读 deps;加 `_PLUGIN_SKILLS_ROOT=sources/plugin-skills` 源(kind=skill);registry `_envelope` 用资产 deps(非硬编码空);**`_load_all` 末尾按 id 去重**(母本 agent-skills 优先于插件重名技能)。**实测**:重铸 195 角色,**11 个插件角色带能力包 deps**(pitch_agent→11技能、statement_auditor→3、meeting_prep→4),发布 36 插件技能→去重后净增 5 个唯一(31 个跟母本重名→母本优先);catalog 426/技能 97/重复 0;**雇佣闭环 HTTP 验证:resolve 角色→它的 deps 技能全可拉取(11/11、3/3、4/4)= 连人带能力包到手**。**关键洞察:能力包 = 对共享技能库的引用(deps),不重复存内容。** **✅ 插件包已落地(用户再选 A,2026-06-30)**:codex 46 插件(`octopus-agent/.octopus/plugins/codex/<svc>/.codex-plugin/plugin.json`)→ 脚本解析(name/displayName/description/category/version + 自带技能名)生成扁平资产 → enterprise `sources/codex-plugins/<id>.json`;catalog 加 `_CODEX_PLUGINS_ROOT` + `_asset_from_plugin_json`(kind=plugin,source=codex,registry 里 `_safety_kind`→code)。插件**自包含**(deps=[],其技能是内含 payload 随插件下载,不作外部 deps)。**插件不走 echo 铸造**(它们是集成包非人设,直接 ingest;echo=角色铸造厂)。**实测**:registry `?type=plugin`=46,`plugin/linear` kind=code;给 `util_pitch_agent` 加 `deps:[...,plugin/canva]` → 雇佣 12 deps(11技能能力包+1插件包)12/12 全 resolve = **"雇一个人才=人+能力包+插件包"**。**⚠️顺手修真 bug**:registry 原按裸 slug 解析,`plugin/browser` 会误中同名 `skill/browser` → 改 `_resolve(asset_id)` 按 **(type,slug) 类型感知**(`catalog.get_asset(slug, kind=)` 加 kind 过滤;`_load_all` 去重改 `(kind,id)` 让不同 type 同 slug 各自保留)。catalog 472(role204/skill97/plugin46/task22/twin3/twin-role100)。**✅ 脱敏记忆经验已落地 = 人力市场三件套全齐(用户再选 A,2026-06-30)**。**用户关键 scope:经验只收「自进化提炼出来的」(已提炼好→天然低 PII),不收原始记忆/日志。** 摸清生态自进化源 = **octopus-agent 的 Variation+Selection 三回路**(`protocols/evolution.md`、`runtime/cli_reflect.py`、`data/evolution_fitness.json`):正向 SkillForge 锻技能/负向 RuleExtractor 规避规则/知识 KG 三元组/MemoryConsolidator 聚类记忆 + 适应度。新 `echo-universe-engine/echo_engine/neural/experience_foundry.py`:`desensitize(text)` 轻 PII 兜底扫描(email/phone/9+位裸数字 ID→⟨...⟩)+ `mint_experience(spec)` 把提炼物(summary/avoidance_rules/lessons/fitness/evolved_skills)铸成 `type=experience` 资产、**入市前必经脱敏闸**(标 `desensitized=true`+`redactions` 计数,"未脱敏不入市")、evolved_skills 作经验自带能力包 deps。enterprise:`_EXPERIENCE_ROOT=sources/experience` + `_asset_from_experience_json`(kind=experience);registry `_DATA_TYPES` 加 experience(kind=data)。**实测**:给 util_pitch_agent 铸经验(故意掺 PII)→ 3 处 PII 全脱敏、原始没了;经验自带 [skill/dcf-model,skill/pitch-deck];挂到角色 deps → **雇 pitch_agent = 人设+能力包11技能+插件包1插件+经验包1经验,递归 resolve 整 bundle(含经验自带技能)15/15 全可拉** = 一次性到手。catalog 473(role204/skill97/plugin46/experience1/task22/twin3/twin-role100)。**人力市场愿景(角色=可雇佣人才,自带能力包+插件包+脱敏经验)registry/服务端层全部端到端打通。** **✅ 活管线已落地(用户再选 C,2026-06-30)**,两半:① **消费端自动反映**:enterprise catalog `_load_all` 从 `@lru_cache` 改成 **mtime 指纹缓存**(`_sources_fingerprint`=sources 全文件 max mtime+文件数;`catalog_version()` 带 2s TTL 降 stat 开销;`reload()` 强制重建);registry `_hash_index` 从 lru_cache 改成**跟随 `catalog_version()` 自动失效重算**。**实测:改一个源资产 → registry 不重启、~2s 内自动反映**(version 内容寻址自动变,双向)。② **生产端一键**:echo CLI 加 `rebuild-registry --publish-dir <enterprise sources 根>`(一条命令重铸 195 工具角色 + 能力包技能 + export 9 白幽灵角色 → 全发布)。**全链路:`rebuild-registry` 生产+发布 → registry 自动反映,无需重启。** **✅ 自动触发已封顶(用户再选 A,2026-06-30)**:echo CLI `rebuild-registry` 加 `--watch [--watch-interval]` 常驻模式 —— 轮询监听 `utility_roles/`+`characters/` 的 mtime 指纹,**变了就自动 `_rebuild()`+发布**。**实测**:起 watch(初始铸 195角色+9人物)→ 改 `utility_roles/design/design-ui-designer.md` 的 frontmatter name → watch 自动检测 fp 变 → 自动重铸 → republish 的 `util_design_ui_designer.json` name 自动变 `WATCHTEST` ✅。**全自动闭环:改角色规格 → echo watch 自动重铸+发布 → registry mtime 自动反映 → 各端拉新版,零人工。**(坑:watch 启动 print 误用 `file=sys.stderr` 但 cli.py 没 import sys→NameError,去掉即可。)**整条「资产/人力市场 + 活管线」服务端全部端到端打通且自治。** **✅ 消费端 client + 公网部署都已落地(用户选「1 2」,2026-06-30)**:**① 消费端"雇佣"client(用户选「独立参考 CLI」,2026-06-30)**:`octopus-enterprise/clients/`(顶层、非 backend/clients)—— 库 `registry_client.py`(`RegistryClient`:catalog/info/download/**install**[任意类型递归]/hire,纯 stdlib urllib/hashlib)+ **CLI `octopus-hub`**(`list/search/info/hire/install/installed/path/rm`,`--base/--cache/--json` 子命令前后均可)。`hire(role)` = `install` 角色 + **递归 resolve deps**(能力包/插件包/经验,含经验自带技能,去重)→ **校验 checksum** → 落 `~/.octopus/assets/<type>/<slug>/<version>/`(版本钉命中即不重下;离线可用)。**参考实现,正式生态消费仍对齐 Codex octopus-runtime 不另造平行 SDK、不嵌产品树**。**实测**:跑公网 registry,search/info/hire(裸 slug 自动补 role/)/install(命中不重下)/installed/path/rm 全绿,雇 util_pitch_agent=13 资产 checksum 全过。文档 `clients/README.md`。(坑:`--json` 原是全局选项放子命令后 argparse 不认 → 用 parent parser + `default=SUPPRESS` 修。)**顺手修真 bug**:`rebuild` 从 bundle 重铸会**剥掉角色的经验/插件 dep**(只留 bundle 自带技能);修法 `utility_foundry.mint_from_markdown` 合并 frontmatter 声明的 `deps:`(逗号分隔,如 `deps: experience/exp_pitch_agent_v1`)+ bundle 技能 dep,保序去重 → 重铸不再剥。pitch-agent.md 已加声明。**② registry 已上公网**:`https://api.octoapk.com/api/v1/registry/`(role204/skill97/plugin46/twin3/exp1)。形态=**极轻独立服务**(`backend/registry_app.py`:只挂 registry 路由,**无 DB/redis/minio**,最小依赖 fastapi+uvicorn)。部署在现有 Lightsail 盒子做**隔离第二服务**(详见 [[octopus-mobile-payment-server]])。**实测端到端**:外网 HTTPS 雇 util_pitch_agent=11技能+1经验=13资产 checksum 全校验、收款 8081 零影响。**✅ 一键发布脚本已落地并加固(用户「补」,2026-06-30)**:`octopus-enterprise/scripts/publish-registry.sh` —— dev → 盒子发布。**先经对抗式 workflow 审查(5 维 26 findings → 22 确认)后重写成蓝绿**:盒子 `sources` 改成**符号链接 → releases/<ts>/**;发布=rsync 到「从当前 release 硬链接做种子的全新 release 目录」(`--delete` 只碰新目录、**线上永不被删**)→ 逐目录校验(6 目录非空 + 计数==dev)→ `mv -T` **原子切换符号链接**(无半同步窗口)→ **真健康门**(盒子 catalog 资产数 + 公网 total 必须一致达标,否则非零退出)。`--rebuild` 先 echo 重铸(重铸后复检本地树挡欠铸);`--apply` 真发,`--rollback` 原子切回上一 release,`--allow-shrink` 放开 `--max-delete=30` 上限;缺省 DRY-RUN。守卫覆盖全 6 目录(agent-skills/codex-plugins/echo-characters/echo-utility/experience/plugin-skills)非空(原只查 3 个→`--delete` 能静默清空盒子,是用户揪出的真洞)。**实测**:缺目录/空目录均被拦;真改一个角色 name → `--apply` 蓝绿发布、公网 version 内容寻址自动变;`--rollback` 原子切回、version 还原;每步公网 473==盒子 473、收款零影响。注:盒子布局已从实目录迁成 release+symlink(详见 [[octopus-mobile-payment-server]])。生产端 `echo rebuild-registry --watch` 仍在 dev 跑。**整条「资产/人力市场 + 活管线 + 公网分发」端到端打通且自治。** 相关 [[octopus-storage-and-echo-hardening]]、[[octopus-ecosystem-and-os-fork]]。剩:②部署到公网;③adapter 喂母本 skills→kind=skill、codex 插件→kind=plugin 填满技能/插件 tab;④客户端拉取(先 mobile:去 `assets/skills/` 打包→HubClient→`filesDir/hub-cache/`→SkillManifest 从缓存加载,零件 SkillManifest/PluginManager 已现成);⑤执行体执行层。消费 SDK 仍照旧决策对齐 Codex `octopus-runtime`、别造平行 SDK。
