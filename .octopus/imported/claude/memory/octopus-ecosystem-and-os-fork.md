---
name: octopus-ecosystem-and-os-fork
description: octopus 四个项目的生态关系、octopus-os fork 的由来与战略决策
metadata: 
  node_type: memory
  type: project
  originSessionId: 1d6d27c3-dc6a-43a7-97bf-65acc5304019
---

/Users/dangbei/Public/octopus/ 下四个项目(2026-06-13 起托管于 GitHub 私有库,账号 dengdenghua:octopus-agent / octopus-os / octopus-enterprise(本地目录 2026-06-13 起也已改名 octopus-enterprise)/ octopus-mobile;gh CLI 已装在 ~/.local/bin 并配好凭证助手):
- **octopus-agent**(母体):通用 Agent OS runtime,工程最成熟(CI 20+ 检查、1612 测试)。仿生命名部分过时:模型路由实际在 `runtime/sensing/model_router/`(含 ollama_router.py),安全层在 `runtime/safety/`(approval/audit/sandboxing/evolution/recovery 等),CODE_WIKI 里的 eyes/mantle/regeneration 是旧名。
- **octopus 企业版**:AI 项目管理 SaaS(FastAPI+React)。2026-06-12 我修复了 CORS/SECRET_KEY 持久化/依赖锁定并补了 CI(3 commits)。它用进程内 import 集成母体(脆弱,建议改服务化)。
- **octopus-mobile**:Android Agent(LLM 经无障碍服务控制手机)。同日轮换了弱密码 keystore(新密码在 local.properties,旧库备份为 release-old-weakpass-backup.keystore)、清理 25 个 PS1 脚本、补 CI+lint 基线(2 commits)。曾有 53 个失败测试,用户已开始修(Robolectric 方案)。
- **octopus-os**(2026-06-13 新建):母体的 fork(rsync 复制,排除 node_modules/.venv/data),目标是面向家庭服务器盒子的 Appliance AI OS。分支 `os-main`,remote `mother` 指向本地母体。**总体方案在 docs/OCTOPUS_OS_PLAN.md**(P1 NAS Docker 应用 → P2 窗口化+技能+索引 → P2.5 HDMI kiosk → P3 Debian 整机镜像)。

关键战略决策(用户已认同):做设备 OS 不做通用 PC OS;Debian 薄 remix 不 fork 上游;存储底座用 OMV/CasaOS 不自研;OS 专属代码放 `appliance/` 顶层目录以减小与母体的合并冲突;删除一律回收站语义;网页内容→Agent 输入是第一攻击面。

**PM 归并决策(2026-06-13,用户认同)**:企业版 = 生态唯一 PM 服务(任务/甘特/里程碑/风险),可独立 SaaS 亦可作 OS/mobile/agent 的可插拔插件。依赖单向无环:客户端→企业版(PM),企业版/客户端→agent(AI),**agent 永不依赖企业版**。关键区分:agent 的 `runtime/company/`(~人类PM,只被 app.py 引用的 bolt-on)该退役;但 `team_rooms`/`team_tasks` 是 Agent 协调(swarm 神经),**保留**。用户选"只剥 company,留 team 协调"。**已做 A+B**(企业版 commit 24e0f5a,OS commit 48102ca):A 契约——`backend/scripts/export_openapi.py` + `docs/openapi.json`(60 端点)+ `docs/PM_INTERFACE.md`;B OS 插件——企业版 compose 的 frontend 标 `sh.octopus.name=项目管理`、基础设施标 hide,部署到 OS 即自动进启动器(OS 侧加契约测试验证)。**已做 C**(OS commit c336932):app.py 的 company 块用 `OCTOPUS_ENABLE_COMPANY` flag 包裹,appliance(OCTOPUS_APPLIANCE=1)默认关、母体/非 appliance 默认开;team_rooms/team_tasks 保留,company 未挂时 team 广播经 getattr 自动跳过同步(无需改 team 代码)。实测 appliance company 路由 0/team 11、非 appliance company 22/team 11(母体不变)。**重要纠正(用户明确)**:要删的是 company(人类 PM:项目/任务/里程碑/甘特),**不是整个"工作"模式 surface**。"工作/Octopus/浏览器"三模式切换必须保留;"工作"模式里的团队/多人协作(team_rooms/team_tasks 的 UI)也保留。我一度把整个工作 surface 删了(母体 089e87b、os 6495f88),母体已修复(b6b07ff:恢复三模式切换+团队/人力池协作 UI,"工作"落地 /workspace/team,只去掉公司 PM 导航项+页+core)。**os 前端也已恢复**(2d8ca16,同母体:三模式+团队协作,落地 /workspace/team,去掉 C 阶段的 useCompanyEnabled 探测;company PM 仍删)。两端工作 surface 现都正确:保留团队/多人,只去公司 PM。

**company(人类 PM)删除进展**:os 前端删(6495f88,删 company surface/页/core-company/company-capability,保留 team);os 后端 flag 默认关(c336932,未删)。**母体 octopus-agent 也彻底删了 company**:前端(089e87b,同 os;WIP 先隔离于 a1b4913,36 个前端 WIP 没碰)+ 后端(54f7734,删 runtime/company + app.py 装配块 + 2 个 company 测试;团队 getattr 自动跳过同步)。母体实测 serve company 路由 0/team 11。保留:task-board 甘特组件(intelligence 在用)、collab/team(Agent 协调)。**os 后端 company 也已删**(3395567):删 runtime/company + flag 块 + 2 测试 + README 的 OCTOPUS_ENABLE_COMPANY 行,与母体对齐(消除将来 merge 冲突)。早先 C 前端尾巴(OS 5a247cb,useCompanyEnabled 探测隐藏)已被 6495f88 彻底删除取代。**结论:company(人类 PM)在母体和 os 的前后端全部删除完毕,PM 完全交企业版;两边都保留 team_rooms/team_tasks(Agent 协调)、task-board 甘特组件(intelligence 用)。验证均为 serve 实测 company 路由 0/team 11。****已做 D②**(企业版 e068649):企业版 agent 集成改服务化——删 `from runtime...` 进程内 import,改 HTTP 调 agent 的 `/v1/chat/completions`(新 `app/services/octopus_client.py`,配置 `OCTOPUS_AGENT_URL`/`OCTOPUS_AGENT_TOKEN`;llm_client/agent_bridge 走网关、失败回退直连)。107 测试(+7 路由)+ 模拟 runtime 缺失仍可导入。**已做 D①(os 侧)**(aca200e):`appliance/pm_skills.py` 给 OS Agent 加三个工具 pm_list_projects/pm_list_tasks/pm_create_task,调企业版 `/api/v1/*`(JWT+租户头);`builtins.register_all` 在 OCTOPUS_PM_URL 配置时 +3 注册(门控,不配母体行为不变);compose/README 加 OCTOPUS_PM_URL/TOKEN/TENANT。9 单测 + register_all 实测 179→182。**已做 D① mobile 侧**(mobile 59556ff):`tool/impl/PmTaskTools.kt` 给 mobile Agent 加 create_pm_task/list_pm_projects 两个工具,OkHttp 调企业版 /api/v1/*,配置经 MMKV(octopus.pm.url/token/tenant),注册进 ToolRegistry.registerCommonTools;4 个 MockWebServer 单测(./gradlew testDebugUnitTest --tests PmTaskToolsTest → BUILD SUCCESSFUL 4/4)。端到端需真机。**D① 两端(os+mobile)完成。待办**:E=SaaS 运营件(租户开通/计费);真机验证(P1 build + 应用窗口化 + 企业版↔agent↔PM 三方联调)。注:企业版有用户的前端测试 WIP(AgentChatPanel.test 等),提交时勿带。

**两条路线已厘清(2026-06-13)**:octopus-agent=寄生路线(透明叠加真实桌面的整理工具,类比腾讯桌面/360桌面助手,桌面助手透明是刻意的);octopus-os=原生路线(桌面即系统,自带壁纸+启动器)。差异在 frontend `src/app/desktop/page.tsx` 收敛到常量 `IS_NATIVE_DESKTOP`(母体 false,OS true)。仓库把 `motion/react` 别名成精简 shim(缺 useTransform 等),OS 的 Dock 邻近缩放故用纯 React+CSS 变量实现。

**octopus-os P1 已完成(2026-06-13,均已推 os-main)**:① 后端 `appliance/app_registry`(Docker 容器→启动器,label 兼容 CasaOS/homepage/unraid;`OCTOPUS_APPLIANCE=1` 门控)+ 前端 Dock 接真实应用;② 桌面视觉:极光壁纸 + Dock 邻近缩放(`frontend/src/appliance/dock.tsx`,纯 React+CSS);③ 原生默认主页(去 opt-in 门/透明叠加/穿透);④ 打包 `deploy/appliance/`(compose 挂 docker.sock + CasaOS x-casaos;Dockerfile+pyproject 纳入 appliance 包);⑤ 单用户认证(`appliance/auth.py` 首启设密码/随机生成+30天会话,复用 runtime local_auth bcrypt+JWT;`/api/appliance/*` 加 JWT 校验 + 公开 `/auth/status`;前端 `appliance/auth.ts`+`login.tsx` 原生登录屏)。appliance 后端 22 测试,前端预览实测全流程截图确认。**P2 进展(2026-06-13,推 os-main 3b8a2f2)**:✅ NAS 文件管理器 + 回收站硬约束已做完——后端 `appliance/files`(manager 纯逻辑+路径安全,router 复用抽出的 `appliance/security.py` JWT 依赖,app.py 按 OCTOPUS_NAS_ROOT 挂载)+ 前端 `file-manager.tsx`(原生玻璃面板:浏览/进文件夹/删除→回收站/恢复/清空),删除一律回收站(.octopus-trash),物理删除仅"清空"一条路径;39 测试 + 预览 UI + 物理磁盘端到端验证。桌面文件夹 Dock 图标原生模式开它、Electron 仍开透明整理抽屉。✅ 窗口管理器(b7862a2):`frontend/src/appliance/app-window.tsx`,应用 iframe 开成 macOS 风格桌面内窗口(拖拽/缩放/聚焦/最小化/关闭,纯 pointer 事件),启动器原生模式开窗口、Electron 走新标签;预览 mock 应用端到端验证六项操作。**P2 剩余**:反向代理剥 X-Frame-Options(窗口化的难点,需真实 NAS 应用,Docker 后做;当前对允许内嵌的应用即刻可用,其余有"新标签打开"兜底)+ 应用技能 SKILL.md + 语义文件索引。**缺口**:本机无 Docker,镜像 `docker build` 未端到端验证(下一步应在真 NAS/Docker 上 `docker compose up -d --build`)。

**P0 插件扩展 API 完成(2026-06-13,「消费而非 fork」生态地基)**:回答"OS 如何跟 agent 拉开差距"——见 octopus-os/docs/OS_DIFFERENTIATION.md(engine vs device,类比 Chrome/ChromeOS;四条差异化轴;fork→依赖迁移路线 P0→P1→P2→P3)。**母体 octopus-agent fedd0c5**:新增 `runtime/platform/extensions.py`——`load_app_extensions`(读 env `OCTOPUS_APP_EXTENSIONS="mod:func"`,在 create_app 末尾以 `(app, AppExtensionContext)` 调用挂自定义路由,context 暴露 identity_store/stack/agent_registry)+ `load_skill_extensions`(读 `OCTOPUS_SKILL_EXTENSIONS`,在 register_all 末尾以 `(registry)` 调用注册技能,返回计数);未配置全 no-op、行为不变,单扩展异常只记日志不拖垮启动。6 测试 + 实测 register_all 179→184、母体 serve 467 路由无回归。**os 24e9bb1**:os 改用扩展点而非 fork——appliance 块从 app.py 抽到 `appliance/extension.py`(`OCTOPUS_APP_EXTENSIONS=appliance.extension`,内部仍 OCTOPUS_APPLIANCE=1 门控),PM 技能改经 `OCTOPUS_SKILL_EXTENSIONS=appliance.pm_skills:register_pm_skills`(register_pm_skills 内部已按 OCTOPUS_PM_URL 门控);app.py 的 create_app 尾部 / builtins 的 register_all 尾部与母体 P0 重新对齐,**消除这两处长期分叉源**(注意:app.py 整体仍有既有 fork 欠债如缺 twin_responder,属 P1 整体去 fork 范畴)。compose 注入两 env;新增 tests/appliance/test_extension.py;51 appliance 测试全过 + os serve 经扩展点挂 appliance 10/auth 3/files(/api/appliance/files/*)路由实测通过。**P1 后端去 fork:已执行并本地验证(2026-06-13,os 60e835e;runbook 0ca8d12)**:os 删继承的 runtime/(~22万行)+tools/(共 2216 文件)、把 agent 当 pinned 依赖装入、appliance 经扩展 API 挂载。**已做**:① pyproject 包改名 octopus-agent→octopus-os(否则与 agent 依赖自冲突)、依赖钉 `octopus-agent[serve,web] @ git+...@fedd0c5`、`packages.find include` 仅 `["appliance*"]`、移除与 agent 重复的 console scripts;② Dockerfile 不再 COPY runtime/tools,改 `--find-links agent-dist/` 本地装 agent wheel + `pip install . --no-deps`(agent 私有仓库免 token);③ 新增 `deploy/appliance/prepare-agent-wheel.sh`(构建前预构建 agent wheel 投喂,uv build/优先);agent-dist/ 入 .gitignore。**本地验证**:os venv `uv pip install ../octopus-agent`(uv 管理、venv 无 pip,须用 `uv pip`/`python -m`)后移走本地 runtime/tools,os serve(480 路由,appliance 经扩展点挂)+ 51 appliance 测试全过、零 import 错误;prepare 脚本实测产出顶层含 runtime/tools 的 wheel。**待验证(本机无 Docker)**:镜像组装需在带 Docker 的 NAS `compose up --build`。**注**:删 runtime/ 同时丢弃了其内的 rsync-冻结 WIP(react_*/realtime_* 等),已先确认这些在母体均已提交落地;os 仓库仍留 421 个继承测试 + agents/skills/prompts/teams 等数据目录(数据目录运行时要,继承测试清理留 P3)。**之前的可行性证明**(保留作背景):目标 os 删继承的 runtime/、把 agent 当 pinned 依赖装入、appliance 经扩展 API 挂载。

**P2 前端去 fork:第一块接缝已落地并可视验证(2026-06-13,os b5573da)**:把 agent 工作台当一个桌面应用开在 os 窗口管理器的窗口里(dogfood 窗口系统),URL 由 `frontend/src/appliance/agent-workspace.ts` 的 `resolveAgentWorkspaceUrl()` 决定:默认同源 `/workspace/realtime/new`,可经 `window.__OCTOPUS_AGENT_WORKSPACE_URL__` 注入指向**外部 agent 服务** → os 不再 fork agent 前端、只在窗口里消费它。这是 P2 接缝:换 URL 即从"自带"切到"外部消费",前端组件无须改。`desktop/page.tsx` Dock 首位加 "Agent 工作台" 项(openWindow 开 iframe 窗口;Electron 寄生模式 `!!window.octopus?.isElectron` 为真才整页回退)。**可视验证**:vite dev(3001,母体前端占着 3000 勿动)+ auth-off 后端(molili.enabled=false 会逼 anthropic planner→需 dummy ANTHROPIC_API_KEY;改 /tmp 副本配置不动 config.example.yaml),点 Dock 项开出 macOS 风窗口、iframe 加载工作台全 UI、URL 仍 /#/desktop 无递归、零 console 错误。注:预览浏览器 UA 含 "Electron" 但 window.octopus 为 undefined,故 isElectronShell 仍 false(按 window.octopus 判定,非 UA)。**用户选定部署模型="同机 agent webui 投喂"**(os 镜像构建并打包 agent 前端、后端子路径 serve;非独立容器)。**问题 A(外部 agent UI 由谁 serve)已解决并验证(os cc6e2de+70ec7ce)**:① 后端 `appliance/agent_ui.py`——`OCTOPUS_AGENT_WEBUI_DIST` 指向独立 webui 时挂 `/agent-ui/`(StaticFiles html=True),公开 `/api/appliance/config` 暴露 `agent_workspace_url`(/agent-ui/#/workspace/realtime/new),未投喂回 null;前端 `loadAgentWorkspaceConfig()` 桌面挂载时拉取设全局 → 窗口加载外部 webui 否则同源回退。② 部署 glue:`deploy/appliance/prepare-agent-webui.sh` 以 `base=/agent-ui/` 构建 agent 前端(`pnpm exec vite build --base=/agent-ui/`,实测 26s 成功、assets 引用 /agent-ui/);Dockerfile COPY agent-webui→/app/agent-webui+设 env;`.dockerignore` 加 `!deploy/appliance/agent-dist`、`!deploy/appliance/agent-webui` 例外(**并修复 P1 隐患**:deploy/ 被整体 dockerignore 会让 wheel/webui 的 COPY 构建时失败)。实测 os serve 真实 agent webui:config 回 URL、/agent-ui/ 真实 Octopus index、assets 200。**verify 经验**:appliance auth 门 + agent auth(molili.enabled=true)双重门挡预览;`uv pip install .`(octopus-os)会在 site-packages 留 appliance 副本 shadow 掉 cwd 版(服务进程加载陈旧版,表现为部分路由缺失)——本地从 cwd 跑勿装 octopus-os 包;`uv pip uninstall octopus-os` 会误删 site-packages/runtime,需 `uv pip install ../octopus-agent --reinstall` 恢复。**P2 step2 部分完成(os 64aa315)**:泛化接缝——后端 config 增 `agent_ui_base`(/agent-ui/ 或 null),前端 `resolveAgentAppUrl(route)`(有 base→`${base}#${route}` 即 /agent-ui/#/...,否则同源 route)+ `__OCTOPUS_AGENT_UI_BASE__` 全局。`DesktopApp.windowed` 标记 + openApp 对 windowed 应用开窗口;**无歧义的** agent 应用(工作台 realtime、终端日志 observability)已转窗口化;消解了单独的 "Agent 工作台" dock 项(由"工作台"自身窗口化替代)。预览实测:点"工作台"开窗口、iframe /workspace/realtime/new(同源回退)、无 console 错误。**待用户产品决策**:3 个 dock 应用与 os 桌面重叠——"AI 浏览器"(/browser,os 设备浏览器?)、"本地文件"(/workspace/knowledge vs os 的 NAS 文件管理器)、"本地应用"(/workspace/store vs os 的 Docker 启动器);是 os 设备功能还是 agent 功能,决定转窗口/重指 os 原生/删除。**P2 剩余(destructive)**:上述决策后 → 甄别 core/(共享/浏览器依赖留,纯工作台删)→ 删 `components/workspace`(314)等 + 摘 router。每步 pnpm build+冒烟;镜像组装待 NAS。**前提已具备**:agent 的 pyproject 已声明 `include=["runtime*","tools*"]` v0.2.0 → pip 可装、import 路径不变。**已做两项去风险证明**:① os 的 `appliance/` 对 runtime.* 的全部依赖只有 5 个模块(local_auth(+.config)/suckers.registry(+.testing)/safety.auth.identity),且都在母体**已提交**树(非 WIP)→ 干净消费者;② 黄金启动证明——用纯母体 agent(无 appliance 代码)+ 仅把 os 的 appliance/ 经 OCTOPUS_APP_EXTENSIONS 挂上、PYTHONPATH **完全不含 os runtime/**,实测 appliance 启动器4+认证3+文件6 路由全挂载。**→ os=appliance/+agent依赖、零 forked runtime 后端可行。切换步骤/回滚见 docs/P1_BACKEND_DEFORK_RUNBOOK.md。阻碍点**:os 的 runtime/ 里有 fork 时 rsync 带入的母体「就地冻结」WIP(react_*/realtime_* 拆分等 8改+12新,未提交),`rm -rf runtime/` 会丢——切换前须先确认这批 WIP 已在母体落地/可弃(待用户定夺)。P1 只覆盖后端;前端去 fork 是 P2(os 窗口管理器把 agent 工作台开成窗口)。

**os 仓库 WIP 注意(2026-06-13 新发现)**:octopus-os 工作树也有大量他人/进行中改动(runtime/core/cerebrum/react_* 拆分、runtime/sensing/gateway/realtime_* 拆分、workspace-sidebar.tsx、多个 test),**提交时只显式 `git add` 自己的文件,绝不 `git add .`/不 stash 全树**(同母体约束 [[octopus-agent-ci-baseline-state]])。

**原生 shell / 真正设备 OS(2026-06-13,用户拍板走 A 路线;os 09a5713)**:用户要做"真正的 agent OS——开机即原生桌面,不是浏览器里的网页桌面"。**认清三种形态**:A=Electron 当会话 shell(复用 React 桌面,主进程拿系统手);B=自研 Wayland 合成器(月级系统工程,能合成真实原生程序窗口);C=网页 UI 远程浏览器访问、本机 headless(**飞牛 fnOS/绿联 UGOS/群晖/QNAP/OMV/CasaOS 都是 C**,= os 现状)。**用户选 A**(NAS 应用几乎全是 Docker 网页服务,A 体验无可感差距、工作量周级 vs B 月级;真要合成原生程序再上 B,React 桌面届时平移成 layer-shell)。**能力确认**:A 能管所有本地应用(扫 `/usr/share/applications` 的 .desktop 枚举+spawn 启动)、文件(主进程直连 Node fs)、注册图标显示(.desktop Icon + freedesktop 图标主题);**唯一边界**:原生 GUI 程序窗口没法浮动合成进桌面(NAS 伪需求,可全屏切)。**已做第一片(本机可验证)**:`frontend/electron/system-shell.cjs`(系统手:.desktop 解析/Exec 字段码清洗/图标主题解析/listApplications/launchApplication/IPC)+ `system-shell.test.cjs`(纯函数 5 测试过、node 直跑;mac 上 listApplications 返回 [])+ main.cjs(require+注册 IPC+`OCTOPUS_NATIVE_SHELL=1` 会话窗口 fullscreen/frame:false/kiosk)+ preload(`window.octopus.apps.{list,launch}`)+ electron.d.ts(NativeApp 类型)。**方案/roadmap 见 docs/NATIVE_SHELL_PLAN.md**。**②整机套件已做(os 41e4115)**:`deploy/native-shell/`——`setup-native-shell.sh`(一键把 Debian 装成设备:cage/plymouth/node/docker+设备用户+同步源码构建前端+装 systemd 会话 unit+默认图形)、`octopus-shell.service`(cage -- launch.sh 会话,PAMName=login+TTYPath 给 cage 弄 seat,退出即重启)、`octopus-shell-launch.sh`(cage 托 `OCTOPUS_NATIVE_SHELL=1 electron` + Wayland 标志)、README(VM 验证清单+卡点:seat/GPU/Wayland/sandbox/splash);main.cjs 加 NATIVE_SHELL 从源码也加载构建好的 dist。v1 从源码跑(便于 VM 验证);硬化=electron-builder AppImage / debootstrap .img。bash -n+语法校验过,逻辑待 VM/真机验证。**③ Dock 接真实应用已做(os 5b82381)**:`system-shell.cjs` 每应用带 iconDataUrl(图标读成 data URL,避 file:// 沙箱,>256KB/非png-svg→null);`appliance/apps-native.ts` 的 `useNativeApps()`(window.octopus.apps.list();非 Electron→空,Dock 不变);`desktop/page.tsx` Dock 在 Docker 应用后插原生应用段(真实图标+点击 launch)。tsc 全项目 0 错误;真实图标/启动需 Linux 设备。**A 路线 ①系统手 ②整机套件 ③Dock 接应用 均已做**。**待做 ④硬化**(electron-builder AppImage 免 node / debootstrap .img 出厂镜像)+ VM/真机跑通整链。Electron 已有寄生路线脚手架(main.cjs 677行/preload),NATIVE_SHELL 未开即原寄生窗口模式、不受影响。**存储**:整机路线集成 OMV 存储栈(消费不自研),把 OMV 管理界面用窗口管理器开成桌面应用;应用数据约定 bind-mount 到共享 /data/nas 树,Agent+文件管理器才好统一管(Docker named volume 隔离反而是阻碍)。

fork 基线注意:带入了母体 25 个未提交 WIP 文件,就地冻结未 merge。

**2026-07-01 全生态深度扫描发现**:octopus-os 的 pyproject 依然 pin 在 `octopus-agent@fedd0c5`,而母体现在已经领先该 commit 约 581 个提交(含近期一整批安全加固"harden xxx"提交)——**依赖严重过期,os 若要上生产必须先重新同步 pin**。os 仓库仍挂着 416 个母体遗留测试文件(P3 清理范畴,尚未做);工作区处于"去 fork 大扫除"中间态,大量未提交改动。同时发现生态级模式:agent(领先 origin 56 commit)、enterprise(+6)、echo-universe-engine(+3)三个仓库都有未推送的本地提交,属于高强度独立开发的普遍副作用,非单一项目问题,建议定期集中 push。
