apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: octopus-agent

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: octopus-agent

# Implementation note.
# images:
#   - name: ghcr.io/octopus-agent/octopus-agent
#     newTag: "v0.2.0"
