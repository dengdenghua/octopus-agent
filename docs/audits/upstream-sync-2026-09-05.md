# Production-hardening 同步验收

2026-09-05，本地 `codex/unified-execution-runtime` 合并
`origin/codex/production-hardening` 的 `51b99ab904fb5d758cf8ea9165b218c915bbe3dc`。
相对原有基线 `d61ae74b`，引入 40 个远端提交。

## 恢复点

- 合并前本地代码：`a4f1c931`，分支 `backup/pre-production-hardening-20260905`。
- 运行数据库备份：`.codex-run/backups/pre-production-hardening-20260905/`。
- 保留当前本地配置、账号凭据和项目数据；无远端推送。

## 整合决策

- 处理 23 个冲突文件，保留上游聊天恢复、团队策略、权限与插件生命周期。
- 保留独立引擎选择、持久执行凭据、产物交接、隔离工作树和项目管理插件化。
- 通用/设计模式根据任务意图选择自动引擎，目录和菜单标签不单独决定引擎；兼容旧模式参数。
- 团队协调与补救执行经过现有统一执行入口，成员可继续使用各自执行后端。
- 并行完成策略的取消信号与父任务停止信号关联，保留线程上下文传播。
- 工作台继续分进程构建，并从受版本控制的清单读取元数据，构建不再覆盖权限和版本说明。
- 修复 Windows 协议测试的 Python 命令选择；插件并发测试使用空缓存，按事件等待生命周期边界，避免依赖本机插件扫描在一秒内完成。
- 后台另一个任务正在进行的记忆来源改动留在工作区，不纳入此次合并提交。

## 验证

- 核心执行、网关、交接、取消与委派：382 项通过。
- 插件、目录权限、团队恢复、上下文、执行记录与 OpenAPI：263 项通过（含最后单独复验的插件并发用例）。
- 前端：69 个测试文件，1,017 项通过（含修复后的协议生成检查）。
- TypeScript 检查、主应用生产构建和七个工作台构建通过。
- 七个已安装工作台使用合并后的代码重新安装；本地后端 8000、前端 3000 已启动。
- 实际 HTTP 检查：健康、目录根、插件目录均返回 200；Browser、Documents、Spreadsheets 仍启用，OpenCode Zen 保持原先未启用状态。
- 浏览器检查：新任务、通用/设计菜单、独立引擎入口、项目管理插件及创建表单、设计工作台均可加载。

验收使用受控执行器和本地持久化测试；没有发送实际模型任务，也不代表所有第三方账号或远程渠道均已配置。
