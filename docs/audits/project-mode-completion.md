# 项目模式完成性核对

目标：项目模式完整打磨到位。状态：进行中，不能用局部单测替代完整实跑。

## 必须验证的用户流程

| 要求 | 当前证据 | 待完成 |
| --- | --- | --- |
| 无成员时主角先立项 | OpenCode / big-pickle 已真实生成方案与项目 | 新鲜案例复测全链路 |
| 范围、预算、岗位、阶段方案 | initiation 结构化方案与实跑记录 | 检查缺失信息、修订方案体验 |
| 人工可见审批 | 前端 Dialog；组件测试验证没有自动批准 | 浏览器内真人交互到结果；脚本批准不算 |
| 从已有角色、HUB、新建匹配 | recruitment 单元测试与准备角色接口 | HUB 真实候选添加、失败重试、拒绝不加入 |
| 分阶段加入与执行 | phase 授权与人员范围测试 | 多阶段实际交付，后阶段只在获批后加入 |
| 工作目录与权限一致 | turn input 提前分配受管理目录；安全测试 | 修复后实际子任务产物 |
| 预算可解释、可恢复 | 持久化回报及缺失暂停 | 旧失败案例恢复、费用完整性与界面一致 |
| 交付可验收 | 验收指纹与 owner 检查测试 | 真实产物、用户验收、重验和最终关闭 |
| 项目面板清晰 | 合并首页入口、指令高亮；前端构建 | 阻塞状态不能同时显示正常；统一进度 |
| Skill 可复用 | 三份流程 Skill 已注册 | 成功案例沉淀；不得宣称自动学习完成 |

## 本轮发现并修复

- 查明 OpenCode 完成费用此前未回传项目，且回执只带末条消息费用。新增按当前 user_message_id 汇总全部助手消息显式费用、去重 final/history，并由角色执行回传项目 usage callback；不再同时累计通用治理快照。缺少或非法费用仍为未知，显式 0 可接受。67 项独立相关测试通过（backend 42 + hooks 17 + cost 8）。仅覆盖正常完成，历史中断缺失费用的对账恢复仍待完成；本轮未部署。
- P-e7f8826c 恢复前确认费用缺失暂停，未绕过预算继续。新增 project.usage_reported／project.usage_missing 事件，关联任务与阶段；未知费用为 null，PM 提示待核对任务。21 项相关测试通过。旧记录未关联任务，不能凭此补写为零；对账审批入口仍未完成，文档写入测试授权未实施。
- 停止信号在执行结果返回后／QA 返回后再次检查：保留原产物为 pending review，不再继续验收或派发任务；取消异常释放当前 claim，执行次数保留真实记录。引擎回归 89 项通过，worker 3 项通过（首次错误地期待计数归零，按持久化单调计数修正后定向复测）。仍未部署，旧测试运行已从 T3 转到 T2，取消接受并非执行结束。
- 项目同步 worker 缺少将 realtime interrupt 连接到子任务 cancellation token 的桥接；新增 run_project_worker 保留上下文并监视停止信号，执行器在下一任务和下一 tick 前检查取消。37 项独立相关测试通过（worker 3 + governance 17 + hooks 17）。旧服务 T3 仍 running，尚未部署及真实中断复测。
- 立项新增 max_tasks_per_phase 结构化数量上限，并进入审批文本和阶段 spec；执行器在保存与运行拆解任务前拒绝超量结果。旧案例没有此字段，不自动篡改历史方案。修复尚未部署。
- P-09b1c0c8 的观察脚本 240 秒超时退出不代表后端停止：实时 API 仍显示 T3 running，日志明确 server-side continues。取消传递待修复，不重启覆盖现场。后续脚本观察超时会继续监听。
- 阶段审批指纹覆盖立项约束、验收标准、依赖与排期；范围变化会使旧审批失效，状态／任务进度变化不影响审批。审批预览同步显示标准、期限与立项约束；37 项相关测试通过。历史较窄指纹需重新审批，不自动迁移授权。
- HUB 检索数量、检索失败和首阶段入组结果已在对话中显示；立项审批等待延长至 10 分钟。27 项相关测试通过，仍不等于真人已完成弹窗审批。
- 质量检查服务故障后保留原产物，只重试检查；耗尽重试后恢复项目也复用原产物，真正质量不合格仍返工。PM 风险说明区分检查故障与交付内容；已补无重复执行的恢复测试。
- 执行提示补上立项约束、任务标准及已完成任务／前置里程碑产物。
- 质量检查异常或返回非布尔判定时不再默认通过；保留为未完成检查。
- 预算检查区分 usage_missing 与 limit_reached；PM 将暂停阶段标为 blocked，不再推荐执行任务。40 项相关测试通过；运行中接口已确认 P-28586f9d 显示费用缺失和 blocked，未清除历史记录。
- 预算恢复审批及实际费用对账仍未完成，不能通过提高预算假装解决缺失回报。

## 已知未闭环案例

- 已部署正常完成费用回传（PID 9452，健康检查通过）。新交付案例 P-90eadb58／thread f24c1a1867e846e9af8d94d5ceb5af23 已真实立项，首阶段 T1 running。脚本 session 85505 持续观察，CASE_DOCUMENTS=1 仅批准该案例 output/final 直属 Markdown 文档写入（预览脱敏路径与指定目录精确匹配），不批准 Shell／新增角色。尚未确认文件和费用回执，不重复发起。角色费用回传新增 3 个测试，roles 全部 37 项通过。
- P-e7f8826c（thread 0bc541e49a014c73ad1589956363ecef）：真实 big-pickle 方案已包含 max_tasks_per_phase=1，首阶段仅 T1。写文件审批真实发出（测试脚本拒绝，未完成交付）。对 trn_00ab19047efd43cc 发送停止后收到 turn/completed status=interrupted，API 确认 T1=pending、attempts=1，没有后续任务，完成真实取消验证；恢复与实际文档落盘仍待完成。
- 旧服务在已接受停止后仍从 T2 派发到 T1，2026-09-11 18:14 明确终止有缺陷旧服务并加载修复（新 PID 40268，健康检查通过），不是因为观察超时重启。新案例 thread 0bc541e49a014c73ad1589956363ecef 的真实立项正在进行，观察脚本 session 95196（.codex-run/project-case-cancel-fixed），不得重复启动。
- 旧 P-09b1c0c8 已返回实际文本，说明工作目录权限边界已跨过；写文件审批被测试脚本拒绝，未能落盘，不构成完整交付证据。后续实跑需单独处理文档写入审批，不能拿脚本拒绝导致的未交付作为引擎成功。
- P-09b1c0c8（thread ba052f9d10344e6d8ffc31fa7c6dafc9）：真实 OpenCode/big-pickle 完成立项；首阶段违反单任务约束拆成三项，依赖静默丢失导致修订任务先执行。已请求中止 trn_77655007bdc647ff，服务端返回 interrupted:true，仍需确认完全退出。修复依赖 ID 解析和失败时禁止替代任务，17 项 hook 测试通过；任务数量硬约束未完成，新改动未重启加载，避免打断尚未确认退出的执行。
- P-d4f8dde3：早期目录初始化失败，保留失败与预算暂停证据。
- P-28586f9d：早期父子工作区权限不一致，保留失败与预算暂停证据。
- 两个案例均不构成完整成功案例，不能标记目标完成。

继续顺序：核验预算与失败恢复 → 新鲜真实执行及产物 → 人工审批和 HUB 招募交互 → 阶段验收与结束 → 完成性逐项复核。

## HUB 场景入口核验（2026-09-11）

实际浏览器发现部门场景只是普通对话草稿，预先写入协作成员，未走项目立项。现改为 /project run 草稿，仅主角参与，岗位作为建议，缺岗不阻止立项；显示“先立项 · 审批后组队”。14 项场景、审批和角色准备测试通过，Vite build 成功，4173 实际页面已验证新入口。

浏览器当前真实立项 thread tjHpqdcg5AsCcyAffRgEk6，big-pickle，页面已显示核对 136 个已有角色、60 个 HUB 候选。不得脚本或代理代替用户批准。此前脚本自动接受的审批仅为接口测试，不能算人工审批成功。

真实页面已出现“立项与组建团队 · 需要审批”模态框，显示 Eve 已有角色与 HUB 待添加角色、职责和参与阶段、完整方案、批准/拒绝。代理未点击批准。thread tjHpqdcg5AsCcyAffRgEk6，浏览器 tab 4 保留给用户。方案候选仍错误保留企业专属“鹏城信息AI专家”，虽已在补充需求中排除；候选匹配/约束校验仍未完成，不应把该方案视为质量验收通过。立项系统提示已增加非关键偏好不阻塞规则，12 项立项测试通过；后端 PID 42200 健康，前端构建成功。完整目标保持未完成。

## 招募目录统一与真实审批复验（2026-09-11）

根因：HUB 页面使用精选目录，Project OS 却读取旧全量目录并使用旧名字。现以 runtime/projectos/role_catalog.json 为共用来源，前端直接导入，后端仅选择对应单人角色；金融与团队目录原样保留，不删除已添加角色。包数据显式包含 JSON。后台 18 项、前端 7 项测试通过，ruff 通过，Vite build 成功。

已在同一浏览器演示会话 tjHpqdcg5AsCcyAffRgEk6 退回错误方案；后端 PID 51060 加载修复后，真实页面显示 58 个 HUB 候选，新审批选择 Eve 产品经理 + HUB 内容创作专家（阶段2），不再是企业专属角色。弹窗包含范围、两阶段、每阶段最多1执行任务、外部0美元、AI执行上限2美元及验收标准。审批仍待用户本人操作，代理未批准；不能宣称已实际入组或完成项目。

## 审批等待期间的入组与费用校验（2026-09-11）

已补充 AgentRegistry 的 HUB 来源绑定：安装接口将目录ID绑定到实际加载角色版本，立项审批回传ID必须匹配该绑定；其他已存在角色、热替换后的角色不能冒充获批候选。21项招募与立项测试、26项云端目录和接口权限测试通过。

查到实际团队分支 cowork_bridge 未转发模型费用回执（仅普通子任务分支已有转发），已补充每成员独立回执回调；实际回执不被通用汇总覆盖，一位成员缺回执也不会因另一位已上报而被忽略。21项团队测试通过，含两成员回执和单成员缺失覆盖，ruff通过。

上述本轮后台改动尚未部署：浏览器同一演示线程仍显示待用户审批，不重启破坏审批。HUB实际安装、入组、后续阶段与完整验收尚待实际验证；当前目标未完成。

## 费用回执恢复与审批超时核验（2026-09-11）

真实浏览器 tjHpqdcg5AsCcyAffRgEk6 已显示“本次立项审批未完成，方案已保留，未添加成员或启动项目。”当前不再有可批准的弹窗，不能继续告诉用户它正在等待。发现前端没有保留方案的重新提交审批入口；仍需补齐此交互，避免用户回来后无法继续。

费用缺失改为按执行 root_id 持久记录；对应提供方累计费用回执补齐后仅解除对应缺失，其他成员与无编号旧记录仍暂停。重复回执保持最大累计值，补齐后仍检查预算上限。新增费用表随项目删除清理，并拒绝已删除项目的迟到回执。立项提示统一为全项目全部成员累计 AI 预算，避免写成单次调用额度。56项治理/团队/招募测试先行通过，扩大验证正在运行。上述源码尚未部署，实际费用恢复仍需真实提供方回执核验，不构成全目标完成。
补充验证：治理、立项和 Project OS 核心共128项测试全部通过（64.81秒）；ruff检查通过。尚未部署。

## 已保留方案的审批恢复（2026-09-11 19:02）

新增对话底部持久方案卡片，终止/超时后可“重新提交审批”；/project review <id> 验证原方案编号、状态与主角身份，复用原方案并重新校验当前候选，不调用模型改写。旧编号不能覆盖新方案；重新打开不等于批准。26项立项/招募测试、2项前端恢复入口测试通过，ruff通过，Vite构建成功。

后端已更新至PID24708，健康检查通过，包含之前HUB来源绑定、团队费用回调和费用缺失恢复。重新登录演示页后，真实浏览器看到保留方案卡片，点击“重新提交审批”后成功重开原方案（Eve + HUB内容创作专家，阶段2）。用户尚未批准，代理未批准。tab4/thread tjHpqdcg5AsCcyAffRgEk6 保留可审阅。原方案风险文案仍含旧的“单次2美元”错误，复用逻辑未静默改写原文；新的生成提示已统一累计口径，旧方案纠正仍待处理。实际HUB安装、入组、执行与验收仍未验证完成。

## 旧审批并发保护与预算展示（2026-09-11 19:05）

发现旧审批超时保存会覆盖更新后的方案。立项保存现使用 ThreadStore.update_state_if_unchanged 原子写入，并校验上次方案版本；与消息等无关状态冲突可重试，方案已变更则拒绝旧结果。覆盖模型规划结束、审批超时、批准写入之间的新方案竞争测试，旧操作不得入组。真实持久存储已有CAS接口被复用。

方案render顶部增加实际ai_budget_usd累计上限（包括零与未设置），明确全部阶段和成员累计而不是单次调用，旧模型预算文字不再作为唯一额度展示。32项后台立项/招募测试、13项前端审批/准备角色测试通过，ruff通过。新增前端测试证明准备中审批过期会停止后续角色、不会返回批准载荷；已准备角色保留，不自动入组。

本轮后台修改尚未重启部署，以保留当前用户审批会话。实际人工批准、HUB入组、阶段交付和验收仍待验证，不能将测试视为人工审批完成。

## 阶段验收真实性校验（2026-09-11）

验收入口此前仅检查done状态，允许无输出任务申请验收；验收指纹也未覆盖任务目标和任务标准。现拒绝None、空白文本、空集合及仅含空内容的嵌套输出；delivery_accepted同样拒绝。指纹增加阶段名称/目标/立项约束、任务目标及任务标准，变更后旧验收失效。弹窗预览列出任务验收标准与对应输出。

41项验收与阶段治理测试通过，ruff通过。测试首次写入空输出未生效，是存储默认保护终态不可覆盖；测试改为显式allow_terminal_rewrite模拟合法修订，没有修改终态保护规则。当前源码改动未部署，保留用户审批；全目标尚待真实HUB组队、执行和用户验收。

界面仍有待改进点：Project OS下一步的owner_acceptance条目目前统一跳到任务列表，尚无直接打开验收的按钮。后续应接入明确的/project accept阶段命令，避免用户手工复制编号。

## 项目面板直接验收入口（2026-09-11 19:13）

owner_acceptance下一步条目改为“审阅交付物并验收”，通过页面受保护的发送回调派发/project accept精确阶段ID；会话忙时禁用，校验阶段属于当前列表。不会调用run或直接批准。22项工作台测试全部通过，含新入口/忙状态；旧测试按已合并的菜单结构更新（不再期待不存在的项目tab）。Vite构建成功。

真实浏览器新tab5打开历史交付案例thread f24c1a1867e846e9af8d94d5ceb5af23，面板出现验收按钮；点击后实际显示“阶段成果验收·需要审批”，包含任务标准与交付文件摘要。代理未批准。可见源文件为output/final/产品定位与发布简报.md。未能据此宣称人工验收通过。

现场新发现：该项目仅1个已拆解事项完成、0/2里程碑完成、第二阶段待执行，整体进度却显示100%。下一轮需修正未拆解阶段被排除的分母问题，并明确待验收状态。验收弹窗通用说明也仍误称批准后会加人/推进，应按验收动作单独说明“不自动启动下一阶段”。

## 实际进度口径修复（2026-09-11 19:17）

PM overall_progress改为全部已规划阶段交付比例的等权平均，未拆解阶段也计入，新增progress_basis与awaiting_acceptance_count。页面明确为“交付进度”，单列里程碑完成数及待验收提示，不把任务完成率当项目完成。验收与预算弹窗改为各自动作说明，验收不自动启动下一阶段。

31项PM/验收测试、30项前端工作台/审批测试通过，ruff通过，Vite build成功。已核验两个演示审批均结束后更新后端PID42952并通过健康检查（包含前轮CAS旧审批保护与验收输出/标准校验）。真实浏览器tab5/thread f24c1a1867e846e9af8d94d5ceb5af23显示交付进度50%、1/1事项完成、0/2里程碑达成，并明确“交付待你验收”。未批准验收或启动下一阶段。

## 工作台执行按钮接入审批（2026-09-11 19:22）

发现工作台执行按钮直接POST普通项目接口，未走实时对话的阶段审批。run/tick动作现在携带/project run与/project tick，聊天工作台优先走受保护的发送回调（恢复执行已有命令也复用），无对话通道的独立页面仍保留REST回退。tick解析为max_ticks=1，并要求已有项目；仍经过同一阶段授权逻辑。

45项后端立项/团队测试完整复跑通过（首次并发屏障测试5秒超时，独立及完整复跑均通过），23项前端工作台测试通过，ruff和Vite build通过。已部署PID19560，健康通过。实际tab5点击“推进一步”，对话收到/project tick、ticks1，明确等待阶段验收，仍为50%交付、0/2里程碑；未进入下一阶段，没有代理批准。

完整目标仍未达到：HUB角色实际获批入组、后续执行、人工验收与最终结项未发生；当前动作验证不替代这些证据。
