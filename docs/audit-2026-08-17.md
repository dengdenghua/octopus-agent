# 项目综合审计 — 2026-08-17

**审计范围**: 2026-08-16 00:11 → 2026-08-17 00:11(46 个提交 / 294 个文件)
**审计方式**: git 提交历史 + 关键提交 diff 抽查 + 关联测试实测

---

## 1. 提交健康度

- 类型分布:**26 fix / 8 feat / 4 test / 3 refactor / 1 perf / 1 docs / 1 style**。
  修复主导,无 wip / revert / 杂烩提交。
- 全部经过 commitlint(72 字符规范),Co-Authored-By 协作署名规范。
- 工作区 **0 个未提交文件**,仓库状态干净。
- 测试规模 **12,137 个**(+94);抽查的关键功能测试 **85 个全过**。

## 2. 核心进展:编排层升级

这两天主题聚焦——多代理编排从「并行扇出」进化到「依赖图 + 隔离 + 缓存」:

1. **`call_agent_graph` 声明式 DAG**(`fb9d9206`)
   - 节点带 `depends_on`,服务端解析 `{node.output.field}` 引用,上游文本
     不进 lead 上下文;「菱形」依赖不再需要手工往返。
   - **fails-closed**:环 / 未知引用显式拒绝,不会静默跑 acyclic 前缀。
   - 复用 `graph_runtime._topo_layers` / `_lookup`,非新造轮子。
2. **per-spawn worktree 隔离**(`4ffb3b3c`)
   - 每个 spawn 独立工作区(trusted flag),无自动合并,diff 返回评审。
   - 测试 **mutation-checked**:强制走非隔离路径挂 4 个测试,断言绑定行为。
3. **内容哈希 resume + replay 未变节点**(`074d09d3`)
   - `delegation_result_cache.py`(175 行)+ 268 行测试;空拒绝守卫
     mutation-checked。

## 3. 安全整改成体系(`df4cb853`)

| 编号 | 内容 |
|---|---|
| S-01 | 前端 `rehype-sanitize` XSS 防护(7 个前端测试) |
| S-03 / A-02 | 敏感配置清理 + `.gitignore` 防泄漏 |
| T4 | job registry watchdog 超时强制(188 行测试,4 场景) |
| P-01 | scheduler `max_workers` 默认 1→2 |
| P-02 | `journal_max_bytes` 自动轮转配置 |

## 4. 质量信号

- **测试先行 + mutation-checked**:新功能全部带专用测试且做变异验证。
- **向后兼容 rename**:`codex_mode → workflow_mode`(62 文件/1851 行),
  保留 `/codex` 命令与 grant 逻辑兼容。
- **协作模式**:多提交 Co-Authored-By Claude,人机协作进入生产节奏。

## 5. 风险与观察

1. **rename 影响面大**:建议跑全量回归,确认无残留 `codex_mode` 引用。
2. **覆盖率未实测**:`coverage.json` 清理后未重跑,「76%」为 8-14 旧数据。
3. **剩余差距未动**:ACP / e2b / credential references / quiet-wakeup
   独立调度器依旧空缺;编排变强了,生态互操作仍是短板。
4. S-03 删除操作未见于此提交,建议核对历史中的 JWT 残留。

## 6. 综合评级

**工程健康度:A−(90/100)**,较 8-16 提升:

- 修复主导(26/46)是在还债而非堆债;安全整改、mutation-checked、
  fails-closed 都是成熟团队信号。
- `call_agent_graph` 的依赖图 + worktree 隔离 + 内容哈希 resume 是
  DSH workflow(仅 parallel/pipeline)的**超集**,构成新护城河维度。
- 扣分点:覆盖率未实测、生态互操作三项未动。

**对比参考**:能力面 Octopus 89→91,工程成熟度 82→85(详见
`docs/octopus-vs-dsh-full-comparison.md`)。
