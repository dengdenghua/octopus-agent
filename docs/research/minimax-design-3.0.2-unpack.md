# MiniMax Design 3.0.2 解包与 Octopus 迁移结论

日期：2026-08-23  
样本：`/Applications/MiniMax Design.app`（macOS arm64）

> 本文只记录可验证的产品结构与工程模式。Octopus 复用自身设计系统、数据模型和执行基座，不复制 MiniMax 的品牌资产或专有代码。

## 1. 产品不是“一个画板”

MiniMax Design 的核心是一个以画布为主视图的创作操作系统：

1. **画布层**：自由画布与工作流双模式，共享节点、资产与视口。
2. **能力层**：Agent、Skill、Plugin、媒体模型与 ComfyUI 工作流均可成为画布能力。
3. **资产层**：图片、视频、音频、文本、文件、表格等均有稳定资产身份和依赖关系。
4. **执行层**：节点关系转成可执行任务，运行结果回写为新资产和新节点。
5. **工作区层**：画布、聊天、文件、版本、插件状态都绑定到 workspace，而不是散落在单次会话里。

## 2. 解包证据

### 应用构成

- `app.asar`：约 43 MB，Electron 主进程与 React 渲染器。
- `gateway`：约 50 MB，本地 API、SQLite 和资产服务。
- `opencode`：约 138 MB，Agent 执行基座。
- `bundled-plugins`：约 43 MB，内置网页插件。
- `project-templates`：约 80 MB，工作区模板。
- `ffmpeg`：媒体处理运行时。

### 无限画布

渲染器主包使用 React Flow，关键结构包括：

- `CanvasMode.Freeform` / `CanvasMode.Workflow`
- 节点类型：Image、Video、Audio、Text、File、Placeholder、Table、Group、Sticker
- 可视区域裁剪：`onlyRenderVisibleElements`
- 背景：dots / grid / solid
- MiniMap、边隐藏、框选、手型拖拽、滚轮缩放、节点连线
- 快捷键：N 添加、V 选择、H 手型、C 评论、M 小地图、F1 帮助
- 自动整理：网格、横向、纵向，并支持按连接或媒体类型排序
- 自由模式和工作流模式分别保存节点位置

### 持久化与防护

- 画布 API：`/api/canvas`、`/api/canvas/add-node`、`/api/canvas/search`
- 通过 revision header 处理并发保存。
- 有破坏性保存和非法保存拒绝码，避免 Agent 错误覆盖整张画布。
- 资产关系单独建表，支持输入顺序、操作类型、父子来源和软删除恢复。
- 文本文档采用内容寻址版本库，版本属于资产而不是节点。

### 插件与技能

- Skill API 同时覆盖运行时、本地、市场、安装、卸载、白名单和 fork。
- Plugin manifest 包含多语言名称、描述、标签、入口、显示模式、尺寸、按需 Skill 和 Agent methods。
- 插件节点状态不再塞进画布 JSON，而是存入 workspace SQLite 的节点级 KV：
  `plugin_node_storage_scopes` + `plugin_node_storage_entries`。
- 内置示例包括 ComfyUI、Clip Studio 和 3D 导演台；插件既是 UI，也可以向 Agent 暴露结构化方法。

## 3. 值得学习的关键决策

### 双模式共用数据

自由画布用于探索、参考收集和并行创作；工作流用于表达依赖与执行顺序。两者不应成为两个互相跳转的产品，也不应复制两份节点。

### 资产先于消息

图片或文件不是聊天附件的临时副产物，而是带身份、依赖、版本和来源的工作区资产。聊天只是创建、解释和调度资产的入口之一。

### 插件节点是隔离运行单元

插件拥有独立 manifest、UI、方法和存储空间。画布只保留引用与布局，插件数据按节点隔离并设置配额，能显著降低画布保存成本与故障半径。

### Agent 写画布必须有门禁

Agent 可新增和修改节点，但保存应带 revision，并拒绝大面积误删。用户编辑、Agent 编辑与后台生成需要统一冲突模型。

## 4. Octopus 对应设计

| MiniMax Design | Octopus 基座                        |
| -------------- | ----------------------------------- |
| Workspace      | 项目 + 项目群 + 工作目录            |
| Agent          | 白幽灵角色与云端职业角色            |
| Skill          | 现有 `/api/skills` 与角色技能包     |
| Plugin         | 现有 PluginHub / Codex Plugin / MCP |
| Workflow run   | 实时会话执行、团队模式与项目里程碑  |
| Asset graph    | 本地数据库、工作区文件、产物面板    |
| Canvas         | 新增 Octopus Design 双模式画布      |

## 5. 落地阶段

### M1：平台骨架（已开始）

- 新增 `/workspace/design` 与侧边栏入口。
- 自由画布 / 工作流双模式。
- 无限平移、缩放、节点拖拽、连线、适应视口、自动整理。
- 接入真实本地 Skill / Plugin 列表，点击即可生成能力节点。
- 画布本地持久化。
- “交给 AI 执行”将画布编译成任务，进入现有实时执行基座。

### M2：项目与资产（进行中）

- [x] 项目详情提供“创作画布”入口，画布按 project id 隔离持久化并显示项目归属。
- [x] 项目画布保存到后端共享文档，带 revision 乐观锁、原子写入、项目权限作用域和 2 MB 上限。
- [x] 项目画布支持同浏览器窗口即时广播、跨设备 2.5 秒修订轮询和三方增量合并；不同节点的并发修改自动合并，同节点冲突由成员选择“合并保存”或“载入新版”。
- [x] 项目 Design 画布显示在线席位与跨设备多人光标；席位带 8 秒过期和 64 人上限，光标通过轻量心跳近实时同步。
- [x] 绑定项目的群聊右侧工作台提供“设计”页签，直接挂载同一项目 ID 的 Canvas document；画布不塞进消息气泡，也无需离开当前群聊来回跳页。
- 将工作区文件、消息产物和媒体素材拖入画布。
- 运行结果回写输出节点，并保留来源边。
- 保存画布版本和撤销历史。

### M3：可执行编排

- 节点端口、条件分支、并发、重试和人工确认。
- Agent / Skill / Plugin 参数面板。
- 更细粒度的 WebSocket/CRDT 实时操作流（当前已有在线席位、多人光标、revision、三方合并和破坏性保存保护）。
- 运行状态、耗时、成本与错误沿节点和边可视化。

### M4：插件工作台

- 插件 manifest 声明 editor surface、尺寸、方法与按需技能。
- 插件 iframe/webview 沙箱和权限门禁。
- 节点级 KV 存储、配额和迁移。
- ComfyUI、3D 导演台、视频剪辑等垂类工作台接入。

## 6. 原生视觉走查记录

本次直接操作 MiniMax Design 3.0.2，而不是只读代码。关键尺寸与交互如下：

- 创作首页使用点阵无限画布背景，品牌区居中；主输入器约 760 px 宽、24 px 圆角，模型与 Skill 入口位于输入器底部，下面直接承接项目选择、胶囊分类和三列媒体案例卡。
- 首页输入器只保留添加文件、模型、Skill 与发送，ComfyUI/工作流不在这里抢占主操作位；顶部另有更新公告和 Design/H3 使用指南。
- “模型”不是传统单选下拉，而是 Agent 可调用能力白名单：包含全选、视频/图片/音频分类、模型规格和逐项开关，默认全开。
- 一级侧栏宽约 264 px；画布占主区域，对话面板默认约 300 px。
- 工作区支持四种布局：对话 + 画布、对话在左、仅对话、仅画布，均在当前页面内切换。
- 画布为白色点阵背景；底部中央是紧凑工具胶囊，右上是视口控制胶囊。
- 添加节点是约 330 px 的按需菜单，而不是常驻能力库。原生入口包括文本、表格、图片、视频、音频、3D 导演台、视频剪辑与 ComfyUI 工作流。
- 节点设置、项目资产和帮助都按需出现；常态画布尽量不被面板占用。
- 3D 导演台以“导演视角 / 机位视角”为顶部主切换，中央是 3D 视口和底部工具胶囊，时间线独立占底部，右侧属性栏依次提供场景缩放、XYZ 平移、XYZ 旋转、天空颜色、背景图片、全景/平面、水平旋转、球形半径与角色标签；场景设置和时间线缩放是两套状态。
- Skill 与 ComfyUI 使用相同的市场页范式：标题和一句说明、主操作、分类/搜索、四列视觉卡片。
- Skill 市场不是单层卡片库：顶部为“Skill / 我的 Skill”，下方依次是官方精选、用户精选与其他 Skill；卡片悬浮出现详情和下载动作，并显示官方认证与下载量。实机可见的官方精选覆盖 3D 动画、极简产品广告、纸拼贴、微表情、FPV、多语言配音、B-roll、数字产品宣传与动态海报等。
- ComfyUI 市场使用“精选工作流 / 我的工作流”双标签与四列模板卡。每张模板有预览、说明、来源和下载动作；导入或新建后仍在 Design 当前工作区打开，不跳到外部产品。
- 资产中心以角色、场景、风格包、道具和自定义为稳定分类，同时提供网格/列表视图。
- 资产中心的“导入资产包”是正式入口而非说明按钮。Octopus 现支持带 `manifest.json` 的 ZIP 资产包：逐项保留名称、分类、描述、标签和来源包，限制条目数与解压体积，拒绝加密项、符号链接、重复路径和目录穿越，并在任何一项失败时回滚整包；导入结果立即进入同一资产库和画布复用链路。
- Octopus 首页已按上述层级重构：案例改为三列真实项目图片，项目选择和添加文件形成真实链路；模型面板读取当前真实 Agent 模型，并把基座图像生成、ComfyUI 本地工作流和内置剪辑工坊按图片/视频/音频分类。勾选结果本地持久化并随创作任务交给画布 Agent，而非只复刻外观。

### 6.1 主包视觉 token（3.0.2）

从 `app.asar/out/renderer/assets/index-BGYdCymP.css` 可直接验证：

- 浅色/深色画布底色分别为 `#fafafa` / `#0a0a0a`。
- 节点底色分别为 `#ffffff` / `#1a1a1a`，边框为 `#e3e3e3` / `#4a4a4a`。
- 媒体节点外圆角 16 px、内圆角 14 px；主控件背景为白色/`#1a1a1a`。
- 普通连线为 `#c4c4c4` / `#525252`，流程高亮为 `#8B72FF`，光晕为 `#C996FF`。
- 面板阴影为 `0 2px 5px`；菜单阴影为 `0 8px 32px` + `0 2px 8px`，比常见的重浮层更克制。
- 首页输入框半径 24 px、编辑区最小高度 90 px；场景快捷标签高 52 px、最小宽 148 px、圆角 14 px。

Octopus Design 的画布底色、节点圆角/边框、连线、控制胶囊和菜单阴影已按这些 token 校正。

这些结论已用于重构 Octopus Design：移除左右常驻能力库和检查器，换成按需节点菜单、浮动节点设置、项目资产面板和四种工作区布局。

## 7. 解包能力与许可边界

### 可验证能力

- 创作 Skill：动画游戏 PV、品牌广告、电影片头、音乐 MV、教育视频、视觉设计、KOC 视频、UI 动效、视频拆解。
- 工作流：广告 TVC、短剧、通用视频、MV，以及共享资产/音频/字幕/视频合并管线。
- 3D 导演台方法：场景读取与批量编辑、程序化模型生成与对比、多视角快照、诊断、相机路径与动作 DSL。
- 剪辑方法：项目读取/编辑/预览/历史/快照/诊断，覆盖轨道、片段、字幕、转场、效果和调色。
- ComfyUI：完整节点编辑器、本地后端连接、工作流导入和运行队列。

当前方法级覆盖（以解包 manifest 为证据，不把相似命名算作完成）：

| 插件 | 已有真实闭环 | 仍未完成 |
|---|---|---|
| 3D 导演台 | `scene.get/edit/history/snapshot/diagnostics`、安全声明式 `model.generate/capture/compare`、`motion.read`、`campath.read` | 不执行 MiniMax 的任意 JavaScript 模型代码；改用可审计声明式几何 |
| AI 剪辑工坊 | `project.get/edit/view/history/snapshot/diagnostics/export`，含真实媒体抽帧、真实音频静音分析与波纹切除、字幕、15 类效果、7 类转场中间态、范围编辑、SRT、调色、多层视频合成、片段/效果参数关键帧与带音轨 MP4 编码导出 | 长片后台渲染队列、硬件编码器选择和专业响度标准化 |
| ComfyUI | 用户触发的托管安装/取消/稳定版更新、隔离 Python 环境、官方 Registry 节点扩展浏览/安装/更新/可恢复卸载/回滚、Hugging Face/Civitai 公开模型链接逐项授权下载与可恢复移除/还原、已有安装的受控启动/停止、模型/扩展盘点、`object_info` 动态节点规格、工作流列表/读取/导入、原生 API-prompt 节点编辑、坐标与 revision 持久化、按 ID 排队、结果轮询与输出预览、节点/输入/枚举/本地模型兼容性诊断 | 带封面/作者/许可证的完整模型目录、私有模型凭据托管、节点扩展之间的依赖版本求解 |

“仍未完成”项保留为明确验收缺口；不以结构 JSON、占位图、静态模板或自动下载未知模型代替。

### 迁移规则

- MiniMax 自有 Skill 文本、工作流文本和 3D 插件未提供可分发许可证，因此只迁移能力结构与交互范式，不复制源码或提示词。
- Clip Studio 包附带 OpenReel MIT 许可。Octopus 当前先提供原创轻量剪辑插件，并保留后续对接 OpenReel 上游的兼容方向；未复制解包产物。
- ComfyUI 采用本地服务桥接：只允许 `localhost` / loopback 地址，支持状态探测、工作流 JSON 持久化和队列提交，不自动下载模型权重。
- 大模型资源可能达到约 40 GB，必须由用户明确选择后再下载，不能作为应用默认迁移内容。

## 8. 当前已落地

- 画布视觉结构与工作区布局已按原生走查结果重构。
- 画布视图控制继续按解包键名与实机行为补齐：点阵/网格/纯色背景、默认/纸白/冷灰/暖灰/雾蓝/雾绿/淡紫/浅粉/沙色九种色调、连线显隐、真实节点小地图、空画布操作提示、以指针为中心的滚轮缩放、Space 临时小手及 V/H 工具快捷键均可实际使用，视图偏好本机持久化。
- 空白处双击会在对应世界坐标打开添加节点入口；整理不再是一键黑盒，支持按连线、媒体类型、网格、水平和垂直五种真实布局，并在应用后提供“保留 / 恢复”确认。
- 自由画布与工作流现在分别保存节点坐标；切换模式会恢复该模式上次排版，拖动和整理不再覆盖另一种模式的探索布局，同时保持旧版 v1 画布文档可读取。
- 节点选择支持 Shift/⌘ 追加选择；两个以上节点可组合为带边界的真实分组，拖动分组会同步移动成员，且可无损解散。分组关系、颜色与两种模式的位置进入同一画布文档并参与项目同步。
- 贴纸工具按实机语义区分“跟随目标 / 自由贴纸”：跟随贴纸绑定产物并随拖动、整理一起移动，自由贴纸保持画布绝对位置；批量清空前显式确认且可整体撤销一次，关系同样随项目画布持久化。
- 选择工具拖动空白区域会绘制矩形框选，Shift/⌘ 可追加，成组节点能一起拖动；平移只由小手工具或 Space 临时小手触发，消除了选择模式误移动画布的问题。
- 工作流节点左右端口已由装饰圆点改为真实编排入口：从右侧端口开始、预览虚线连接并在目标左侧端口完成；重复、自连和非执行节点连接会被模型层拒绝，节点设置中可查看输入/输出关系并逐条解除，Esc 可取消未完成连接。
- 工作流连线补齐解包中的 connect-to-add-node：从输出端口拖到现有目标会直接完成连接，拖到空白位置则在落点打开添加节点菜单，选择节点后自动继承来源连线；单击开始连接后点击空白也走同一路径，关闭添加菜单会取消悬空连接。
- 添加节点弹层不再永远固定在底部工具条上方：双击空白、右键“添加节点”和连线落空都会以对应画布坐标为锚点打开，并按 330px 弹层宽度及可见高度在四周边界内回收；底部“+”仍使用居中入口。
- 画布加入上限 80 步的统一撤销/重做历史，覆盖节点增删、连线、分组、贴纸、整理、颜色和文本编辑；拖动及连续输入分别合并为单次事务，避免每个像素或字符占用一步，支持 ⌘Z / ⇧⌘Z。
- 画布补齐与 MiniMax 实机一致的节点/空白区分层右键菜单和 ⌘C / ⌘V / ⌘D：节点菜单提供“复制节点”、编组/解组与删除；空白菜单按实机 240px 层级提供上传、定点添加、撤销/重做和粘贴，上传继续进入项目资产持久化链路。复制分组时会带上成员、目标跟随贴纸、内部连线及选区外指向选区内的上游输入，粘贴后统一重建 ID、分组归属、贴纸绑定和边关系，快捷键重复粘贴逐次偏移，右键粘贴以指针位置为中心。
- 右键菜单继续对齐解包样式参数：空白菜单使用 240px、`text-sm`、8px 内边距和分段快捷键；节点菜单使用 192px、图标间距 10px 与同级字重，点击菜单外或 Esc 会关闭。删除分组会连同成员和跟随贴纸处理；当节点或连线不少于 10 个且一次操作将剩余量降到一半以下时，按原生阈值弹出“大量画布内容”确认，并在确认前检测画布是否已发生变化。
- 文件型节点的右键菜单继续补齐实机动作：“添加到对话”会把稳定资产 ID、路径、地址和说明写入同页协作对话；“重命名”进入可回车提交的真实弹层；“存为资产”会读取节点源文件并写入用户级资产库；“存到项目资产”会上传到当前项目、发布持久项目产物，并把节点重新绑定到返回的新资产 ID。所有网络失败均显式反馈，不用静态菜单或成功提示冒充保存完成。
- 原生素材节点右键实机顺序进一步确认为“添加到对话 / 复制 / 另存为 / 重命名 / 标签 / 复制节点 / 存为资产 / 在 Finder 中打开 / 删除”。Octopus 现以剪贴板复制稳定资产引用，以鉴权读取后的真实 Blob 触发另存，以画布节点字段持久保存最多 12 个标签；Finder 入口仅在桌面桥存在且资产是绝对本地路径时显示，Web 或项目相对路径不展示无法兑现的按钮。
- 原生媒体节点实机确认不使用统一白色信息卡：名称与媒体图标位于素材上方，图片或视频预览本身构成节点主体，选中态只环绕素材区域；9:16 视频按真实比例显示为窄高节点，而不是裁成固定横卡。Octopus 已将带真实地址的图片、视频和音频拆为独立媒体节点，并在媒体元数据加载后按固有宽高自动计算节点尺寸（9:16 为 190×363，16:9 为 260×171）；视频和音频提供原生播放控制，悬浮显示真实“另存为”，工作流左右端口、拖动、框选和右键资产动作保持不变。没有可预览地址时仍降级到通用文件节点，不以空白播放器冒充素材。
- ComfyUI 模板复制序号按解包字段 `comfyuiTemplateCopyOrdinal` 的语义进入 Octopus 画布文档：同一工作流身份依次显示“副本 1 / 副本 2”，序号随项目画布持久化，复制已有副本仍从该模板当前最大序号递增。
- 原生客户端视觉走查确认 ComfyUI 首页把导入和空白创建收进同一黑色“导入/新建工作流”按钮，下拉只有“导入本地工作流 / 在画布中创建”两项；Octopus 已按相同层级收敛，两个动作分别继续走真实 JSON 导入与空白画布创建，不再额外平铺“空白工作流”按钮。
- 同次实机走查确认底部“添加节点”是约 236px 的单列浮层：标题下依次只有文本、表格、图片、视频、音频、3D 导演台、视频剪辑和 ComfyUI 工作流，视觉层只显示 32px 图标、名称与必要徽标。Octopus 已移除该浮层中不可达且偏离原生的角色/Skill/插件页签、描述副行与“交付物”；角色、Skill 与可扩展插件仍分别从资产中心、Skill 市场和插件节点入口加入画布，能力不因快捷菜单收敛而消失。点击外部或 Esc 会关闭菜单并清理悬空连线。
- 原生底部工具条实机确认常态仅保留“添加节点、移动/小手工具、项目资产、帮助指南”。Octopus 已把两个独立工具按钮收为带 V/H 快捷键的二选一下拉，并移除常态栏上的分组、解组和贴纸噪声；分组仍在节点右键菜单，贴纸移至空白画布右键入口。帮助指南按原生四项提供教程、反馈、功能许愿和快捷键，其中反馈与功能许愿真实提交到现有 `/api/feedback` 日志，不是提示气泡。
- 2026-08-24 再次实机截图确认缩放与视图胶囊独立固定在画布顶部正中，依次承载整理、缩小、缩放百分比、放大、画布设置、连线和小地图；撤销/重做不占用该常驻胶囊。Octopus 已把原先偏右的工具条移到相同锚点，并把整理与画布设置浮层同步改为从顶部胶囊展开；撤销/重做仍由 ⌘Z、⇧⌘Z 和空白画布右键菜单真实提供。
- 节点菜单已覆盖文本、表格、图片、视频、音频、3D 导演台、视频剪辑、ComfyUI 和交付物。
- 58 个 Octopus 原创创作 Skill 已进入 `all_skills`。覆盖视频、剪辑、分镜、导演台、ComfyUI 工作流、电商图、品牌视觉、配乐、声音设计、播客、动漫、像素艺术、多视角渲染、局部重绘和微短剧等方向；全部通过 Skill 结构校验。
- Skill 市场已按实机结构加入分类、搜索、市场/已安装标签、四列卡片、官方标记、悬浮详情与加入画布动作。
- 新增本地 ComfyUI 桥接 API：状态、内置/用户工作流列表、工作流详情、导入、版本化保存和运行队列。内置三份只使用 ComfyUI 核心节点的原创模板：基础文生图、参考图重绘和潜空间高清放大；模型文件仍由用户本机选择。
- Design 内置深色无限节点工作台：可拖动节点、编辑参数、建立/解除连线、新增/删除节点、保存画布坐标并直接提交队列；在线时从本机 `object_info` 读取真实节点目录，离线时降级到核心节点；默认不再依赖离线时空白的 iframe，同时保留切换本机 ComfyUI 原生界面的入口。
- ComfyUI 市场明确区分“已内置”和“需依赖”：三份核心模板可直接导入，其余七类高级工作流仅作为能力目录展示，在本地模型或扩展就绪前不会伪装成可运行模板。
- 项目详情已接入项目专属 Design 入口；同一浏览器中的不同项目使用独立画布存储，标题栏持续显示项目归属并可返回项目管理。
- 项目画布现以本地存储作为离线缓存，并自动同步到 `/api/design/projects/{project_id}/canvas`；标题栏显示载入、保存、已同步、冲突或仅本地状态。后端以 revision 拒绝静默覆盖其他成员的新版本。
- 项目画布协作增加双通道文档同步：同浏览器多窗口通过 `BroadcastChannel` 即时通知，不同设备每 2.5 秒拉取服务器修订；以前的 409 后强制刷新已改成三方合并，不同节点修改自动保留，真正重叠的修改才进入显式冲突选择。在线席位和画布光标另以 750 ms 心跳同步、8 秒自动过期并限制 64 人；它提供近实时协作反馈，但仍不冒充 WebSocket/CRDT 操作流。
- 3D 导演台、AI 剪辑工坊和 ComfyUI 桥接均已提供 PluginHub manifest，可在插件市场被发现；三个插件同时注册 Agent 可发现的结构化 Skill，不再只有网页入口。
- 插件节点仍在当前“对话 + 画布”布局内打开，不整页跳转；打开剪辑、导演台或 ComfyUI 时左侧协作区分别切换为剪辑 Agent、导演台 Agent 或 ComfyUI Agent 的任务语义。2026-08-24 实机进入剪辑插件确认其自身顶栏已包含导出和“退出编辑”，因此 Octopus 删除了重复的 44 px 宿主标题栏，由插件通过受限消息关闭当前工作台；默认项目布局改为与 MiniMax 实机一致的左对话、右画布，仍可切换右对话、仅对话或仅画布。
- 新增内置 AI 剪辑工坊插件：本地媒体预览、三轨编排、字幕片段与项目 JSON 导出。
- AI 剪辑工坊新增 `project.get/edit/history/view/diagnostics` 五类真实接口；批量操作原子提交，失败整批回滚，支持撤销/重做、轨道/媒体/字幕/片段/标记操作与黑场、重叠诊断。内嵌编辑器已读取同一项目并展示 Agent 写入的时间线。
- 剪辑原子操作继续覆盖到可实际编排的范围：复制片段、关闭间隙、波纹删除、范围切除、SRT 导入、统一字幕样式、转场、效果与调色；诊断新增短片段、媒体丢失和字幕越界。自动剪静音已接真实音频分析；快照也不以静态占位图冒充真实渲染。
- 剪辑 `project.snapshot` 已改为真实媒体渲染：PyAV 定位视频源帧，Pillow 完成画布适配、字幕和 MiniMax 实机菜单对应的 15 类效果合成（亮度、对比度、饱和度、色温、色调、色相、模糊、运动模糊、径向模糊、锐化、暗角、胶片颗粒、投影、辉光、色差），并真实渲染 7 类转场中间态（交叉淡化、淡入黑场、淡入白场、擦除、滑动、推动、缩放）。内嵌编辑器的效果/转场目录已可选择时间线片段并写入同一项目；未知类型仍显式返回 warning。
- 剪辑快照与导出已从“只取最上方一个片段”升级为真正的多轨合成：按视频轨道顺序叠加，支持独奏/隐藏、位置、缩放、旋转、不透明度以及正常/滤色/正片叠底/添加混合模式。片段变换和效果 amount/value 都可写入 linear、hold、ease-in/out/in-out 关键帧；内嵌编辑器提供调整面板和播放头关键帧入口。
- 新增 `clip_studio.project_export` 与同源下载端点：PyAV 逐帧编码合成结果为 MP4，混合视频/音频轨上的本地音源并编码 AAC，保留字幕、效果、转场和关键帧。导出最长 120 秒、最大边 1280，避免同步请求占满本机；长片后台队列仍列为后续缺口。
- 剪辑 `cut_silences` 使用 PyAV 解码真实音轨并按 RMS/dB 聚合连续静音，在同一事务内反向波纹切除；切分后同步维护 `sourceInSec/sourceOutSec`，保证后续快照仍读取正确源帧。
- 新增原创 3D 导演台：场景树、WebGL 视口、三种角色素体、19 种姿态、场景属性、可见运镜片段、路径/动画时间线和图片导出均为可操作状态。后端提供 `scene.get/edit/history/snapshot/diagnostics`、`motion.read` 与 `campath.read`，批量编辑失败整体回滚；新增 14 类可审计道具目录、对象移动路径、重命名、环境设置与通用移除，Agent 写入的道具会在 Three.js 场景树和真实预览中呈现。前端的素体、姿态、位置、天空色和相机路径已按项目自动保存并可在刷新后恢复。`scene.snapshot` 读取前端同步的真实 WebGL PNG；编辑器未打开时返回 `PREVIEW_NOT_READY`，不再把结构 JSON 冒充视觉快照。
- 3D 导演台进一步按实机属性栏补齐：场景根节点的缩放、XYZ 平移和 XYZ 旋转会真实作用于 Three.js 世界并持久化；用户可选择不超过 2.8 MB 的背景图，以全景球或平面背景显示，水平角度和球形半径可调；角色标签按 3D 坐标投影到界面并可关闭。底部时间线缩放已与场景缩放分离，不再互相误改。
- 导演台时间线不再只是静态色块：播放与拖动播放头会真实插值相机路径和角色/道具/模型移动路径，角色动画片段驱动标准姿态、循环步态及 Octopus 动作 DSL；暂停或定位后把对应 WebGL 帧同步给 Agent 视觉快照。配套原创 Skill 已补齐镜头语言、动作设计、动作 DSL、运镜路径 DSL 与声明式模型搭建参考。
- 导演台加入安全声明式 `model.generate/capture/compare`：Agent 用盒、球、圆柱和圆锥部件生成可持久化模型，Three.js 画布读取同一数据，服务端生成 front/side/top/iso 多视角 PNG，并可输出参考图像素差异图。该实现不执行任意 JavaScript，避免把解包插件的代码执行面直接迁入主进程。
- Agent 可直接调用 `clip_studio.project_get/edit/history/view/snapshot/diagnostics/export`、`director_stage.scene_get/edit/history/snapshot/diagnostics`、`director_stage.model_generate/capture/compare`、`director_stage.motion_read/campath_read` 与 `comfyui_bridge.status/dependencies/workflows/workflow_get/workflow_save/workflow_diagnostics/queue/result`；测试已验证写入、撤销、播放头定位、多轨合成、关键帧、带音轨 MP4、工作流节点坐标、revision 持久化和依赖诊断，ComfyUI 队列仍只接受本机地址。
- 新增供应商中立的 AI 漫剧分阶段模板：剧本与集数范围、制作方案、角色/场景锚点、分镜与站位、场景关键帧、镜头生成、剪辑交付共七个稳定阶段。阶段状态、尝试次数和依赖随画布持久化；前置阶段未通过时阻止误执行，Agent 结果只推进本轮锁定阶段并进入人工审核，重试会保留上游已通过内容、仅重置可能受影响的下游阶段；同轮多个交付文件自动成组。
- 实机资源复核确认 MiniMax Design 3.0.2 随包仅有两份插件 Craft Skill（Clip Studio 与 3D Director Stage），并非另有一批隐藏的 ComfyUI Skill 文件。Octopus 已分别以 `creative-video-editor` 和 `creative-director-stage` 提供原创等价层；剪辑 Skill 现按需路由节奏/转场/静音/变速与字幕/调色/效果判断，导演台 Skill 覆盖 blocking、镜头语言、动作与运镜 DSL、程序模型和视觉验收。另新增供应商中立的 `creative-comfyui-workflow`，负责本机诊断、工作流修改、排队、结果读取和视觉验收；其余 55 个创作 Skill 是 Octopus 对实机市场方向的原创能力扩展，当前总量 58 个，不冒充解包原文。
- ComfyUI 执行链已延伸为 `workflow_get → queue → result`：Agent 和 Design 市场都可按工作流 ID 运行，无需手工拼接 prompt JSON；市场显示排队、生成、完成/失败和真实输出预览，结果查询仍只访问 loopback 本机服务。
- ComfyUI 市场加入用户主动触发的托管引擎安装：Octopus 在应用数据目录创建隔离虚拟环境，固定使用核对过的官方 `comfy-cli 1.17.0` 安装最新稳定版，后台任务支持阶段回显、取消、版本/commit 展示和停止服务后的更新。安装器不下载 checkpoint、LoRA、VAE 或 ControlNet；`comfyui_bridge.manager_status` 仅允许 Agent 只读查看状态，安装与更新端点不暴露为 Agent Skill。
- ComfyUI 节点扩展已接官方 `api.comfy.org` Registry：提供九个高使用量扩展入口与 Registry ID 精确查询。安装/更新会执行第三方依赖脚本，因此界面逐次确认且 Agent 无权触发；更新前自动移动旧源码到备份区，失败自动恢复；卸载同样移动而非删除，界面可一键回滚。服务运行中禁止修改扩展，避免已加载代码与磁盘状态不一致。
- ComfyUI 模型中心支持用户逐项粘贴 Hugging Face/Civitai 公开 HTTPS 文件链接，选择 checkpoint、LoRA、VAE、ControlNet 等目标目录后后台下载并可取消。链接会拒绝账号密码、token/API key 查询参数和非白名单主机；本地模型可盘点大小并以移动到备份区的方式移除、随后恢复。模型写操作没有注册为 Agent Skill，也不会随引擎或工作流自动下载；当前仍不是带封面、作者、许可证和搜索能力的完整模型市场。
- ComfyUI 工作流详情加入只读兼容性诊断：连接本机服务时以真实 `object_info` 核对节点类型、必填输入和枚举值，并扫描本机 checkpoints、diffusion models、VAE、LoRA、ControlNet、文本编码器、CLIP Vision 与放大模型目录；缺节点、缺模型、未知输入和非法枚举会逐项显示。离线或未配置模型目录时明确标记“未完整检查”，不把未知状态冒充可运行，也不会自动安装扩展或下载权重。
- 2026-08-24 的 MiniMax 实机走查补充确认：Skill 页采用“官方精选 / 用户精选 / 其他 Skill · 50”三级市场，带分类、搜索、安装态、仅看未安装和排序；ComfyUI 页则把“精选工作流 / 我的工作流”作为首层，只展示导入/新建、探索开源、搜索和四列卡片，复杂安装运维不抢占首页。当前可见模板是 H3 轻量版/满血版 × 文生视频/首尾帧/多参考，再分别叠加官方超清扩写或 fal LoRA 的组合；页面没有独立模型市场。Octopus 因此保留供应商中立的十类原创模板，不伪造 MiniMax H3 可运行性；58 个原创 Skill 可发现，引擎、模型与节点管理默认收进“本地环境”，工作流市场保持为主信息层。
- 同日原生市场逐项复核后，补齐七个此前没有独立工作流的方向：双人游戏开场、纸艺定格科普、技能课程详情页、文化角色英语启蒙、梦核阈限空间、电影风格规则提炼与有意低模影像。它们均为 Octopus 原创执行规范，不复制 MiniMax 官方或用户精选 Skill 的私有文本、封面与下载数据。
- 资产中心已从“项目文件上传”升级为用户级可复用资产库：创建弹层包含名称、角色/场景/风格包/道具/自定义类型、描述、文件和标签，后端按已认证用户隔离并持久化元数据与内容；项目产物仍独立保留，避免把全局角色资产错误绑定到单一项目。
- 画布“项目资产”已由左下角空浮层改为画布与对话之间的固定 280 px 中间栏，提供画布/资产双标签、树/网格、搜索、类型过滤、刷新、上传文件与“在画布中定位”；已有节点会居中定位，尚未加入的资产才创建节点。
- ComfyUI 卡片点击后进入工作流详情层，展示完整说明、资源文件、本地依赖、来源与许可及侧边工作流切换；内置模板只声明 Octopus 原创和 Apache-2.0，不复制 MiniMax 的模型包、封面、说明或第三方许可关系，也不伪造数十 GB 下载量。
- 工作流详情会读取真实 workflow JSON，逐项列出节点类型以及 checkpoint、VAE、LoRA、ControlNet、图像/视频/音频等文件输入；不再用泛化的“Checkpoint / VAE 由用户选择”代替真实依赖信息。
- Skill“查看详情”不再是提示气泡：弹层按实机结构提供左侧文件树、右侧文本预览与底部启用/加入画布操作；后端只允许读取 `creative-*` 原创 Skill 下的受限文本文件，拒绝路径穿越、符号链接、二进制和超大文件，不暴露任意运行时目录。
- 2026-08-24 实机打开“3D 动画短片”详情后确认：约 920px 宽的居中弹层顶部展示名称、分类标签与完整说明，左侧是可折叠目录树，右侧把 `SKILL.md` 渲染为正文，并将 frontmatter 作为带 YAML 标识和复制操作的代码块展示，底部只保留安装动作。Octopus 的 Skill 详情已从原始 `<pre>` 升级为同源 Markdown 阅读器，复用现有安全渲染、语法高亮和复制能力；`references` 等目录按真实层级折叠，安装/启用与加入画布拆成两个真实阶段。
- Design Skill 市场可直接把 58 个内置原创 Skill 按需加载进运行时、持久启用并立即加入画布；未加载的卡片不再只提示用户跳转到另一管理页，“我的 Skill”继续读取同一运行时安装状态。
- Skill 与 ComfyUI 精选卡片改用项目内已有图片和抽象壁纸作为真实封面层，并把安装、详情、加入画布和直接运行动作收进悬浮层；四列密度、页签下划线、搜索框和工作流详情双栏进一步贴近 MiniMax 实机层级，同时保留 Octopus 自己的素材与本地运行语义。
- 资产中心不再固定空白：读取 Octopus 真实角色资产，支持分类、搜索、网格/列表切换并可绑定到项目画布节点。
- 项目产物现通过 Project OS 的 `project.artifact_published` 事件进入 Design 资产中心；本地图片、视频、音频和文档可直接上传为带稳定 ID、项目归属、路径与预览 URL 的持久资产。加入画布后会保留 `asset` 绑定和来源信息，图片节点直接显示真实预览，Agent 执行提示同时携带资产 ID 与工作区路径。
- 画布右侧“创作协作”已接入真实实时对话线程：发送后在当前 Design 布局内运行，不再整页跳往普通对话；嵌入态复用现有 Agent 执行、消息流和项目参数，同时隐藏重复的全局侧栏。
- 插件节点状态已从画布文档剥离为独立 SQLite KV：命名空间按项目、插件和节点三层隔离，支持逐键 revision、冲突拒绝、单值 256 KB、单节点 2 MB 与 64 键配额。`PluginNodeFrame` 通过受限 `postMessage` 桥接 get/set/delete，父级固定权限作用域并代发鉴权请求，插件 iframe 不接触用户凭据，也不能自行切换到其他项目或节点。
- 同日实机进入 3D 导演台确认初始态：场景树默认收起；顶部依次是场景、导演/机位视角、虚拟摄像机、快捷键、撤销/重做、图片/视频导出和退出；中部是 WebGL 视口与七项浮动工具；底部时间线约 230 px；右侧固定约 286 px 场景属性栏，外层协作区切换为导演台 Agent。Octopus 已同步默认折叠、顶栏降噪、虚拟摄像机/快捷键入口、七项浮动工具和时间线高度，保留真实保存与画面同步但不再用常驻文字占据工具栏。
