---
name: creative-video-editor
description: 在 Octopus AI 剪辑工坊中读取和修改多轨视频项目；适用于剪辑、字幕、节奏、轨道整理、时间线诊断与可撤销批量编辑。
license: Apache-2.0
metadata:
  category: 平台工具
  origin: octopus-original
---

# AI 多轨剪辑

先读取项目，再做内容判断。不要从文件名猜素材内容，也不要在不知道片段、轨道和绝对时间的情况下生成编辑操作。

## 工作流

1. 调用项目读取接口；需要媒体路径或标记时使用完整视图。
2. 将同一用户意图的变更组成一个批次。涉及大量片段或高风险调整时先 `validateOnly`，验证通过后再提交。
3. 任何移动、裁切、分割、变速或删除后都运行时间线诊断。黑场和同轨重叠属于必须修复的错误。
4. 字幕、调色、效果或转场属于视觉变更；在可用时检查受影响时间点的预览帧，确认文字可读且不遮挡主体。
5. 将播放头停在结果附近，并用项目时长、改动范围和仍存在的警告总结交付。

详细接口和操作字段见 [references/api.md](references/api.md)。完成判定见 [references/verification.md](references/verification.md)。只读取当前任务需要的参考文件。

## 约束

- 所有时间使用项目秒数，不使用帧号或未经解析的时间码字符串。
- 轨道、片段和媒体 ID 必须来自项目读取结果；唯一前缀可用，不得编造。
- 用户点名轨道时按可见名称匹配；同名轨道不唯一时先澄清。
- 批量操作失败会整体回滚。根据失败项修正后重发完整批次，不要只补发剩余操作。
- 不自动导入用户未授权的外部文件，不覆盖其他项目，也不把依赖缺失包装成成功。

这是 Octopus 原创技能，不包含 MiniMax 私有提示词或未授权实现。
